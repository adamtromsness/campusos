# CampusOS

Cloud-native, multi-tenant School Operating System. Replaces 8–15 disconnected school software systems with one platform.

## Project Status

**Cycle 7 (Tasks & Approval Workflows) IN PROGRESS — Step 1 done.** Plan at `docs/campusos-cycle7-implementation-plan.html`; handoff at `HANDOFF-CYCLE7.md`. Step 1 lands `packages/database/prisma/tenant/migrations/026_tsk_tasks_and_auto_rules.sql` with 6 logical base tables for the M1 Task Management module: `tsk_acknowledgements` (per-(school, subject) PENDING / ACKNOWLEDGED / ACKNOWLEDGED_WITH_DISPUTE / EXPIRED with multi-column dispute_chk + ack_chk; soft polymorphic source_type + source_ref_id + source_table); `tsk_auto_task_rules` (per-(school, trigger_event_type) catalogue with priority + task_category enums + is_system flag distinguishing seed rules from school-authored; partial UNIQUE INDEX (school_id, trigger_event_type) WHERE is_system=true so schools can layer a custom override on the same event); `tsk_auto_task_conditions` (AND-ed conditions with 7-value operator enum EQUALS/NOT_EQUALS/IN/NOT_IN/GT/LT/EXISTS, JSONB value, ON DELETE CASCADE on rule); `tsk_auto_task_actions` (3-value action_type CREATE_TASK/CREATE_ACKNOWLEDGEMENT/SEND_NOTIFICATION + JSONB action_config + sort_order, CASCADE on rule); **`tsk_tasks` RANGE-partitioned by created_at monthly with 24 partitions** covering 2025-08 → 2027-08 (matching the msg_messages window from Cycle 3), composite PK (id, created_at) because partition column must appear in unique constraint, multi-column completed_chk enforcing TODO/IN_PROGRESS ⇒ completed_at NULL and DONE/CANCELLED ⇒ completed_at NOT NULL, FK acknowledgement_id → tsk_acknowledgements ON DELETE SET NULL replicates onto each of the 24 partitions plus the parent (25 pg_constraint rows for one logical FK matching the pay_ledger_entries.family_account_id precedent), partial INDEX(owner_id, source, source_ref_id) WHERE source != 'MANUAL' for auto-task dedup investigation (authoritative idempotency stays Redis SET NX in the future Step 4 Task Worker since partitioned-table UNIQUE constraints must include the partition column which would defeat dedup); **`tsk_tasks_archive` RANGE-partitioned by created_at annually** with 3 partitions covering 2025/2026/2027, same shape plus archived_at column, no FK back so a deleted ack leaves archived rows stable (the archiver job moving DONE/CANCELLED rows older than 30 days from tsk_tasks → tsk_tasks_archive is deferred to ops — schema-only this cycle). 4 new intra-tenant DB-enforced FKs (2 CASCADE on rules, 1 SET NULL on tsk_tasks → tsk_acknowledgements replicated 25× across the partitioned parent + 24 leaves), 0 cross-schema FKs. **Tenant logical base table count after Step 1: 114** (was 108 after Phase 2 polish; +6 logical tsk_* tables — the 24 monthly tsk_tasks partitions + 3 yearly tsk_tasks_archive partitions are not counted as logical base tables, matching the sis_attendance_records / msg_messages / msg_notification_log / msg_moderation_log / pay_ledger_entries precedent). **Live verification on `tenant_demo` (single BEGIN…ROLLBACK with savepoints, 12 assertions all green):** happy-path 5-table multi-insert; status_chk + completed_chk (both directions) + dispute_chk + ack_chk + operator_chk all fire; partition out-of-window rejection for 2024-12-31; partition routing inspection via `SELECT … FROM ONLY tsk_tasks_2026_04` confirms the row landed in the right leaf; CASCADE on tsk_auto_task_rules drops attached condition + action rows; SET NULL on tsk_acknowledgements clears tsk_tasks.acknowledgement_id while preserving the task row; partial UNIQUE rejects duplicate is_system=true rule and accepts is_system=false override on the same (school, event). Sanity counts: 24 monthly leaves + 3 yearly leaves + 25 FK constraint rows (24 partitions + 1 parent). **Splitter `;`-in-string trap not tripped this time** — Python audit script run before first provision attempt confirmed zero `;` inside any block comment or single-quoted string. First cycle since Cycle 4 to clear the splitter trap on first attempt. Idempotent re-provision verified (zero new applies on second run). (Steps 2–10 of Cycle 7 — Workflow schema, seed data, Task Worker Kafka consumer, Task NestJS module, Workflow Engine, Leave migration, Tasks UI, Approvals UI, vertical-slice integration test — remain to ship.)

**Phase 2 Parent Polish landed 2026-05-03 (5 commits, no review yet).** Five separate commits between `44dff03` and `c9d2de7` ship cross-cutting improvements identified during parent testing — none of them tied to a numbered Cycle. (1) `44dff03` — iPhone-style dashboard launchpad: app tiles shrink from aspect-square cards to a 56×56 rounded icon box with a label below; hover tints the icon-box from campus-50 to campus-100; badge moves to `-right-1 -top-1` with `ring-2 ring-white` so it sits on top of the icon-box edge. Grid widens 4 cols mobile / 6 cols desktop. (2) `9f8658c` — calendar event RSVPs + my-children filter + multi-school selector. Tenant migration `023_sch_calendar_event_rsvps.sql` adds the RSVP table (3-value GOING/TENTATIVE/NOT_GOING CHECK, UNIQUE(event, person), soft FK to platform.iam_person). New `CalendarRsvpService` with 3 endpoints under `sch-003:read` — `POST /calendar/:id/rsvp` upserts, `GET /calendar/:id/rsvps` (admin all, others own), `GET /calendar/:id/rsvp-summary` aggregate counts + my response. `CalendarService.list` accepts an optional `myKidsPersonIds` array; controller resolves the parent's own personId plus children's personIds (via `StudentService.listForGuardianPerson`) when guardian + `myKidsOnly=true`. UI: guardian-only `My children only` toggle + multi-school selector dropdown (only renders for 2+ schools — placeholder for cross-tenant aggregation). Event detail Modal grows an RSVP panel with chip buttons + aggregate counts. Phase 3 carry-over: full audience targeting on calendar events (filter is RSVP-only today). (3) `adc78d4` — Add Child workflow with admin approval. Tenant migration `024_sis_child_link_requests.sql` with mutually exclusive LINK_EXISTING / ADD_NEW shapes (multi-column `shape_chk`) + PENDING/APPROVED/REJECTED lifecycle (`reviewed_chk` keeps reviewer fields all-set or all-null). New `ChildLinkRequestService` with 6 endpoints under `/children` — search, link-request, add-request, list, approve, reject. `approve()` runs in `executeInTenantTransaction` with FOR UPDATE on the request row; ADD_NEW path atomically creates iam_person + platform_students + sis_students + sis_student_guardians + (when guardian has a household) platform_family_members CHILD entries. Emits `iam.child.linked`. UI: `/children` gains an Add child button with a 3-step Modal (SEARCH → PICK_RESULT → ADD_NEW); pending banner above the cards; new admin queue at `/children/link-requests` with status filter chips + per-row Approve/Reject Modal capturing reviewer notes. (4) `ae32299` — public enrollment search by location. Platform migration `20260503130749` adds `latitude` / `longitude` / `fullAddress` / `sharedBillingGroupId` (the last wired in commit 5) to `platform.schools`. Tenant migration `025_enr_period_allows_public_search.sql` adds `allows_public_search BOOLEAN DEFAULT true` to `enr_enrollment_periods` so existing OPEN periods auto-appear; invitation-only cycles flip false. New `EnrollmentSearchService` + `@Public()` `EnrollmentSearchController` at `GET /enrollment/search?lat=&lng=&radiusMiles=&gradeLevel=` (radius capped at 100 miles). Cross-tenant — phase 1 reads platform.schools + platform_tenant_routing then app-side Haversine filter; phase 2 enters each surviving tenant via the new `TenantPrismaService.executeInExplicitSchema(schemaName, fn)` helper. Two infra fixes the public endpoint surfaced: (a) `TenantGuard` is global so it ran on @Public() endpoints and threw on `getCurrentTenant()` — now reads `IS_PUBLIC_KEY` via `Reflector` and short-circuits, matching `AuthGuard`. (b) The middleware's exempt-path matcher used `req.path`, which Nest strips of the global `/api/v1` prefix before middleware runs — switched to `req.originalUrl` so the existing `/api/v1/health` entry actually matches (latent bug). Web: new `/find-schools` route at the app root (outside the auth-gated `(app)` layout) with lat/lng + "Use my location" button + radius slider + grade filter; login page gains a "Find a school accepting applications →" link below the account picker for unauth visitors. Verified live on `tenant_demo`: Springfield query finds Lincoln Elementary at 0.3 mi with the seeded Fall 2026 Admissions period; NYC query empty; grade=9 hits, grade=12 misses. (5) `c9d2de7` — multi-school billing structure. `family-account.service.ts` SELECT base joins `platform.schools` to surface `school_name` + `shared_billing_group_id` on every row; `FamilyAccountResponseDto` and the matching web `FamilyAccountDto` pick up the two new fields. Parent billing dashboard restructured to render one section per school, with sections grouped by `sharedBillingGroupId` when set (so two schools sharing a district billing module collapse into one combined section). Demo case (single tenant) shows one section with "Lincoln Elementary" header. Per-account UI extracted into `FamilyAccountSection.tsx` so the page is now a thin layout that maps over grouped accounts. Phase 3 carry-over: true cross-tenant aggregation (since tenancy is still per-request, this page only ever sees the current tenant's accounts). Schema impact: 2 new tenant tables (`sch_calendar_event_rsvps`, `sis_child_link_requests`) and 1 new tenant column on `enr_enrollment_periods` bring tenant base table count from 106 → **108**. 4 new platform columns on `platform.schools`. **HANDOFF-PHASE2-POLISH.md** carries the full per-feature detail; this list summarises what changed and where.

**Cycle 6 (Enrollment & Payments) is COMPLETE + APPROVED at `64993a8` (REVIEW-CYCLE6-CHATGPT — final verdict). Round 1 against `f7db6a5` returned REJECT pending 8 actionable fixes; Round 2 against `64993a8` returned APPROVED** after all 8 fixes landed. REVIEW-CYCLE6 fixes (2026-04-29): the reviewer surfaced 9 critical/blocking + 7 majors. Triage triaged 3 claims wrong (1 ResolvedActor.employeeId already on the interface; 2 Kafka consumer error-swallowing already fixed by REVIEW-CYCLE3 BLOCKING 1; 5 StudentService visibility already on actor.employeeId since Cycle 4 Step 0), 5 accepted DEVIATIONs (13 consumer envelope tenant validation already on Phase 2 from REVIEW-CYCLE5; 14 v11 platform endpoints forward-looking; 15 ledger immutability triggers ADR-010 explicit choice; 16 outbox pattern already on Phase 2 from REVIEW-CYCLE5; 11 the name-based sis*students link a documented Phase 2 design), and 8 valid bugs fixed in the closeout commit: (Fix 3 BLOCKING) `GET /classes/:id/attendance/:date` now resolves the actor + applies row scope (admin/class teacher → roster, student → own row, parent → linked children's, else 404) and lazy `PRESENT/PRE_POPULATED` writes only fire for managers — verified live with 6 scenarios on `tenant_demo`; (Fix 4 BLOCKING) `getStudentGradebook` filters classes to STAFF caller's `sis_class_teachers` rows so a teacher of one class can't see other classes' grades — verified live by stripping Rivera from 5 of 6 classes and watching the response drop from 4→1; (Fix 6 BLOCKING) `InvoiceService.cancel` writes a compensating `ADJUSTMENT` ledger entry equal to `-(total - sum(completed payments))` for non-DRAFT invoices so the family balance stays correct after cancel — verified live with Tech Fee SENT cancel dropping balance $400→$0; (Fix 7 BLOCKING) shared `INVOICE_AMOUNT_PAID_SQL` formula nets out completed refunds (`SUM(payments WHERE status IN (COMPLETED,REFUNDED)) - SUM(refunds WHERE status=COMPLETED, on payments for this invoice)`), `RefundService.issue` locks the parent invoice **before** the payment row to match `PaymentService.pay`'s lock order, and after the ledger write recomputes invoice status PAID ↔ PARTIAL ↔ SENT — verified live with $400 pay→PAID→$50 refund→PARTIAL→$50 pay→PAID; (Fix 8 BLOCKING) `generateFromSchedule` per-family invoice insert now runs inside `executeInTenantTransaction` with a per-(family, fee_schedule) `pg_advisory_xact_lock` and in-tx existence re-check — verified live with 5 parallel calls landing exactly 1 winner + 4 skipped; (Fix 9 MAJOR) `CapacitySummaryService.recompute` takes a per-(period, grade) advisory tx lock so concurrent transitions serialise — verified by build-artifact + happy-path; (Fix 10 MAJOR) `OfferService.issue` no longer emits `guardianPersonId: dto.familyResponse` (copy-paste bug that always emitted null on fresh ISSUED offers); now reads `guardian_person_id` from the locked application snapshot returned by the inner tx — verified live by capturing the wire envelope on `dev.enr.offer.issued` showing the correct guardian id; (Fix 11 MAJOR) `PaymentAccountWorker.createOrLinkAccount` takes a per-school advisory tx lock before computing `MAX(account_number)+1` so concurrent enrolments serialise on the FA-#### allocation — verified by build-artifact inspection; (Fix 12 MINOR) tenant-resolver JSDoc rewritten to accurately describe the actual middleware (header-first `X-Tenant-Subdomain`, then hostname). The `RefundService.issue` lock-order change (invoice → payment) now matches `PaymentService.pay`'s order. See `REVIEW-CYCLE6-CHATGPT.md` for the full triage table + verification trail. **Cycle 6 ships clean to Round 2 of the post-cycle architecture review.** **Step 12 lands `docs/cycle6-cat-script.md`** — the reproducible end-to-end vertical slice CAT, verified live against `tenant_demo` on 2026-04-29. The script wraps a 4-check schema preamble (106 tenant base tables / 10 enr* tables / 12 pay* tables / 0 cross-schema FKs, plus row-count verification of the 19 seeded tables) around the plan's 12 scenarios: S1 admin admissions pipeline (4 seeded applications across SUBMITTED→ENROLLED); S2 parent row-scope (David Chen sees only Maya); S3 parent submits new "CatStep12 Chen" Grade 9 application → guardian*person*id stamped from `actor.personId`, capacity_summary recomputes Grade 9 received 3→4; S4 admin transitions SUBMITTED→UNDER_REVIEW→ACCEPTED with `assertTransitionAllowed` + row lock + admin notes; S5 admin issues UNCONDITIONAL offer (response_deadline +14d); **S6 keystone — parent ACCEPTS offer**: offer flips to ACCEPTED + application to ENROLLED + capacity_summary Grade 9 offers_accepted 1→2 / available 109→108 + **`enr.student.enrolled` envelope captured live** on `dev.enr.student.enrolled` with full ADR-057 shape (`source_module:'enrollment'`, payload includes applicationId/offerId/schoolId/enrollmentPeriodId/studentFirstName='CatStep12'/studentLastName='Chen'/studentDateOfBirth/gradeLevel=9/admissionType=NEW_STUDENT/guardianPersonId=David Chen iam_person.id/guardianEmail/enrolledAt) + **PaymentAccountWorker reaction verified**: UPSERT idempotency hit (FA-1001 already exists for David), graceful skip of `pay_family_account_students` insert because `sis_students` row for CatStep12 doesn't exist yet (the documented Step 7 edge case for the future EnrollmentConfirmedWorker); S7 admin billing reads (1 family account, 2 invoices); S8 `generate-from-schedule` idempotency check on Tech Fee 2026 → `{created:0, skipped:1}` (UNIQUE-by-(family, fee_schedule_id, non-CANCELLED status) catches the seeded SENT row); S9 parent partial-pay $200 → invoice flips SENT→PARTIAL + balance 400→200 + PAYMENT ledger entry + **`pay.payment.received` envelope captured live** with full ADR-057 shape (payload includes paymentId/invoiceId/familyAccountId/amount/paymentMethod=CARD/invoiceStatus=PARTIAL/totalAmount/amountPaid/paidAt); S10 parent pays remaining $200 → invoice flips PARTIAL→PAID + balance 200→0 + second PAYMENT row + second `pay.payment.received` envelope + ledger SUM = $0 over 5 entries + overpay attempt 400 ("Invoice is already PAID"); **S11 admin partial-refund $50** → REFUND ledger entry restoring balance 0→50 + payment stays COMPLETED (partial-refund leaves it as-is, only full-amount flips to REFUNDED) + ledger SUM = $50 over 6 entries + **`pay.refund.issued` envelope captured live** with full ADR-057 shape (payload includes refundId/paymentId/familyAccountId/amount/refundCategory=GOODWILL/reason/status=COMPLETED/authorisedBy/completedAt + mock `re_dev*<uuid>`Stripe id); S12 6 permission denials covering all three tiers — gate-tier 403 (teacher /family-accounts no fin-001:read; student POST /pay no fin-001:write; parent POST /fee-categories no fin-001:admin; parent PATCH /applications/{Aiden's}/status no stu-003:admin), service-layer admin-only (parent /refunds "Only admins can list refunds"), service-layer self-service rule (parent CASH method "Self-service parent payments accept CARD or BANK_TRANSFER only"). **All 12 scenarios pass.** Cleanup section drops the new application + 1 refund + 2 payments + 3 ledger entries + resets Tech Fee invoice back to SENT + restores capacity_summary so the next run starts clean. Final balance read confirms`$400 cached:true`. **Known gaps surfaced by the CAT (Phase 2 punch list, not Cycle 6 scope):** PaymentAccountWorker's `pay_family_account_students`link is correctly skipped when`sis_students`doesn't exist yet — the future EnrollmentConfirmedWorker will materialise`sis_students`from`enr_applications`on enroll and re-emit`enr.student.enrolled`, at which point the worker will idempotently insert the link; receipt PDF download (the `pay_payments.receipt_s3_key`is nullable and currently NULL — Phase 3 ops); real Stripe API wiring (CARD auto-COMPLETE with mock`pi_dev\**`ids, refunds with mock`re*dev*\_`ids — Phase 3 ops). **Cycle 6 ships clean to the post-cycle architecture review.** **Step 11 lands the Parent Billing UI** at`apps/web/src/app/(app)/billing/page.tsx`+`apps/web/src/app/(app)/billing/pay/[invoiceId]/page.tsx`+`apps/web/src/app/(app)/billing/ledger/page.tsx`— 3 new parent-facing routes alongside the Step 10 admin surfaces. **No new hooks, no new types, no new backend endpoints** — sits entirely on the 23 hooks from Step 10 and the 24-endpoint payments surface from Step 7. The shared`Billing` launchpad tile (`apps/web/src/components/shell/apps.tsx`) now branches on `personType=GUARDIAN`to route guardians to`/billing`(parent dashboard) instead of`/billing/accounts`(admin landing); the gate stays`fin-001:write` so admins / staff / parents all see the tile, and the description copy switches based on persona ("Your balance, invoices, and payments" vs "Fees, invoices, family accounts, and payments"). The admin invoice detail page (`/billing/invoices/[id]`) gains a `Pay now →`button visible to anyone with`fin-001:write`whenever the invoice has a positive`balanceDue`AND status is not DRAFT/CANCELLED — clicks navigate to`/billing/pay/[invoiceId]`. **`/billing`** is the parent dashboard — auto-redirects admins (`fin-001:admin`) to `/billing/accounts`via a`useEffect`, gracefully handles non-guardian non-admin (e.g. teachers/students with `fin-001:write`on edge cases) with a "not available" empty state. For guardians, it loads`useFamilyAccounts()`(row-scoped backend returns only their own family account, typically 1 row), then for`account.id`reads`useFamilyAccountBalance`(Redis-cached, 30s stale) +`useInvoices({familyAccountId})`+`usePayments({familyAccountId})`. Renders a 3-stat header (Current balance colour-coded rose/emerald/normal / Outstanding invoices count / Children on account count) plus a children sentence ("For Maya Chen (Grade 9), Lily Chen (Grade 10)"), an Outstanding-invoices section that filters to `balanceDue > 0 AND status !== 'CANCELLED'`sorted by due-date ascending (each row shows title + due date + status pill + 3-stat (Total / Paid / Balance due) + a`Pay now →`link to`/billing/pay/{id}`when status is not DRAFT/CANCELLED), and a "Recent payments" section showing the 5 newest payments with method / amount / status + a`See all →`link to`/billing/payments`(which already works for parents because the global payments page is gated on`fin-001:write`and the backend is row-scoped). Header`View ledger`button to`/billing/ledger`. **`/billing/pay/[invoiceId]`** is the payment form — gated on `fin-001:write`, refuses to render the form when the invoice is DRAFT/CANCELLED or `balanceDue=0`("This invoice can't be paid" empty state). Loads`useInvoice(invoiceId)`; defaults the amount input to the full `balanceDue`via a`useEffect`that fires once on first load, with a`Pay full balance ($X)`quick-fill button next to the input. Method picker is a chip group constrained to CARD + BANK_TRANSFER for non-admins (matches the Step 7 backend`assertSelfServiceMethod`check that returns 403 on CASH/CHEQUE/WAIVER for non-admins) and the full 5-method menu for admins. CARD method shows a sky-tinted hint ("Card payments are processed in test mode. No real card is charged — the invoice will be marked paid immediately."), BANK_TRANSFER shows an amber hint ("payment is recorded immediately but funds usually arrive in 1–3 business days"). Optional notes textarea (500-char max). Submit calls`usePayInvoice(invoiceId).mutateAsync({amount, paymentMethod, notes})`; the Step 7 backend wraps the conflict check in one tx that locks the invoice FOR UPDATE, recomputes status PARTIAL/PAID, writes the PAYMENT ledger entry, emits `pay.payment.received`, and returns the new payment row. On success the page Toasts "Paid $X.XX by card" (or "Recorded $X.XX bank transfer payment" depending on method) and routes back to the invoice detail page. Client-side validation: amount must be > 0 AND ≤ balanceDue + 0.001 (matching the backend's overpay pre-check); submit button is disabled until valid. **`/billing/ledger`** is the parent ledger — gated on `fin-001:read`OR`fin-001:write`AND`personType=GUARDIAN`(admins are explicitly told to use`/billing/accounts/[id]`for the per-family ledger view; the page renders a "Not available" empty state for non-guardians). Loads`useFamilyAccounts()`to find the family account, then`useFamilyAccountBalance`+`useFamilyAccountLedger({limit: 100})`(newest-first per the backend default). The keystone trick is the **running-balance computation**: anchors at the current balance (the freshest read, Redis-cached) and walks the newest-first ledger backwards — for row N,`runningBalance`is the account state immediately AFTER that entry was posted; row N+1's running balance = row N's running balance − row N's signed amount. Single`useMemo`over`(ledger, balance, account.balance)`. Header card shows the current balance with a subtitle that reads "Outstanding — invoices waiting on payment." (positive) / "Credit on file — applied automatically to future invoices." (negative) / "Account is settled." (zero). Ledger table renders 5 columns — When / Type / Description / Amount (signed via `formatSignedCurrency`) / Balance (after this entry, via `formatCurrency`). Pagination footnote when 100 rows ship ("Showing the most recent 100 entries"). **Build clean** (`pnpm --filter @campusos/web build`): 3 new parent routes ship at `/billing`2.46 kB (114 kB First Load),`/billing/ledger`1.71 kB (114 kB),`/billing/pay/[invoiceId]`3.24 kB (115 kB). **Live verification on`tenant*demo`2026-04-29** (12 scenarios — 5 read-paths + 4 mutation paths + 3 permission denials, with cleanup): parent`GET /family-accounts`200 → 1 row FA-1001 David Chen balance=$400; parent`GET /invoices`200 → 2 rows; parent`GET /payments`200 → 1 row (the seeded $12000 PAID tuition); parent`GET /family-accounts/{id}/balance`200`{balance:400, cached:true}`; parent `GET /family-accounts/{id}/ledger?limit=10`200 → 3 rows newest-first; parent partial-pay $200 on Tech Fee 2026 → COMPLETED + invoice flips SENT→PARTIAL + balance 400→200 + ledger row PAYMENT −$200 + new`pi_dev*<uuid>`Stripe mock id; parent pays remaining $200 → COMPLETED + invoice flips PARTIAL→PAID + balance 200→0 + second PAYMENT row + second`pi*dev*<uuid>`; over-pay attempt 400 ("Invoice is already PAID"); parent CASH method 403 ("Self-service parent payments accept CARD or BANK_TRANSFER only" — backend's `assertSelfServiceMethod`); student `GET /family-accounts`403 (no`fin-001:read`/write); teacher `POST /invoices/{id}/pay`403 INSUFFICIENT_PERMISSIONS at the gate (no`fin-001:write`). **Cleanup** restores tenant to seed state via direct SQL (DELETE FROM `pay*payments`+`pay_ledger_entries`for the 2 smoke payment rows +`UPDATE pay_invoices SET status='SENT'`+`redis-cli DEL ledger:balance:{accountId}`); final balance read confirms `$400 cached:true`so the next CAT run starts clean. **Out of scope this step (deferred):** receipt PDF download from`/billing/payments`(the schema's`pay_payments.receipt_s3_key`column is nullable and currently NULL for every seeded payment — a real S3 wiring is a Phase 3 ops task once Stripe lands properly per ADR-003); a parent-facing payment-plan management UI (the`usePaymentPlan`/`useCreatePaymentPlan`hooks ship in Step 10 but no surface yet — a polish item that depends on a school actually offering payment plans, which the seed doesn't); per-invoice receipt download (no`receipt_s3_key`populated in dev mode);`?before=`pagination on the ledger view (the`limit=100`window covers 99% of family accounts in the first year of operation). **Step 10 lands the Billing Admin UI at`apps/web/src/app/(app)/billing/`** — 4 routes (5 pages with the invoice + family-account detail views) + 23 React Query hooks in `apps/web/src/hooks/use-billing.ts`+ a new`Billing`launchpad tile gated on`fin-001:write`(held by School Admin / Platform Admin / Parent — Step 11's parent dashboard will reuse the same tile against the same gate, since the row-scope at`FamilyAccountService.list`/`InvoiceService.list`/`PaymentService.list`already binds parents to their own family) using a new`BanknotesIcon`in`apps/web/src/components/shell/icons.tsx`. **`/billing/accounts`** is the default landing — directory of family accounts with a 3-stat header (Active accounts / Outstanding balance / Total accounts) + a single search input that filters by account number, holder name, holder email, or any linked student name; rows show account number / holder + email / linked students with grade / status pill / balance colour-coded (rose for owed, emerald for credit, gray for $0). **`/billing/accounts/[id]`** is the per-family detail page — header with account number + holder + status pill + 3-stat (current balance / students / auth policy) on top of the seeded balance from `useFamilyAccount`(overridden by the fresher`useFamilyAccountBalance`Redis-cached read when it lands); linked-students cards; Invoices table with click-through to invoice detail; Payments table with status pills + click-through to invoice; Ledger table rendered newest-first with type pills (CHARGE rose / PAYMENT emerald / REFUND violet / CREDIT sky / ADJUSTMENT gray) + signed amounts via`formatSignedCurrency` (positive rose, negative emerald). **`/billing/invoices`** is the global invoice list — 6+1 status filter chips (All / Draft / Sent / Partial / Paid / Overdue / Cancelled), an account dropdown, a `Generate from schedule`Modal admin-only that POSTs`/invoices/generate-from-schedule`(one DRAFT per linked family for the schedule's grade — backend is idempotent and returns`{created, skipped, invoiceIds}`); rows show invoice title (link to detail) / family / total / paid / balance / due date / status pill. **`/billing/invoices/[id]`** is the invoice detail — header with title + family link + status pill + admin Send-invoice (DRAFT only) and Cancel buttons (anything other than PAID/CANCELLED); 4-stat (Total / Paid / Balance due / Due date); line-items table with fee-schedule attribution shown when present; payment-history table below. The Send button calls `PATCH /invoices/:id/send`which the Step 7 backend wraps in a tx that locks the invoice FOR UPDATE, flips DRAFT→SENT under sent_chk, writes the CHARGE ledger entry, and emits`pay.invoice.created`; Cancel is `PATCH /invoices/:id/cancel` and refuses on PAID. **`/billing/payments`** is the global payment tracker — 4+1 status chips (All / Pending / Completed / Failed / Refunded), method dropdown, account dropdown; rows show paid-at / family link / invoice link / method / amount / status pill; per-row admin-only `Refund`button on COMPLETED rows opens a Modal that pre-fills the refund amount to the full payment amount and forces a 6-value`refundCategory`+ a`reason`(1000-char max) before POSTing`/payments/:id/refund` (Step 7 backend SUM-of-prior-refunds pre-flight rejects over-cap; partials leave the payment as COMPLETED, full refunds flip to REFUNDED). **`/billing/fees`** is the fee management surface — fee categories grid (compact cards with active pill) + admin New-category Modal that POSTs `/fee-categories`; fee schedules table with Schedule / Category / Year / Grade / Amount / Recurrence / Affected-families / Edit columns. The Affected-families counter is a client-side calculation: for each fee schedule, count active family accounts whose linked students match the schedule's `gradeLevel`(or count all active accounts when`gradeLevel=null`for "all grades"). Edit Modal mutates name/grade/amount/recurrence/isActive via`PATCH /fee-schedules/:id`; new schedules use the same Modal pattern with academic-year dropdown driven by the existing Step 8 `useAcademicYears` hook. New helper module **`apps/web/src/lib/billing-format.ts`** holds the constant arrays (`RECURRENCE_OPTIONS`, `INVOICE_STATUSES`, `PAYMENT_METHODS`, `PAYMENT_STATUSES`, `REFUND_CATEGORIES`) + per-enum label maps + per-status pill class maps for invoices/payments/family-accounts/refunds/ledger-entry-types + `formatCurrency`(Intl.NumberFormat USD with 2 decimal places) +`formatSignedCurrency`(positive prefixed`+`, negative prefixed `−`) + `formatDateOnly`/`formatDateTime`mirrors of`admissions-format.ts`so the two helper modules stay independent. The 23 hooks in`use-billing.ts`: `useFeeCategories`(5min staleTime) /`useCreateFeeCategory`; `useFeeSchedules`/`useFeeSchedule(id)`/`useCreateFeeSchedule`/`useUpdateFeeSchedule(id)`; `useFamilyAccounts`(refetch on focus) /`useFamilyAccount(id)`/`useFamilyAccountBalance(id)`(30s staleTime) /`useFamilyAccountLedger(id, args)`; `useInvoices(args)`(refetch on focus) /`useInvoice(id)`/`useCreateInvoice`/`useSendInvoice(id)`/`useCancelInvoice(id)`/`useGenerateFromSchedule`; `usePayments(args)`(refetch on focus) /`usePayment(id)`/`usePayInvoice(invoiceId)`; `useRefunds(args)`/`useIssueRefund(paymentId)`; `usePaymentPlan(id)`/`useCreatePaymentPlan(invoiceId)`. Mutation invalidations always cover the matching list query + the per-id detail query + the family-account list and detail (since balance + ledger update); `useSendInvoice`and`usePayInvoice`and`useIssueRefund`all invalidate`family-accounts`and`family-account`to keep the running balance fresh. Cycle 6 payments DTO surface added to`apps/web/src/lib/types.ts`: `Recurrence`; `FeeCategoryDto`+`CreateFeeCategoryPayload`; `FeeScheduleDto`+`CreateFeeSchedulePayload`+`UpdateFeeSchedulePayload`; `FamilyAccountStatus`/`PaymentAuthPolicy`/`FamilyAccountStudentDto`/`FamilyAccountDto`; `InvoiceStatus`/`InvoiceLineItemDto`/`InvoiceLineItemInputDto`/`InvoiceDto`/`CreateInvoicePayload`/`GenerateFromSchedulePayload`/`GenerateFromScheduleResponse`/`ListInvoicesArgs`; `PaymentMethod`/`PaymentStatus`/`PaymentDto`/`PayInvoicePayload`/`ListPaymentsArgs`; `EntryType`/`LedgerEntryDto`/`LedgerBalanceDto`/`ListLedgerArgs`; `RefundCategory`/`RefundStatus`/`RefundDto`/`IssueRefundPayload`/`ListRefundsArgs`; `PlanFrequency`/`PlanStatus`/`InstallmentStatus`/`PaymentPlanInstallmentDto`/`PaymentPlanDto`/`CreatePaymentPlanPayload`. **No new API endpoints in this step** — sits entirely on Step 7's 24-endpoint payments surface. Web build clean (`pnpm --filter @campusos/web build`): 6 billing routes ship at `/billing/accounts`4.37 kB (114 kB First Load),`/billing/accounts/[id]`4.67 kB (114 kB),`/billing/fees`4.28 kB (117 kB),`/billing/invoices`5.63 kB (115 kB),`/billing/invoices/[id]`5.06 kB (114 kB),`/billing/payments`5.86 kB (115 kB). **Live verification on`tenant_demo`2026-04-28** (13 read-only scenarios, no mutations — tenant remains at seed state): principal`GET /fee-categories`200 → 4 rows (Activity Fee / Registration Fee / Technology Fee / Tuition all active); principal`GET /fee-schedules`200 → 4 rows joined to academic year + category names; principal`GET /family-accounts`200 → 1 row FA-1001 David Chen balance=$400 ACTIVE 1 student=Maya Chen Grade 9; principal`GET /invoices`200 → 2 rows (Tech Fee 2026 SENT bal=$400 due 2026-05-28; Fall 2026 Tuition PAID bal=$0 due 2025-09-01); principal`GET /payments`200 → 1 row ($12000 CARD COMPLETED); principal`GET /family-accounts/{id}/balance`200`{balance:400, cached:true}`(Redis hit from the Step 7 smoke); principal`GET /family-accounts/{id}/ledger?limit=10`200 → 3 rows newest-first (CHARGE +$400 Tech Fee 2026-04-28, PAYMENT −$12000 2025-08-20, CHARGE +$12000 2025-08-15); parent (David Chen)`GET /family-accounts`200 → only FA-1001 (row-scope on`account_holder_id = actor.personId`); parent `GET /invoices`200 → only own family's 2 rows; teacher`GET /family-accounts`403 (no`fin-001:read`); student `GET /invoices`403; teacher`POST /fee-categories`403 (no`fin-001:admin`); parent `GET /refunds`403 ("Only admins can list refunds" — service-layer check). The mutation paths (create-category, create-schedule, generate-from-schedule, send-invoice, issue-refund) reuse the exact payload shapes verified end-to-end in Step 7's 30-scenario smoke; Step 10 is purely additive on the web side and the page wiring matches the controller contracts. **Out of scope this step (deferred):** payment-plan management UI (the hook + Modal landing here would need an installment-progression view with mark-overdue actions; punted to Step 11 since parents are the primary plan consumers); refund-history page (refund rows are visible inline on the family-account detail's ledger panel via the REFUND entry, but a dedicated`/billing/refunds`list would surface filter/search across all families — a polish item); invoice manual-create with custom line items (the API supports it via`POST /invoices`, but Step 10 ships only the `Generate from schedule`bulk path — the manual create form is reasonable for unusual one-off charges that would otherwise land via SQL or by hand); per-account close/suspend transitions (the schema's`status`column accepts`SUSPENDED`/`CLOSED`but there's no current admin UI for it — punted because the canonical reason to close an account is the student withdrawing from school, which Cycle 7+ will surface). **Step 9 lands the Parent Application & Enrollment UI at`apps/web/src/app/(app)/apply/`+`apps/web/src/app/(app)/offers/[id]/`** — 3 routes + a new parent `Apply`launchpad tile gated on`personType=GUARDIAN`AND`stu-003:write`(the existing admin`Admissions`tile from Step 8 stays gated on`stu-003:admin`, so parents and admins never collide on the same tile). Reuses Step 8's `AcademicCapIcon`, the existing enrollment hooks, and `admissions-format.ts`; no new helper modules. **`/apply`** is the parent landing — combines a "Your applications" tracker and a Start-new entry point in one page (the plan's separate `/applications`tracker route folded in here for fewer surfaces); fetches own apps via`useApplications({})`(row-scoped at`ApplicationService.list`to`guardian_person_id = actor.personId`); each row shows student name + grade + period + stream + status pill + the latest 3 non-confidential admin notes; rows with an ISSUED offer surface a Respond callout in sky-tinted card with `formatRelativeDeadline`countdown that links to`/offers/:id`; rows with an ACCEPTED offer show an emerald confirmation; if at least one OPEN period exists the header shows a Start-new-application button + a green emerald banner listing the open windows. **`/apply/new`** is the application form — single-page with sectioned cards (Admission window / Student details / Guardian contact / Screening responses), auto-selects the only OPEN period when there's exactly one, filters available streams to `isActive=true`, derives the grade dropdown options from the period's `intake_capacities`rows so parents only see grades the school is accepting, auto-populates`guardianEmail`from`useAuthStore.user.email`(editable), 3-chip admission-type group, screening responses are key/value rows the parent can add/remove (free-form`responseValue`per Step 6's`@Allow()`decorator on`ScreeningResponseInputDto.responseValue`); Submit POSTs `/applications`and routes back to`/apply` on success. **`/offers/[id]`** is the response page — header with student name + grade + status pill + offer-type label; sky-tinted Respond-by callout with `formatRelativeDeadline`for ISSUED offers; conditions panel for CONDITIONAL offers (lists`offerConditions`array, shows whether the school has marked`conditionsMet=true/false/null`and **the Accept button is disabled while`conditionsMet !== true`on a CONDITIONAL offer** so parents can't bypass the schema invariant client-side); 3 response buttons Accept / Decline / Defer; Defer reveals an academic-year selector (driven by`useAcademicYears`, filters out the current year so the target is always a future year per the schema's `deferral_target_year_id`semantics); on Accept the page re-fetches the offer (which flips to`status=ACCEPTED`, `familyResponse=ACCEPTED`, application=`ENROLLED`) and renders an inline emerald **Welcome confirmation banner** ("🎉 Welcome! {studentName} has been enrolled in Grade {N}. A tuition invoice will be generated shortly — check the Billing section once it arrives.") with quick links back to `/apply`and to`/children`. The `useRespondToOffer(id)`hook also invalidates`['notifications']`so the parent's bell badge picks up the email-style ack notification queued by`OfferService.respond`. **No new API endpoints land in this step** — sits entirely on Step 6's enrollment surface. Build clean (`pnpm --filter @campusos/web build`): **3 parent admissions routes ship** at `/apply`2.95 kB (114 kB First Load),`/apply/new`3.65 kB (114 kB),`/offers/[id]`4.4 kB (115 kB). **Live verification on`tenant_demo`2026-04-29** (5 reads + 1 end-to-end submit + 1 row-scope 404 + cleanup): parent`GET /enrollment-periods`200 → 1 OPEN Fall 2026 Admissions period with 2 streams + grade options [9, 10]; parent`GET /applications`200 → 1 row (Maya Chen ENROLLED with 1 note); parent`GET /offers`200 → 1 row (Maya UNCONDITIONAL ACCEPTED); parent`GET /academic-years`200 (Step 8 endpoint reused for the Defer year picker); parent`GET /offers/{Maya's}`200; parent`GET /offers/{Sophia's unrelated}`404 (row-scope at`OfferService.getById`); parent `POST /applications`for fictional "Step9Smoke Test" → 201 with`guardianPersonId`correctly stamped from David Chen's`iam_person.id`, `submittedAt`populated, screening row saved, status=SUBMITTED; parent's tracker now shows 2 rows; cleanup`DELETE FROM enr_applications WHERE student_first_name='Step9Smoke'`restores tenant to seed state. Mutation paths beyond submit (Accept / Decline / Defer) reuse the exact payload shapes Step 6 verified live — the`OfferService.respond`row-scope check at`enr_offers.application_id → enr_applications.guardian_person_id == actor.personId`is the access gate. Out of scope this step (deferred): save-as-draft (the API requires SUBMITTED on first POST per the schema's`submitted_chk`; a true DRAFT-status flow would need a separate PATCH path); document upload from the parent side (the `enr_application_documents`schema is ready, but the signed-S3-URL upload endpoint doesn't exist yet); per-school configurable screening-question schema (parents currently compose free-form`(questionKey, responseValue)`pairs). **Step 8 lands the Enrollment Admin UI at`apps/web/src/app/(app)/admissions/`** — 5 routes + 16 React Query hooks in `apps/web/src/hooks/use-enrollment.ts`+ a new`Admissions` launchpad tile gated on **`stu-003:admin`** (tightened from the plan's `stu-003:write`because parents already hold`stu-003:write`after Step 5; Step 9 will introduce a separate parent-facing`Apply`tile) using a new`AcademicCapIcon`in`apps/web/src/components/shell/icons.tsx`. **`/admissions/applications`** is the default landing — pipeline view grouping `ApplicationDto`rows by status across 6 columns SUBMITTED → UNDER_REVIEW → ACCEPTED → WAITLISTED → REJECTED → ENROLLED, filterable by enrollment period and grade; the period filter accepts a`?enrollmentPeriodId=` query param so deep-links from the period detail surface the right slice. **`/admissions/periods`** lists periods with status pills (UPCOMING/OPEN/CLOSED), Open/Close transition buttons, and a 6-stat capacity rollup totalling Places / Applications / Offers / Accepted / Waitlisted / Available across all grades; New-period modal collects academic year (driven by the new `useAcademicYears`hook), name, opens-at, closes-at, and an`allowsMidYearApplications` toggle. **`/admissions/periods/[id]`** is the period detail with the per-grade Capacity Dashboard inline — table rendering `capacitySummary`rows with green/amber/red Available colour coding (red when`available <= 0`, amber when `available < totalPlaces\*0.1`); admission stream list with active/inactive pills; Add-stream and Add-capacity modals (the capacity modal accepts an optional stream selector when streams exist for the period). **`/admissions/applications/[id]`** is the keystone review page — header card with avatar + DOB + admission type + status pill + 4 status-transition buttons (Move to review / Accept / Waitlist / Reject) + Issue-Offer button surfaced when status=ACCEPTED and no offer exists yet; the **OfferPanel** renders inline below the header when `useOffers().find(o.applicationId === id)`returns a row — for ISSUED offers shows offer-type/issued/deadline + a CONDITIONAL conditions amber panel with Mark-met/Mark-failed admin actions and Accept/Decline proxy buttons that call`PATCH /offers/:id/respond`(Accept disabled until conditions resolved, surfacing the schema's CONDITIONAL invariant); guardian + timeline cards; screening responses rendered as key/value rows with`formatScreeningValue`handling the JSONB free-form value; documents list; **NoteComposer** at the top of the notes timeline letting admin pick a 6-value`note_type`and toggle`isConfidential`(admin-only — confidential notes are hidden from parents per`ApplicationService.list`); plus an emerald banner under the OfferPanel when status=ENROLLED noting that `enr.student.enrolled` has fired so the PaymentAccountWorker will create the family account. **`/admissions/waitlist`** is ordered by grade then position with status filter chips defaulted to ACTIVE plus an All chip; per-row Offer-this-spot button on ACTIVE entries opens a Modal that PATCHes `/waitlist/:id/offer` with a date deadline, atomically promoting the waitlist entry to OFFERED + the application to ACCEPTED + creating the new offer row in one tx. New helper module **`apps/web/src/lib/admissions-format.ts`** holds status labels and pill class maps for periods/applications/offers/waitlist + `NOTE_TYPE_LABELS`+`formatStudentName`+`formatDateOnly`/`formatDateTime`+`formatRelativeDeadline`(renders`responseDeadline`as "in N days" / "in N weeks" / "expired YYYY-MM-DD") +`todayIso`/`addDaysIso`. The 16 hooks: `useAcademicYears`(5min staleTime),`useEnrollmentPeriods`/`useEnrollmentPeriod`, `useCreateEnrollmentPeriod`/`useUpdateEnrollmentPeriod(id)`, `useCreateAdmissionStream(periodId)`/`useCreateIntakeCapacity(periodId)`, `useApplications(args)`/`useApplication(id)`, `useCreateApplication`, `useUpdateApplicationStatus(id)`, `useAddApplicationNote(id)`, `useOffers`/`useOffer(id)`, `useIssueOffer(applicationId)`, `useSetOfferConditionsMet(id)`/`useRespondToOffer(id)`(the respond mutation also invalidates`['notifications']`), `useWaitlist(args)`/`useOfferFromWaitlist(id)`(invalidates waitlist + applications + offers + periods because all four denormalisations change atomically). Cycle 6 enrollment DTO surface added to`apps/web/src/lib/types.ts`: `AcademicYearDto`, `EnrollmentPeriodDto`+ nested`IntakeCapacityDto`/`AdmissionStreamDto`/`CapacitySummaryRowDto`, the period payloads, `ApplicationDto`+ nested`ScreeningResponseDto`/`ApplicationDocumentDto`/`ApplicationNoteDto`(plus all 7`ApplicationStatus`values, 5`AdminTransitionTarget`values, 3`AdmissionType`values, 6`ApplicationNoteType`values),`OfferDto`+`OfferType`/`OfferStatus`/`FamilyResponse`enums + the 3 offer payloads,`WaitlistEntryDto`+`WaitlistStatus`enum + the 2 waitlist payloads. **One small additive API endpoint lands alongside the UI:**`GET /academic-years`on a new`AcademicYearController`wired into`SisModule`and gated on`stu-001:read`— needed by the create-period dropdown which has to pick an`academicYearId`; reads `sis_academic_years`directly with`executeInTenantContext`and returns`{id, name, startDate, endDate, isCurrent}` rows ordered by start_date DESC. Web build clean (`pnpm --filter @campusos/web build`): **5 admissions routes ship** at `/admissions/applications`1.97 kB (114 kB First Load),`/admissions/applications/[id]`5.88 kB (118 kB),`/admissions/periods`3.43 kB (115 kB),`/admissions/periods/[id]`3.72 kB (115 kB),`/admissions/waitlist`3.16 kB (115 kB). API build clean — all routes mapped on boot including the new`GET /academic-years`. **Live verification on `tenant_demo`2026-04-28** (8 read scenarios + 4 permission denials): principal`GET /academic-years`returns both seeded years (2026-2027 + 2025-2026 with isCurrent=true on the right one); principal`GET /enrollment-periods`returns 1 OPEN Fall 2026 Admissions period with 2 streams + 2 capacities + 2 capacity_summary rows joined; principal`GET /applications`returns 4 (Aiden SUBMITTED, Olivia WAITLISTED, Sophia ACCEPTED, Maya ENROLLED); principal`GET /waitlist`returns Olivia at pos 1 grade 9 ACTIVE; principal`GET /offers`returns Sophia ISSUED + Maya ACCEPTED; teacher`GET /enrollment-periods`403 (no`stu-003:read`per the seed); student`GET /applications`403; parent (David Chen)`GET /applications`returns only Maya's 1 row (row-scope on`guardian_person_id`working as designed); parent`GET /waitlist`403 (admin-only at the service layer). Mutation paths (create-period, status transitions, issue-offer, conditions-met verify, accept-by-proxy, add-note, offer-from-waitlist) reuse the exact payload shapes verified end-to-end in Step 6 + Step 7 smoke runs; Step 8 is purely additive on the web side and the page wiring matches the controller contracts. Out of scope this step (deferred): dedicated period editor (only status-toggle + add-stream / add-capacity modals; full PATCH name/dates editing is a future polish), application notes filter/search, document upload from the admin side (Step 5 seed planted documents directly; the upload UI is part of the parent-facing Step 9), waitlist position drag-reorder. **Step 7 lands`apps/api/src/payments/`** — the keystone Cycle 6 request-path module that closes the enrollment→payments event loop. 7 services + 6 controllers + **24 endpoints** + 3 Kafka emit topics (`pay.invoice.created`/`pay.payment.received`/`pay.refund.issued`) + the **`PaymentAccountWorker`** Kafka consumer subscribed to `dev.enr.student.enrolled`under group`payment-account-worker`. **`LedgerService`** is the foundation — internal-only `recordEntry(tx, ...)`called inside open tenant transactions by InvoiceService.send / PaymentService.pay / RefundService.issue so every ledger write is atomic with the lifecycle change that caused it; balance reads go through Redis at`ledger:balance:{accountId}`TTL=30s with cache invalidation inside the same write tx (three new helpers added to`RedisService`—`getLedgerBalance`/`setLedgerBalance`/`invalidateLedgerBalance`); `pay_ledger_entries` is IMMUTABLE per ADR-010 — no UPDATE/DELETE service methods. **`FeeScheduleService`** — admin-only fee category + schedule CRUD on `fin-001:admin`; reads on `fin-001:read`. **`FamilyAccountService`** — list / getById / listStudents / balance / ledger; row-scoped on `account_holder_id = actor.personId` for parents; admins see all. **`InvoiceService`** — create DRAFT / list / get / **send (locks invoice FOR UPDATE inside the tx, flips DRAFT→SENT under sent_chk, writes CHARGE ledger entry, emits `pay.invoice.created`)** / cancel (refuses on PAID — issue refund instead) / `generate-from-schedule`bulk (one DRAFT invoice per linked family for the schedule's grade; idempotent — re-running returns`created=0 skipped=N` for families that already have a non-CANCELLED invoice attributed to that fee_schedule_id). **`PaymentService`** — list / get / **pay (locks invoice FOR UPDATE in same tx, writes PAYMENT ledger entry with negative amount, recomputes invoice status PARTIAL/PAID from running total, emits `pay.payment.received`)** with row-scope (parent must be the account holder); refuses overpay against balance-due pre-check; Stripe stubbed in dev — CARD payments mark COMPLETED immediately with mock `pi_dev*<uuid>` intent id; self-service parent payments accept CARD or BANK_TRANSFER only. **`RefundService`** — admin-only list (service-layer 403 for non-admins) / **issue (locks payment FOR UPDATE, writes REFUND ledger entry positive amount restoring balance, marks payment REFUNDED if full-amount, emits `pay.refund.issued`)**; refund cap pre-check via SUM(prior refunds) prevents over-refund; partial refunds leave the payment as COMPLETED; Stripe stubbed with mock `re*dev*<uuid>`. **`PaymentPlanService`** — admin-only POST creates plan + auto-generates installment rows in one tx; due dates = startDate + n × (1 month \| 3 months); last installment absorbs sub-cent residue. **`PaymentAccountWorker`** subscribes to `dev.enr.student.enrolled`under group`payment-account-worker`; reuses the `unwrapEnvelope`+`processWithIdempotency`(claim-after-success per REVIEW-CYCLE2 BLOCKING 2) base helpers from Cycle 3; per inbound event UPSERTs`pay*family_accounts`keyed on (school, account_holder_id) with auto-allocated`FA-####`account_number from`MAX(account_number)+1`and idempotently inserts`pay_family_account_students`if the matching`sis_students`row already exists; gracefully skips when`guardian_person_id`is null (admin-submitted application — Step 6 case) or`sis_students`doesn't exist yet (the future EnrollmentConfirmedWorker will materialise it from`enr_applications` and re-emit). PaymentsModule wired between EnrollmentModule and NotificationsModule; imports TenantModule + IamModule + KafkaModule + NotificationsModule (for RedisService access). Build clean, all 24 routes mapped on boot, payment-account-worker subscription confirmed live. **`seed-iam.ts`updated mid-smoke:** Parent role gains FIN-001:write (parent now 15 perms, was 14). The Step 5 plan said "FIN-001 read to Parent" but the parent self-service Pay Now action demonstrably needs write at the gate — the row-scope check at`PaymentService.pay`(account_holder_id == actor.personId) keeps parents bound to their own family account. **Live verification on`tenant_demo` (30 scenarios, 2026-04-28):** principal lists 4 fee categories + 4 fee schedules with academic-year + category names joined; parent reads own FA-1001 with balance $400 + 1 student (Maya); balance Redis cache hits on second read (`cached:true`); ledger view shows 3 seeded entries (CHARGE +$12k, PAYMENT -$12k, CHARGE +$400) summing to $400; parent partial-pay $200 → invoice PARTIAL + balance $200; parent pays remaining $200 → PAID + balance $0; overpay rejected 400; **`pay.payment.received`envelopes (×2: PARTIAL+PAID) captured live on`dev.pay.payment.received`** with full ADR-057 shape and `source_module:'payments'`; admin refunds $200 → status COMPLETED + balance back to $200 + payment REFUNDED + `re_dev**`mock Stripe id; second-refund attempt 400 ("only COMPLETED payments are refundable"); parent ledger now 6 entries with the REFUND visible; parent service-layer 403 on`/refunds`; bulk `generate-from-schedule`on Tech Fee →`created=0 skipped=1`proving idempotency on existing invoices; **PaymentAccountWorker keystone test:** parent submits Lily Chen application → admin transitions to ACCEPTED → admin issues offer → parent ACCEPTs →`enr.student.enrolled`fires → worker logs`[payment-account-worker] reusing existing pay_family_accounts.id=… for guardianPersonId=…`(UPSERT idempotency hit because David already has FA-1001) and`no sis_students row for Lily Chen yet — skipping link, will be picked up on later re-emit`(graceful handling of the documented edge case where`sis_students` materialisation is deferred to a future EnrollmentConfirmedWorker); 5 permission denial paths all 403 (teacher /fee-schedules, parent POST /fee-schedules, student GET /invoices, parent POST /payment-plan, parent GET /refunds service-layer). **`tenant_demo`restored to seed state** post-smoke via direct SQL DELETE/UPDATE so downstream Step 8 (Enrollment admin UI) starts from the seeded shape. **Step 6 lands the first Cycle 6 NestJS request-path module at`apps/api/src/enrollment/`** with 5 services + 4 controllers + 17 endpoints + 5 Kafka emit topics (`enr.application.submitted`, `enr.application.status_changed`, `enr.offer.issued`, `enr.offer.responded`, **`enr.student.enrolled`** — the topic the future Step 7 `PaymentAccountWorker`will subscribe to).`EnrollmentPeriodService`(period CRUD + nested stream + capacity create; admin writes lock the row FOR UPDATE inside the tx; window CHECK re-validated after PATCH against the merged state).`ApplicationService`(parent submission with`guardian_person_id`stamped from`actor.personId`; admin status transitions with **`assertTransitionAllowed`function** rejecting illegal moves at the service layer — only`SUBMITTED→UNDER_REVIEW/ACCEPTED/REJECTED/WAITLISTED/WITHDRAWN`, `UNDER_REVIEW→ACCEPTED/REJECTED/WAITLISTED/WITHDRAWN`, `WAITLISTED→ACCEPTED/REJECTED/WITHDRAWN`, `ACCEPTED→WITHDRAWN`; ENROLLED is reserved for `OfferService.respond`accept path; admin patch locks row FOR UPDATE and recomputes capacity_summary in same tx; non-admin reads strip`is_confidential=true`notes from the response).`OfferService`(admin-only`POST /applications/:id/offer`; CONDITIONAL offers require `conditions_met=true` before parent ACCEPT; **`PATCH /offers/:id/respond`locks BOTH`enr_offers`AND`enr_applications`FOR UPDATE in the same tx — Cycle 5 review carry-over satisfied** — on ACCEPT, application transitions to ENROLLED + capacity_summary recomputes +`enr.student.enrolled`fires with full student/guardian/grade payload; on DECLINED, only the offer flips; on DEFERRED, family_response set but offer.status stays ISSUED so the partial INDEX on response_deadline keeps tracking the deadline).`WaitlistService`(admin-only list — service-layer 403 for non-admins;`PATCH /waitlist/:id/offer`locks waitlist + parent app FOR UPDATE in one tx, flips waitlist status to OFFERED + application to ACCEPTED + inserts new`enr_offers`row atomically).`CapacitySummaryService`(internal-only —`recompute(tx, periodId, grade)`aggregates`enr_intake_capacities`+`enr_applications`+`enr_offers`and UPSERTs the summary row; called inside open tenant transactions by ApplicationService and OfferService on every status flip; no DB triggers per the plan, service owns the formula). EnrollmentModule wired into`AppModule.imports`between`SchedulingModule`and`NotificationsModule`; imports `TenantModule + IamModule + KafkaModule`. Build clean (`pnpm --filter @campusos/api build`→`nest build`succeeds), all 17 routes mapped on boot. **Live verification on`tenant_demo`(16 scenarios across 4 personas, 2026-04-28):** principal sees 4 applications + 2 offers; parent (David Chen) sees only Maya's 1 application + 1 offer (row scope on`guardian_person_id`); teacher and student get 403 at gate (no `stu-003:read`); confidential REFERENCE_CHECK note on Sophia's application hidden from parent payload (`confidentialNotes=0`); admin transitions Aiden SUBMITTED→UNDER_REVIEW→ACCEPTED with reviewedAt + reviewedBy populated and an admin note created in the same tx; admin issues offer (UNCONDITIONAL, 14d deadline); second-offer attempt returns 400 ("Application already has an offer issued") proving the schema's UNIQUE on application_id is the safety net; admin accepts offer by proxy → offer.ACCEPTED + application.ENROLLED + family_responded_at populated under response_pair_chk; **`enr.student.enrolled`envelope captured live on`dev.enr.student.enrolled`** with full ADR-057 envelope shape — `event_type='enr.student.enrolled'`, `source_module='enrollment'`, `tenant_id`populated, payload includes applicationId/offerId/schoolId/enrollmentPeriodId/studentFirstName/studentLastName/studentDateOfBirth/gradeLevel/admissionType/guardianPersonId/guardianEmail/enrolledAt — exactly what Step 7's PaymentAccountWorker needs; parent submission of new "Lucas Chen" application stamps`guardian_person_id`from`actor.personId`, period must be OPEN, capacity summary recomputed in same tx (grade 9 `applications_received`jumped 2→4); 4 permission denial paths (student POST 403 stu-003:write, parent PATCH .../status 403 stu-003:admin, parent GET /waitlist 403 service-layer admin-only, teacher/student GET /applications 403 at gate). **Bug caught and fixed during smoke:**`ScreeningResponseInputDto.responseValue`was typed`any`with no class-validator decorator, so the global`forbidNonWhitelisted`ValidationPipe rejected parent submissions with`"property responseValue should not exist"`; added `@Allow()`from`class-validator` so the JSONB free-form value passes through. The pattern to remember for any future free-form JSONB DTO field. **`tenant_demo`restored to seed state** post-smoke via direct SQL DELETE/UPDATE so downstream Step 7 (Payments NestJS module +`PaymentAccountWorker`consuming`enr.student.enrolled`) starts from the seeded shape. **Step 5 lands `packages/database/src/seed-enrollment.ts`+`packages/database/src/seed-payments.ts`** as two idempotent seed scripts wired into `package.json`as`seed:enrollment`and`seed:payments`. Each gates on its own first-table count (`enr_enrollment_periods`and`pay_fee_categories`) so re-runs against an already-seeded tenant skip cleanly. **Enrollment seed** lands a `2026-2027` `sis_academic_years`row on its own (is_current=false — leaves the existing`2025-2026`as current;`seed-sis`only seeds the current year), then 1 OPEN enrollment period (Fall 2026 Admissions, 2026-04-01 → 2026-06-30), 2 admission streams (Standard Intake + Music Scholarship, both grade_level=NULL), 2 intake capacities (Grade 9: 120 places / 10 reserved; Grade 10: 110 / 0; both stream_id=NULL exercising the COALESCE-sentinel UNIQUE), 2 pre-computed`enr_capacity_summary`rows (Grade 9 applications_received=2 / offers_issued=1 / offers_accepted=1 / waitlisted=1 / available=108; Grade 10 received=1 / offers_issued=1 / available=109 — the Step 6 CapacitySummaryService will recompute on every transition), 4 applications (Aiden Park Grade 9 SUBMITTED — drives the Step 8 admin pipeline; Sophia Nakamura Grade 10 ACCEPTED with 4 screening + 2 notes incl. 1 confidential REFERENCE_CHECK; Maya Chen Grade 9 ENROLLED historical with`guardian_person_id`back-filled to David Chen's`iam_person.id`showing the post-accept guardian-link state; Olivia Bennett Grade 9 WAITLISTED), 10 screening responses + 3 notes, 2 offers (Sophia UNCONDITIONAL ISSUED with response_deadline 2026-04-29 driving the future expiry sweep; Maya historical UNCONDITIONAL ACCEPTED 2025-05-15 with`family_response`+`family_responded_at` populated under response_pair_chk), 1 ACTIVE waitlist entry (Olivia Grade 9 position 1, priority_score=50.00). **Payments seed** lands 4 fee categories (Tuition / Registration Fee / Technology Fee / Activity Fee), 4 fee schedules tied to the existing 2025-2026 academic year (Grade 9 Annual Tuition $12,000 ANNUAL grade=9; Grade 10 Annual Tuition $12,500 ANNUAL grade=10; Registration Fee $500 ONE_TIME grade=NULL; Technology Fee 2026 $400 ANNUAL grade=NULL — exercises the per-grade-tier coexistence under UNIQUE(school, year, name)), 1 stub Stripe Connect account (`acct_demo_lincoln`, onboarding_complete=true), David Chen's family account (account_number `FA-1001`, status=ACTIVE, payment_authorisation_policy=ACCOUNT_HOLDER_ONLY, account_holder_id=David Chen iam_person), 1 link row to Maya's `sis_students` row (added_by=principal), 2 invoices (Fall 2026 Tuition $12,000 status=PAID with sent_at populated under sent_chk, due 2025-09-01, 1 line item attributed to Grade 9 Annual Tuition fee schedule; Technology Fee 2026 $400 status=SENT with sent_at=2026-04-28, due 2026-05-28, 1 line item attributed to Tech Fee schedule), 1 COMPLETED CARD payment ($12,000, stripe_payment_intent_id=`pi_demo_tuition_2025`, paid_at=2025-08-20, created_by=David Chen platform_user under paid_chk), 3 ledger entries written with explicit `created_at`so partition routing exercises both annual leaves (CHARGE +$12,000 on 2025-08-15 →`pay_ledger_entries_2025`; PAYMENT -$12,000 on 2025-08-20 → `pay_ledger_entries_2025`; CHARGE +$400 on 2026-04-28 → `pay_ledger_entries_2026`). **Live verification on `tenant_demo`2026-04-28:** balance query against`pay_ledger_entries`for FA-1001 returns`400.00`exactly (matches plan: $12,000 charge − $12,000 payment + $400 charge), partition leaves contain 2 rows in`pay_ledger_entries_2025`+ 1 row in`pay_ledger_entries_2026` as expected, idempotent re-runs of both seeds skip cleanly with the right log message. **`seed-iam.ts`updated:** Parent role gains STU-003:read+write (parent submits + tracks own application; row-scope at the future Step 6 ApplicationService layer enforces "your own apps") and FIN-001:read (Step 11 Billing dashboard read paths). STU-003 / FIN-001 admin tiers were already covered by School Admin's`everyFunction: ['read','write','admin']`and Platform Admin's all-444. After`tsx src/build-cache.ts`: cache holds 7 account-scope pairs — admin/principal 444, teacher 34, vp/counsellor 14, student 15, **parent 14 (was 11, +3 new perms)**. Tenant base table count unchanged at **106** (data only — no DDL in Step 5). **Step 4 lands `packages/database/prisma/tenant/migrations/021_pay_invoices_payments_ledger.sql`** with 7 base tables forming the billing engine: `pay_invoices`(6-state lifecycle DRAFT/SENT/PARTIAL/PAID/OVERDUE/CANCELLED; multi-column`sent_chk`keeps sent_at in lockstep with status — DRAFT⇔NULL, SENT/PARTIAL/PAID/OVERDUE⇒NOT NULL, CANCELLED unconstrained because cancel can happen pre- or post-issue and both audit shapes are legitimate;`total_amount NUMERIC(10,2) >= 0`CHECK; due_date nullable for one-off / open-ended bills; INDEX(school, status) + INDEX(account, due_date));`pay_invoice_line_items`(CASCADE on invoice; nullable no-cascade FK to`pay_fee_schedules`so historical attribution survives schedule deactivation;`quantity NUMERIC(6,2) > 0`, `unit_price NUMERIC(10,2) >= 0`, `total NUMERIC(10,2) >= 0`— total denormalised so the formula stays in service code);`pay_payments`(no-cascade FKs to invoice + family account so payments preserve audit even when the invoice is cancelled;`amount NUMERIC(10,2) > 0`— refunds are a separate row not a negative payment; 5-value`payment_method`CHECK CARD/BANK_TRANSFER/CASH/CHEQUE/WAIVER; 4-state`status`CHECK PENDING/COMPLETED/FAILED/REFUNDED; multi-column`paid_chk`PENDING/FAILED⇒paid_at NULL, COMPLETED/REFUNDED⇒paid_at NOT NULL; partial INDEX on stripe_payment_intent_id WHERE NOT NULL);`pay_ledger_entries`(IMMUTABLE per ADR-010 — service-side discipline, no DB trigger / revoke per the plan; **RANGE-partitioned by created_at annually 2025–2030 — 6 partitions**; composite PK`(id, created_at)`because the partition column must be in the unique constraint, matching the`msg_notification_log (id, sent_at)`and`sis_attendance_records (id, school_year, class_id)`precedents; 5-value`entry_type`CHECK CHARGE/PAYMENT/REFUND/CREDIT/ADJUSTMENT; signed`amount NUMERIC(10,2)`— CHARGE positive, PAYMENT negative by convention, schema imposes no sign rule so corrections have full freedom; soft polymorphic`reference_id`pointing at the originating pay_invoices/pay_payments/pay_refunds row depending on entry_type; INDEX(family_account, created_at DESC) + BRIN on created_at; **DB-enforced FK family_account_id → pay_family_accounts(id) replicates onto each of the 6 partitions** plus the parent — 7 pg_constraint rows, expected);`pay_refunds`(no-cascade FKs to payment + family account;`amount > 0`; 6-value `refund_category`CHECK OVERPAYMENT/WITHDRAWAL/PROGRAMME_CANCELLED/ERROR_CORRECTION/GOODWILL/OTHER;`reason TEXT NOT NULL`— admin must justify every refund; 3-state`status`CHECK PENDING/COMPLETED/FAILED; multi-column`completed_chk`keeps completed_at populated only for COMPLETED;`authorised_by`/`authorised_at`NOT NULL on creation — authorisation is a precondition);`pay_payment_plans`(UNIQUE(invoice_id) one-plan-per-invoice;`installment_count > 0`CHECK; 2-value`frequency`CHECK MONTHLY/QUARTERLY; 4-state`status`CHECK ACTIVE/COMPLETED/DEFAULTED/CANCELLED);`pay_payment_plan_installments`(CASCADE on plan; nullable no-cascade FK to`pay_payments`for the fulfilling payment; UNIQUE(plan_id, installment_number);`installment_number > 0`; `amount >= 0`; 4-state `status`CHECK UPCOMING/DUE/PAID/OVERDUE; multi-column`paid_chk`keeps (status, payment_id, paid_at) consistent — PAID requires both populated, every other state requires paid_at NULL but allows payment_id NULL because the row may not yet have an associated payment). 12 new conceptual intra-tenant DB-enforced FKs (1 invoice→account + 1 line item→invoice CASCADE + 1 line item→fee_schedule + 2 payment→invoice/account + 1 ledger→account replicating to 6 partitions + 2 refund→payment/account + 2 plan→account/invoice + 1 installment→plan CASCADE + 1 installment→payment), **18 rows in`pg_constraint`** because the partitioned ledger FK lives once on the parent and once on each of the 6 partitions. 0 cross-schema FKs. Tenant logical base table count after Step 4: **106** (was 99) — 7 new logical base tables (the 6 partition leaves of `pay_ledger_entries`are not counted as logical base tables, matching the`sis_attendance_records`and`msg_notification_log`convention). Cycle-6 total intra-tenant FKs: 30 conceptual (10 from Step 1 + 4 from Step 2 + 4 from Step 3 + 12 from Step 4). Live verification on`tenant_demo`(single BEGIN…ROLLBACK transaction with savepoints, ~32 assertions): every CHECK fires (status enums on all 5 status-bearing tables;`pay_invoices.sent_chk`on the 4 mismatch patterns including DRAFT-with-sent_at and SENT-without-sent_at, with CANCELLED accepted regardless;`pay_invoices.total_chk`on negative;`pay_invoice_line_items.quantity_chk`/`unit_price_chk`/`total_chk`; `pay_payments.amount_chk`/`method_chk`/`status_chk`/`paid_chk`on both directions;`pay_ledger_entries.type_chk`; `pay_refunds.amount_chk`/`category_chk`/`status_chk`/`completed_chk`on both directions;`pay_payment_plans.count_chk`/`frequency_chk`/`status_chk`; `pay_payment_plan_installments.number_chk`/`amount_chk`/`status_chk`/`paid_chk` on both directions); every UNIQUE rejects duplicates (`pay_payment_plans.invoice_id_key`second-plan-for-same-invoice,`pay_payment_plan_installments_plan_number_uq` duplicate (plan, number)); every FK rejects bogus refs (`pay_invoices.family_account_id_fkey`, `pay_payments.invoice_id_fkey`, `pay_ledger_entries.family_account_id_fkey`ON THE PARTITIONED TABLE proving the partition-replicated FK fires correctly,`pay_refunds.payment_id_fkey`); the **partition out-of-window rejection fires** for a 2024-12-31 ledger entry (no partition exists for 2024 — only 2025–2030 are defined); happy-path 3 ledger entries land cleanly across two partitions (2 in 2025, 1 in 2026), `SUM(amount) = 400.00`matches the expected outstanding tech-fee balance, partition routing inspection via direct queries on`pay_ledger_entries_2025`and`pay_ledger_entries_2026`confirms the rows landed in the right leaves; happy-path multi-insert across all 7 tables succeeds (1 invoice DRAFT → SENT transition with sent_at → CANCELLED accepted under sent_chk; 1 line item with fee_schedule attribution; 1 COMPLETED CARD payment with stripe_payment_intent_id; 3 ledger entries; 1 PENDING refund; 1 ACTIVE 4-installment monthly plan; 4 UPCOMING installments + 1 PAID transition with payment_id and paid_at populated); ON DELETE CASCADE on`pay_payment_plans`drops 3 remaining UPCOMING installments → 0; **DELETE on`pay_invoices`rejected by`pay_payments_invoice_id_fkey`while a payment is attached** (no-cascade is the financial-records-immutable invariant); after deleting the refund + payment, DELETE on`pay_invoices`cascades the 1 line item → 0; **DELETE on`pay_family_accounts`rejected by`pay_ledger_entries_family_account_id_fkey`while ledger entries exist** (no-cascade — the ledger is immutable so accounts with history cannot be hard-deleted, admin closes the account via`status='CLOSED'`instead); ROLLBACK at the end leaves`tenant_demo`in pristine state. **Splitter`;`-in-string trap caught and fixed pre-provision** — Python audit script flagged 6 stray `;`instances (4 inside`COMMENT ON COLUMN`strings, 2 inside the block-comment header); rewritten with em-dashes / "and" before any provision attempt so no botched apply on`tenant_demo`. **One COMMENT-on-CONSTRAINT mistake caught mid-provision** — initial draft had `COMMENT ON COLUMN pay_payment_plan_installments.paid_chk IS '...'`but`paid_chk`is a CONSTRAINT name not a column; first provision returned`column "paid_chk" of relation "pay_payment_plan_installments" does not exist`; folded the explanation into the existing `.status`COMMENT and reran cleanly. Idempotent re-provision verified (zero new applies on second run). **Step 3 lands`packages/database/prisma/tenant/migrations/020_pay_family_accounts_and_fees.sql`** with 5 base tables: `pay_family_accounts`(UNIQUE(school_id, account_holder_id) one family account per (school, guardian) — the invariant the future PaymentAccountWorker relies on for idempotent insert-or-link on`enr.student.enrolled`; UNIQUE(school_id, account_number) so the school can address an account by number on a paper invoice; 3-state `status`CHECK ACTIVE/SUSPENDED/CLOSED; 2-state`payment_authorisation_policy`CHECK ACCOUNT_HOLDER_ONLY/ANY_AUTHORISED; soft refs`school_id`→`platform.schools`and`account_holder_id`→`platform.iam_person`); `pay_family_account_students`(UNIQUE(family_account_id, student_id) link table; ON DELETE CASCADE on both family account and`sis_students`consistent with the rest of the sis_students child-table convention; soft`added_by`→`platform.platform_users`); `pay_fee_categories`(UNIQUE(school_id, name);`is_active`flag — admin inactivates rather than hard-deletes a category that has fee schedules attached, since the FK from`pay_fee_schedules`is no-cascade);`pay_fee_schedules`(UNIQUE(school_id, academic_year_id, name); DB-enforced FKs to`sis_academic_years`(no-cascade) and`pay_fee_categories`(no-cascade);`amount NUMERIC(10,2) >= 0`CHECK — discounts are a separate concern that goes through the future`pay_discount_rules`table; 5-value`recurrence`CHECK ONE_TIME/MONTHLY/QUARTERLY/SEMESTER/ANNUAL; nullable`grade_level`so a school can either ship one all-grade fee or per-grade-tiered tuition);`pay_stripe_accounts`(UNIQUE on both`school_id`(one Stripe account per school) and`stripe_account_id`(a single Stripe acct cannot be reused across schools); stubbed in Cycle 6 —`PaymentService`accepts the reference but does not make Stripe API calls;`onboarding_complete`flag —`PaymentService` refuses CARD payments for schools whose Stripe account is not onboarding_complete=true). 4 new intra-tenant DB-enforced FKs (`pay_family_account_students.family_account_id`CASCADE +`…student_id`CASCADE +`pay_fee_schedules.academic_year_id`no-cascade +`…fee_category_id`no-cascade), 0 cross-schema FKs. Tenant logical base table count after Step 3: **99** (was 94). Live verification on`tenant_demo`(single BEGIN…ROLLBACK transaction with savepoints): every CHECK fires (status, payment_authorisation_policy, amount, recurrence), every UNIQUE rejects duplicates ((school, holder) + (school, account_number) on family_accounts, (family_account, student) on the link table, (school, name) on fee_categories, (school, year, name) on fee_schedules, school_id and stripe_account_id both UNIQUE on stripe_accounts), every FK rejects bogus refs (family_account_id, student_id, academic_year_id, fee_category_id), happy-path multi-insert across all 5 tables succeeds (1 family account + 1 link + 1 category + 2 schedules including NULL-grade + grade-9 coexistence for the same (school, year) + 1 stripe acct); status round-trip ACTIVE→SUSPENDED→ACTIVE accepted; ON DELETE CASCADE on`pay_family_accounts`drops the 1 child link row → 0. Rollback at the end leaves`tenant_demo`in pristine state. **Splitter`;`-in-string trap caught and fixed pre-provision** — Python audit script flagged a stray `;` mid-sentence in the block-comment header (`UI hint for ANNUAL/MONTHLY/etc; recurrence is the authoritative cadence`— splitter splits on every`;`regardless of quoting context); rewritten with an em-dash before the first provision attempt so no botched apply on`tenant_demo`. **Step 2 lands `packages/database/prisma/tenant/migrations/019_enr_offers_and_waitlist.sql`\*\* with 2 base tables: `enr_offers` (UNIQUE on application*id one-offer-per-app, ON DELETE CASCADE on the parent application; 2-value `offer_type` CHECK UNCONDITIONAL/CONDITIONAL with multi-column `conditions_chk` enforcing UNCONDITIONAL ⇒ both columns NULL and CONDITIONAL ⇒ `cardinality(offer_conditions) > 0` (NOT `array_length(*, 1) > 0`—`array*length`returns NULL on empty arrays so empty-array CONDITIONAL offers slipped through; smoke caught it; switched to`cardinality()`and added an idempotent`ALTER TABLE … DROP CONSTRAINT IF EXISTS … ; ADD CONSTRAINT …`tail block); 6-value`status`CHECK ISSUED/ACCEPTED/DECLINED/EXPIRED/WITHDRAWN/CONDITIONS_NOT_MET; nullable 3-value`family_response`ACCEPTED/DECLINED/DEFERRED with multi-column`response_pair_chk`keeping family_response + family_responded_at all-set or all-null together; multi-column`deferred_chk`requiring`deferral_target_year_id`when family_response=DEFERRED;`deadline_chk`response_deadline > issued_at; soft FK`deferral_target_year_id`to`sis_academic_years`; partial INDEX(response_deadline) WHERE status='ISSUED' for the future expiry sweep job); `enr_waitlist_entries`(UNIQUE(enrollment_period_id, application_id) one waitlist row per (period, application), CASCADE on both period and application; 5-value`status`CHECK ACTIVE/OFFERED/ENROLLED/EXPIRED/WITHDRAWN;`priority_score NUMERIC(5,2) >= 0`CHECK;`position INT > 0`CHECK; multi-column`offered_chk`requiring`offered_at NOT NULL`when status=OFFERED; partial INDEX on (period, grade, position) WHERE status='ACTIVE' for the next-in-line lookup hot path). 4 new intra-tenant DB-enforced FKs (offers→applications CASCADE + offers→sis_academic_years no-cascade nullable + waitlist→period CASCADE + waitlist→application CASCADE), 0 cross-schema FKs. Tenant logical base table count after Step 2: **94** (was 92). 14 intra-tenant FKs across`enr*\*`tables (10 from Step 1 + 4 new). Live verification on`tenant_demo`(single BEGIN…ROLLBACK transaction with savepoints): every CHECK fires (offer_type, status, family_response, deadline, conditions_chk on all 5 mismatch patterns including the previously-broken empty-array CONDITIONAL case after the fix-up, response_pair_chk both directions, deferred_chk DEFERRED-without-target, waitlist status, priority<0, position=0, OFFERED-without-offered_at), every UNIQUE rejects duplicates (one offer per application; one waitlist row per (period, application)), every FK rejects bogus refs (deferral_target_year_id, application_id), happy-path UNCONDITIONAL + CONDITIONAL offer + waitlist entry creation succeeds; status transitions (ISSUED→ACCEPTED with response pair; ACTIVE→OFFERED with offered_at; admin verifies conditions_met=true on CONDITIONAL) all work; ON DELETE CASCADE on`enr_applications`drops both`enr_offers`and`enr_waitlist_entries`rows cleanly; the`enr_applications.enrollment_period_id`FK correctly REJECTS deletion of a period that still has applications attached (admin closes out applications first — no-cascade by design). Splitter trap not tripped — Python audit confirmed zero`;` inside any string literal or block comment.

**Step 1 lands `packages/database/prisma/tenant/migrations/018_enr_enrollment_and_applications.sql` with 8 base tables: `enr_enrollment_periods` (3-value `status` CHECK UPCOMING/OPEN/CLOSED, `closes_at > opens_at` window CHECK, DB-enforced FK to `sis_academic_years`, UNIQUE(school*id, academic_year_id, name), `allows_mid_year_applications` flag, INDEX(school_id, status)); `enr_admission_streams` (FK ON DELETE CASCADE on the period, nullable `grade_level` for "any grade" streams, optional per-stream window with consistency CHECK, UNIQUE(period, name)); `enr_intake_capacities` (FK ON DELETE CASCADE on both period and stream, nullable `stream_id`, UNIQUE INDEX `(enrollment_period_id, COALESCE(stream_id, '00000000-0000-0000-0000-000000000000'::uuid), grade_level)` — same COALESCE-sentinel trick as Cycle 5's `sch_periods` UNIQUE on `COALESCE(day_of_week, -1)` so a NULL-stream row and a specific-stream row coexist for the same (period, grade), 0 ≤ reserved_places ≤ total_places CHECK); `enr_capacity_summary` (UNIQUE(period, grade), 6 counter columns + `available INT` maintained by the future Step 6 CapacitySummaryService via UPSERT, composite non-negative CHECK on every counter); `enr_applications` (8-value `status` enum DRAFT/SUBMITTED/UNDER_REVIEW/ACCEPTED/REJECTED/WAITLISTED/WITHDRAWN/ENROLLED, 3-value `admission_type` CHECK NEW_STUDENT/TRANSFER/MID_YEAR_ADMISSION, soft nullable `guardian_person_id` to platform.iam_person — parent applying for the first time may not yet have an iam_person row, the OfferService back-fills on accept; multi-column `submitted_chk` keeps submitted_at in lockstep with status — DRAFT ⇔ submitted_at NULL, non-DRAFT ⇔ submitted_at NOT NULL — so a buggy service can't land a half-state row; multi-column `reviewed_chk` keeps reviewed_at + reviewed_by all-set or all-null together); `enr_application_screening_responses` (JSONB `response_value` so schools configure their own question shapes without schema migrations, UNIQUE(application_id, question_key)); `enr_application_documents` (signed-S3-URL pattern matching Cycle 4's `hr_employee_documents`, file_size_bytes >= 0 CHECK); `enr_application_notes` (6-value `note_type` CHECK INTERVIEW_NOTES/ASSESSMENT_RESULT/STAFF_OBSERVATION/REFERENCE_CHECK/VISIT_NOTES/GENERAL, `is_confidential` flag — Step 9's parent-portal application status tracker filters confidential rows out, admin pipeline shows everything). 10 intra-tenant DB-enforced FKs across the 8 tables (1 to `sis_academic_years` + 9 within `enr*\*`), 0 cross-schema FKs. Tenant logical base table count after Step 1: 92 (was 84). Live verification on `tenant_demo`(negative-path / happy-path / CASCADE smoke in one BEGIN…ROLLBACK transaction with savepoints): every CHECK fires (3-status, window dates, intake-capacity bounds, 3-admission_type, 8-status, multi-column submitted_chk both branches, multi-column reviewed_chk half-state, 6-note_type), every UNIQUE rejects duplicates (period name within (school, year), stream name within period, intake-capacity COALESCE-sentinel UNIQUE catches duplicate NULL-stream rows AND allows NULL-stream + specific-stream coexistence, capacity_summary per-(period, grade), screening_responses per-(app, question_key)), every FK rejects bogus refs (academic_year, enrollment_period from applications), happy-path multi-insert across all 8 tables succeeds, ON DELETE CASCADE on both`enr_applications`(drops 2 responses + 1 doc + 1 note → 0/0/0) and`enr_enrollment_periods`(drops 2 streams + 2 capacities + 1 summary → 0/0/0) verified. Idempotent re-provision verified (zero new applies on second run). **Splitter`;`-in-string trap not tripped this time\*\* — Python audit script confirmed zero `;`inside any single-quoted string literal or block comment in the migration. (Steps 8–12 of Cycle 6 — UI (admin admissions / parent apply / admin billing / parent billing) + CAT — remain to ship; see `docs/campusos-cycle6-implementation-plan.html` and `HANDOFF-CYCLE6.md` for the plan.)

**Cycle 5 (Scheduling & Calendar) is COMPLETE + APPROVED at `1650497` (REVIEW-CYCLE5-CHATGPT — final verdict). Round 1 against `e214bdc` returned REJECT pending 2 BLOCKING + 2 actionable MAJOR fixes; Round 2 against `1650497` returned APPROVED** after all 4 fixes landed. Round 2 fixes: BLOCKING 1 — `RoomBookingService.create` now wraps conflict check + INSERT in one `executeInTenantTransaction` and takes `pg_advisory_xact_lock(hashtext('sch_room_bookings:'||roomId))` at the top of the tx; verified live with 5 parallel POSTs landing exactly 1× 201 + 4× 409. BLOCKING 2 — `RoomChangeRequestService.approve/reject` now `SELECT … FOR UPDATE` the row inside the same tx that runs the UPDATE, with `WHERE status='PENDING'` belt-and-braces on the UPDATE; verified live with 5 parallel approves landing exactly 1× 200 + 4× 400. MAJOR 3 — `DayOverrideService.create` now wraps pre-check + INSERT in one tx and catches SQLSTATE 23505 / Prisma P2002 to rethrow the friendly `ConflictException` even on a race; verified live with 5 parallel POSTs landing exactly 1× 201 + 4× 409. MAJOR 4 — `sch_timetable_slots.teacher_id` migration COMMENT rewritten from "Soft FK" to "DB-enforced FK to hr_employees(id) — both sides are tenant-scoped per the Cycle 4 Step 0 staff identity convention" to match the `REFERENCES hr_employees(id)` declaration in the schema; applied to live `tenant_demo` via `COMMENT ON COLUMN`. The two accepted MAJOR deviations (consumer envelope tenant validation; `sch.coverage.needed` best-effort emit) stay on the Phase 2 punch list. CI hardening alongside: workflow gained `permissions: contents: read` per CodeQL's least-privilege guidance. **Step 1 lands `packages/database/prisma/tenant/migrations/015_sch_bell_schedules_and_rooms.sql`** with 3 base tables: `sch_bell_schedules` (5-value `schedule_type` CHECK STANDARD/EARLY_DISMISSAL/ASSEMBLY/EXAM/CUSTOM, `is_default` flag with a partial UNIQUE INDEX on `(school_id) WHERE is_default = true` so each school has at most one default schedule, UNIQUE(school_id, name)); `sch_periods` (FK to `sch_bell_schedules ON DELETE CASCADE`, nullable `day_of_week SMALLINT` 0..6 (NULL = rotation-driven for the future Cycle 5b `sch_rotation_cycles` linkage), `start_time TIME < end_time TIME` CHECK, 5-value `period_type` CHECK LESSON/BREAK/LUNCH/REGISTRATION/ASSEMBLY, UNIQUE(bell_schedule_id, COALESCE(day_of_week, -1), start_time)); `sch_rooms` (7-value `room_type` CHECK CLASSROOM/LAB/GYM/HALL/LIBRARY/OFFICE/OUTDOOR, optional `capacity INT >= 0` CHECK, `has_projector` + `has_av` boolean amenity flags, optional DISPLAY-ONLY soft `fac_space_id` ref to a future `fac_spaces` table (M52 Facilities — unenforced, nullable, forward-compat), UNIQUE(school_id, name)). 1 intra-tenant DB-enforced FK (sch_periods → sch_bell_schedules CASCADE), 0 cross-schema FKs. Tenant logical base table count after Step 1: 77 (was 74). Live verification on `tenant_demo`: 11 negative-path assertions all fired the expected ERROR (schedule_type / period_type / room_type CHECK violations, period times-reversed, day_of_week=9, capacity=-1, both UNIQUE(school_id, name) constraints, the partial-default UNIQUE, the period (schedule, day, start_time) UNIQUE, FK rejection on a bogus bell_schedule_id), happy-path multi-insert across all 3 tables succeeds (1 schedule + 3 periods + 2 rooms), ON DELETE CASCADE on `sch_bell_schedules` drops all 3 child periods cleanly. **Splitter `;`-in-string trap struck again** — the first re-provision failed because the `sch_periods.day_of_week` COMMENT contained a literal `;` mid-sentence; the splitter cuts on every `;` regardless of quoting context, so the COMMENT was rewritten with em-dashes and the migration applied cleanly on the second pass.

**Step 2 lands `016_sch_timetable_and_bookings.sql`** with 3 base tables on top of `CREATE EXTENSION IF NOT EXISTS btree_gist` (the extension lives at database scope so it's idempotent across re-provisions and across multiple tenants on the same database): `sch_timetable_slots` (DB-enforced FKs to `sis_classes`, `sch_periods`, `sch_rooms`, and nullable `hr_employees` for TBD-teacher slots; `effective_from`/`effective_to` date range with NULL-end-date meaning open-ended; UNIQUE(class_id, period_id, effective_from); two `EXCLUDE USING gist` constraints — `sch_timetable_slots_teacher_no_overlap` and `sch_timetable_slots_room_no_overlap` — both shaped `(actor WITH =, period_id WITH =, daterange(effective_from, COALESCE(effective_to, 'infinity'::date), '[)') WITH &&)`). The half-open `[)` daterange means a slot ending on a date and another slot starting on the same date do **not** overlap — the natural rotation pattern. Open-ended slots coalesce NULL → infinity so they block every future date. `teacher_id` is nullable and the `=` operator class treats NULL-vs-NULL as not-equal, so two TBD-teacher slots on the same period and overlapping range do **not** conflict (the room EXCLUSION still catches the room collision). Constraint violations raise SQLSTATE 23P01 — Step 5's `TimetableService` will translate that to `409 Conflict`. `sch_room_bookings` (CONFIRMED/CANCELLED status with multi-column lifecycle CHECK that CANCELLED ⇒ cancelled_at NOT NULL, window CHECK end_at > start_at, partial INDEX on (room_id, start_at, end_at) WHERE CONFIRMED for the conflict-check hot path). `sch_room_change_requests` (PENDING/APPROVED/REJECTED/AUTO_APPROVED with multi-column reviewed_chk that PENDING ⇔ reviewed_at NULL; nullable `requested_room_id` for the "any available" case; AUTO_APPROVED reserved for a future auto-approval sweep). 8 intra-tenant FKs (4 on sch_timetable_slots, 1 on sch_room_bookings, 3 on sch_room_change_requests), 0 cross-schema FKs. Tenant logical base table count after Step 2: 80 (was 77). Live verification on `tenant_demo` (with seeded classes + employees from prior cycles): both EXCLUSION constraints reject overlapping slots cleanly with SQLSTATE 23P01; non-overlapping reuse on `effective_from = old.effective_to + 1d` is accepted; NULL-vs-NULL teacher does not conflict; dates_chk + UNIQUE(class_id, period_id, effective_from) + all 4 FKs on slots (period/room/class/teacher) reject orphan refs (the FKs on room_id and class_id were verified separately on a clean state since the open-ended Rivera slot's teacher EXCLUSION preempted the original ordering); bookings + change_requests status / window / multi-column lifecycle CHECKs all fire on bogus inputs.

**Step 3 lands `017_sch_calendar_and_coverage.sql`** with 4 base tables: `sch_calendar_events` (8-value `event_type` CHECK HOLIDAY/PROFESSIONAL_DEVELOPMENT/EARLY_DISMISSAL/ASSEMBLY/EXAM_PERIOD/PARENT_EVENT/FIELD_TRIP/CUSTOM, optional `bell_schedule_id` FK override that pins the date range to a non-default schedule, `affects_attendance` flag that flips off Cycle 1's attendance pre-population, `is_published` draft flag, dates_chk that `end_date >= start_date`, 2-branch time_consistency_chk that keeps `all_day` in lockstep with `start_time`/`end_time` — `all_day=true` requires both NULL, `all_day=false` requires both NOT NULL with `start_time < end_time`); `sch_calendar_day_overrides` (UNIQUE(school_id, override_date), nullable `bell_schedule_id` ref, `is_school_day` flag for snow-day closures — CalendarService resolution order is override first, then matching event, then default schedule); `sch_coverage_requests` (4-value `status` CHECK OPEN/ASSIGNED/COVERED/CANCELLED, UNIQUE(timetable_slot_id, coverage_date) so the future CoverageConsumer's redelivery cannot create duplicate rows, soft `leave_request_id` ref to `hr_leave_requests` so the seed can plant rows out of order, 3-branch `assignment_chk` that OPEN ⇔ assigned_substitute_id NULL AND assigned_at NULL, ASSIGNED/COVERED ⇒ both NOT NULL, CANCELLED unconstrained because a request can be cancelled either before or after assignment and both audit shapes are legitimate); `sch_substitution_timetable` (UNIQUE(original_slot_id, effective_date), DB-enforced FKs to `sch_timetable_slots`, `hr_employees`, `sch_rooms`, and `sch_coverage_requests` with ON DELETE CASCADE — a substitution row without its parent coverage request is meaningless). 9 intra-tenant FKs (3 on `sch_calendar_events` + day_overrides bell_schedule refs + 3 on `sch_coverage_requests` slot/teacher/sub + 4 on `sch_substitution_timetable` slot/sub/room/request), 0 cross-schema FKs. Total scheduling tables: 10. Tenant logical base table count after Step 3: 84 (was 80). Live verification on `tenant_demo` (25 assertions in one BEGIN…ROLLBACK transaction with savepoints, bootstrapping a temp bell schedule + period + room + slot tied to Rivera + an existing seeded class): every CHECK fires (event_type / dates / 5 time_consistency paths / coverage status / 3 assignment_chk paths), every UNIQUE rejects duplicates (override per-date / coverage per-(slot,date) / substitution per-(slot,date)), every FK rejects orphans (calendar→bell_schedule, override→bell_schedule, coverage→slot, coverage→teacher, substitution→request, substitution→slot), happy-path multi-insert across all 4 tables succeeds, OPEN→ASSIGNED transition with `assigned_substitute_id` + `assigned_at` accepted, ON DELETE CASCADE on `sch_coverage_requests` drops the matching `sch_substitution_timetable` row cleanly. Tests 14/15/16 attribute their rejection to `assignment_chk` rather than `status_chk` because the assignment_chk's OR-clause is the load-bearing rule for the whole lifecycle — when no branch matches, it's the constraint that fires; same row-rejection outcome and same observation Step 2 documented for `sch_room_change_requests`. **Splitter `;`-in-string trap not tripped this time** — spot-checked all CHECK predicates, default expressions, and COMMENT strings; the only `--` mid-sentence in the block-comment header sits safely inside `/* */`.

**Step 4 lands `packages/database/src/seed-scheduling.ts`** (idempotent, gated on `sch_bell_schedules` row count) — seeds the demo tenant with 2 bell schedules ("Standard Day" default with 11 periods (8 LESSON / 1 LUNCH / 2 BREAK, 08:00–15:50, all `day_of_week=NULL` so each period applies to every weekday) + "Early Dismissal" with 8 periods (6 LESSON / 1 LUNCH / 1 BREAK, 08:00–12:30)); 10 rooms (Room 101–106 CLASSROOM cap 30 with projector / Science Lab cap 25 with projector+AV / Gymnasium cap 100 with AV / Library cap 50 with projector / Main Hall cap 200 with projector+AV — each with `building` and `floor` populated); 6 timetable slots mapping Rivera's Cycle 1 classes onto Standard Day Periods 1–6 (Algebra→101, English→102, Biology→103, World History→104, Geometry→105, Chemistry→106) with `effective_from='2025-08-15'` and open-ended `effective_to`; 5 calendar events (Spring Break HOLIDAY 04-14→04-18 affects_attendance=true / PD Day PROFESSIONAL_DEVELOPMENT 03-15 affects_attendance=true / Parent-Teacher Conference Evening PARENT_EVENT 05-01 18:00–20:00 non-all-day / End of Year Assembly ASSEMBLY 06-06 / Senior Prom CUSTOM 05-23 draft — 4 published, 1 draft, all authored by Mitchell); 1 day override (2026-02-07 snow day, is_school_day=false, no bell_schedule_id); 1 ASSIGNED coverage request covering Rivera's Period 1 Algebra on 2026-03-09 (Day 1 of Rivera's Cycle 4 APPROVED sick leave) with VP Park as `assigned_substitute_id` and `leave_request_id` joined to the Cycle 4 hr_leave_requests row; 1 substitution_timetable row materialising Park's assignment in Room 101 on 2026-03-09; 1 room booking (Main Hall 2026-05-01 18:00–20:00 status=CONFIRMED) by Mitchell, lining up cleanly with the matching calendar event. **`seed-iam.ts` updated:** Teacher gains SCH-004:read + SCH-005:read+write (31 → 34); Staff gains SCH-001:read + SCH-004:read + SCH-005:read+write (10 → 14). After `tsx src/build-cache.ts`: cache holds 7 account-scope pairs — admin/principal 444, teacher 34, vp/counsellor 14, student 15, parent 11. **Bug caught and fixed mid-run:** the calendar events INSERT initially passed nullable `start_time`/`end_time` as TEXT and the coverage requests INSERT passed `leave_request_id` as TEXT — both columns are typed (TIME and UUID), so Postgres refused to coerce; explicit `$N::time` and `$5::uuid` parameter casts on the affected calls resolved both. Idempotent re-run gates cleanly on the `sch_bell_schedules` count. Tenant base table count unchanged at 84 (data only — no DDL).

**Step 5 lands the first scheduling request-path module at `apps/api/src/scheduling/`** with 5 services + 5 controllers + 22 endpoints. `BellScheduleService` (6 endpoints: `GET /bell-schedules`, `GET /bell-schedules/:id`, `POST /bell-schedules` admin, `PATCH /bell-schedules/:id` admin, `POST /bell-schedules/:id/periods` admin (full upsert — DELETE all + INSERT each, atomic in one transaction so a UNIQUE / CHECK violation aborts the whole replacement instead of landing half-applied), `POST /bell-schedules/:id/set-default` admin (atomically clears any other default for the same school in the same tx so the partial UNIQUE INDEX never rejects the flip)). `TimetableService` (7 endpoints: `GET /timetable` with `classId` / `teacherId` / `roomId` / `onDate` filters, `GET /timetable/teacher/:employeeId`, `GET /timetable/class/:classId`, `GET /timetable/room/:roomId`, `POST /timetable/slots` admin, `PATCH /timetable/slots/:id` admin, `DELETE /timetable/slots/:id` admin) — every read joins through `sis_classes` + `sis_courses` for `classSectionCode` + `courseName`, `sch_periods` for `periodName` + `dayOfWeek` + `startTime` + `endTime`, `sch_rooms` for `roomName`, and LEFT JOINs `hr_employees` + `platform.iam_person` for `teacherName`. **The translateConflict helper is the load-bearing trick**: `INSERT` / `UPDATE` is wrapped in a try/catch; SQLSTATE 23P01 paired with the constraint name (`sch_timetable_slots_teacher_no_overlap` vs `sch_timetable_slots_room_no_overlap`) is decoded into a friendly `409 Conflict` with the conflicting actor's name (e.g. "Teacher James Rivera is already scheduled for Period 1 during the requested date range" or "Room Room 101 is already scheduled for Period 1 during the requested date range" — name lookups via separate `sch_rooms` / `sch_periods` / `hr_employees` reads on the failure path); 23503 (FK violation) becomes 400; 23505 on `sch_timetable_slots_class_period_from_uq` becomes a different 409. Slot writes emit `sch.timetable.updated` via `KafkaProducerService.emit({sourceModule:'scheduling'})`. `RoomService` (4 endpoints: `GET /rooms` with optional `availabilityDate` + `availabilityPeriodId` annotation that returns `available: boolean | null` per row by intersecting against active timetable slots that match the period for the supplied date, `GET /rooms/:id`, `POST /rooms` admin, `PATCH /rooms/:id` admin). `RoomBookingService` (4 endpoints: `GET /room-bookings` with `roomId` / `status` / `fromDate` / `toDate` filters, `GET /room-bookings/:id`, `POST /room-bookings` write — `assertNoConflicts` checks both existing CONFIRMED bookings whose time range overlaps **and** active timetable slots whose period clock-time on the booking's ISO weekday overlaps the booking window (the schema does NOT enforce booking-vs-slot conflict — it's an app-layer gate; per Step 2's out-of-scope decision), `PATCH /room-bookings/:id/cancel` write — owner or admin only). `RoomChangeRequestService` (5 endpoints: `GET /room-change-requests` write — non-admins see only their own rows via row-scope filter on `requested_by = actor.employeeId`, admins see all; `GET /room-change-requests/:id`, `POST /room-change-requests` write — refuses if the slot's `teacher_id` is set and not the actor (admins bypass), reads `current_room_id` from the slot rather than trusting the client; `PATCH /room-change-requests/:id/approve` admin (requires `approvedRoomId` if the original request had `requestedRoomId=null`); `PATCH /room-change-requests/:id/reject` admin). All 5 services use `tenant.schoolId` from `getCurrentTenant()` for the school-scoped fields rather than reading from `school_config` (more direct, since the tenant context is always populated). The `actor.employeeId` populated by `ActorContextService.resolveActor(...)` is the row-scope key for change requests + bookings + room change request submitters; admins bypass the row check. `SchedulingModule` imports `TenantModule` + `IamModule` + `KafkaModule` and lives at index 5 in `AppModule.imports` between `HrModule` and `NotificationsModule`. Build clean (`pnpm --filter @campusos/api build` → `nest build` succeeds). All 22 routes mapped on boot. **Live verification on `tenant_demo` (10 scenarios):** principal `GET /bell-schedules` returns 2 schedules with periods sorted by sort_order; principal `GET /timetable` returns 6 slots with full join shape; principal `GET /timetable/teacher/{Rivera}` returns Rivera's 6 Periods 1–6 in Rooms 101–106; student `GET /bell-schedules` blocked 403 (no `sch-001:read`); parent same 403; teacher `POST /timetable/slots` blocked 403 (no `sch-001:admin`); admin `POST /timetable/slots` against Rivera + Period 1 + a different room (with effectiveFrom 2025-08-15) returns **`409 Conflict — "Teacher James Rivera is already scheduled for Period 1 during the requested date range"`** (teacher EXCLUSION); admin `POST /timetable/slots` with `teacher_id=null` against Room 101 + Period 1 returns **`409 Conflict — "Room Room 101 is already scheduled for Period 1 during the requested date range"`** (room EXCLUSION); admin `POST /timetable/slots` for Period 7 (free LESSON) + Library + null teacher succeeds 201; teacher `POST /room-bookings` for Library 2030-09-15 09:00–10:00 returns 201; second booking for the same window returns `409 — "Room is already booked for an overlapping window (booking …)"`; student `POST /room-bookings` blocked 403 (no `sch-005:write`); teacher cancels own booking → `status=CANCELLED`; teacher submits room change request, teacher tries to approve own → 403, admin approves → `status=APPROVED` with `reviewedAt` populated, second admin approve attempt → 400 ("Request is in status APPROVED; only PENDING requests can be approved"); `set-default` round trip flips Early Dismissal → default, then back to Standard Day → default cleanly via the partial-UNIQUE-aware atomic helper; `GET /rooms?availabilityDate=2026-05-15&availabilityPeriodId={P1}` returns 10 rooms with Room 101 marked `available=false` (Rivera's Algebra slot covers it) and the other 9 marked `available=true`. **All smoke residue cleaned up** (`DELETE FROM sch_room_change_requests` + `sch_room_bookings` for the smoke rows) so `tenant_demo` is back to seed state for downstream Steps 6–10.

**Step 6 lands the calendar / coverage NestJS side at `apps/api/src/scheduling/`** — 4 services + 3 controllers + 16 endpoints + 1 Kafka consumer (`CoverageConsumer`) + 2 Kafka emits (`sch.coverage.needed`, `sch.coverage.assigned`). `CalendarService` (5 endpoints under `sch-003:read` / `sch-003:write` admin gates: `GET /calendar` with `fromDate` / `toDate` / `eventType` / `includeDrafts` filters — non-admins only ever see published; admins with `includeDrafts=true` see drafts too. `GET /calendar/:id` (drafts hidden from non-admins by 404). `POST /calendar` admin — schedule-aware time-shape assertion (`allDay=true` clears times; `allDay=false` requires `startTime < endTime`). `PATCH /calendar/:id` admin — when `allDay` toggles to `true` the UPDATE clears `start_time` + `end_time` in the same statement so the schema's time-consistency CHECK never fires mid-flight. `DELETE /calendar/:id` admin. `GET /calendar/day/:date` — the resolution endpoint that the calendar UI hits for a single day; reads override → published events with `bell_schedule_id` → school's `is_default=true` schedule → none, returns `{date, resolvedFrom: 'OVERRIDE'|'EVENT'|'DEFAULT'|'NONE', isSchoolDay, bellScheduleId, bellScheduleName, overrideId, overrideReason, eventIds[]}`). `DayOverrideService` (3 endpoints: `GET /calendar/overrides`, `POST /calendar/overrides` `sch-003:admin`, `DELETE /calendar/overrides/:date` `sch-003:admin`; UNIQUE(school_id, override_date) handled at the service layer with a 409 instead of letting the schema raise — better UX). `CoverageService` (4 endpoints under `sch-004:read` / `sch-004:write`: `GET /coverage` daily board with `fromDate` / `toDate` / `status` filters — non-admin staff see only rows where they are the absent or assigned employee; `GET /coverage/:id`; `PATCH /coverage/:id/assign` admin — locks the row with `FOR UPDATE`, flips OPEN→ASSIGNED, populates `assigned_substitute_id` + `assigned_at`, INSERTs the matching `sch_substitution_timetable` row in the same transaction, then emits `sch.coverage.assigned` with the full join shape outside the tx so the substitute receives an IN_APP notification through the Cycle 3 pipeline; `PATCH /coverage/:id/cancel` admin — drops any matching substitution row in the same tx so the substitute's day-view stops showing the cover, then flips status=CANCELLED). `SubstitutionService` (2 read-only endpoints: `GET /substitutions?fromDate=&toDate=` and `GET /substitutions/teacher/:employeeId?fromDate=&toDate=`; substitution rows are written exclusively by `CoverageService.assign` — there's no direct INSERT path). **`CoverageConsumer` (the long-awaited piece — Cycle 4's `hr.leave.coverage_needed` finally has a consumer)**: subscribes to `dev.hr.leave.coverage_needed` under group `coverage-consumer` using the same `unwrapEnvelope` + `processWithIdempotency` pattern as Cycle 3's notification consumers. Per inbound event, enumerates every weekday (Mon–Fri) date in `[startDate, endDate]`, joins through `sch_timetable_slots → sch_periods` for each `affectedClasses[].classId`, filters slots whose date range covers each date AND whose period `day_of_week` is NULL or matches the date's ISO weekday, then INSERTs one `sch_coverage_requests` row per matching `(slot, date)` pair with status=OPEN. `UNIQUE(timetable_slot_id, coverage_date)` is the schema-side belt-and-braces dedup — Kafka redelivery raises 23505 and the consumer swallows it. The deterministic event_id from REVIEW-CYCLE4 MAJOR 3 is the primary dedup gate (`processWithIdempotency` claims after success, so transient DB failures are retried). When new rows land, emits `sch.coverage.needed` with `{leaveRequestId, employeeId, startDate, endDate, createdCoverageIds[]}` so the Step 8 admin UI can react. **Build clean** (`pnpm --filter @campusos/api build`). All 16 new routes mapped on boot. The consumer subscribes successfully on boot (`Subscribed: groupId=coverage-consumer topics=dev.hr.leave.coverage_needed`). **Live verification on `tenant_demo` (10 scenarios):** parent `GET /calendar` returns 4 published events; admin with `includeDrafts=true` sees all 5 (Senior Prom DRAFT included); student passing `includeDrafts=true` still gets only 4 (gate-tier permission gates plus service-tier double-check); `GET /calendar/day/2026-02-07` resolves to `{resolvedFrom: 'OVERRIDE', isSchoolDay: false, overrideReason: 'Winter storm closure'}`; `GET /calendar/day/2026-04-15` (within Spring Break) resolves to `{resolvedFrom: 'DEFAULT', isSchoolDay: true, bellScheduleName: 'Standard Day', eventIds: [<spring break>]}` because the seeded Spring Break event has no `bell_schedule_id` override; `GET /calendar/day/2026-09-15` (regular weekday) resolves to `{resolvedFrom: 'DEFAULT', bellScheduleName: 'Standard Day', events: 0}`; `GET /coverage` returns the seeded ASSIGNED row Park-covers-Rivera-2026-03-09; `GET /substitutions` returns the matching substitution row. Permission denials all return 403 (parent calendar admin POST, student day-override POST, teacher coverage assign). Calendar event create-as-draft → parent doesn't see it → admin patches `isPublished=true` → parent sees it → cleanup delete returns 200. Day override create-resolve-delete round trip clean. **End-to-end CoverageConsumer flow verified live**: Rivera submits a 1-day sick leave for 2026-09-23 (Wednesday) → admin approves → within 1 second the consumer chain (`hr.leave.approved` → `LeaveNotificationConsumer.emitCoverageNeeded` → `hr.leave.coverage_needed` → `CoverageConsumer.handle`) creates 6 OPEN coverage rows (one per Rivera class — Algebra/English/Biology/World History/Geometry/Chemistry — Periods 1–6), one per class for that single date; admin assigns Park to Period 1 Algebra → status=ASSIGNED + `assignedSubstituteName=Linda Park` + `assignedAt` populated → matching `sch_substitution_timetable` row materialises in Room 101; admin cancels the assigned row → status=CANCELLED + the substitution row is dropped (the substitute's day-view stops showing the cover); the other 5 OPEN rows untouched. **All smoke residue cleaned up** (DELETE FROM `sch_coverage_requests` + `hr_leave_requests` for the smoke 2026-09-23 + restored Rivera's Sick balance) so `tenant_demo` is back to seed state.

**Step 10 lands `docs/cycle5-cat-script.md`** — the reproducible end-to-end vertical slice CAT, verified live against `tenant_demo` on 2026-04-28. The script wraps a 4-check schema preamble (84 tenant base tables / 10 sch\_ scheduling tables / 0 cross-schema FKs / 2 btree_gist EXCLUSIONs, plus row-count verification of the 9 seed tables) around the plan's 10 scenarios: S1 admin sees Rivera's 6 timetable slots; S2 teacher sees own week via `/timetable/teacher/:employeeId`; S3 parent David Chen sees Maya's 4 enrolled-class slots via the new Step 9 `/timetable/student/:studentId` row-scoped endpoint; S4 admin attempt to schedule Rivera twice for Period 1 → `409 Conflict — "Teacher James Rivera is already scheduled for Period 1 during the requested date range"` (EXCLUSION 23P01 → translateConflict); S5 seeded ASSIGNED coverage row + matching substitution row for Park-covers-Rivera-2026-03-09 verified; **S6 live leave→coverage**: Rivera submits 1-day Sick leave for 2026-09-30 (Wed) → admin approves → within 3 seconds the consumer chain (`hr.leave.approved` → `LeaveNotificationConsumer.emitCoverageNeeded` → `hr.leave.coverage_needed` → `CoverageConsumer.handle`) creates 6 OPEN coverage rows (one per Rivera Period 1–6); **S7 assign substitute**: admin assigns Park to Period 1 Algebra → `status=ASSIGNED` + matching `sch_substitution_timetable` row materialises in Room 101 → ADR-057 envelope on the wire on `dev.sch.coverage.assigned` with `source_module=scheduling` and full join shape inline (coverageRequestId, timetableSlotId, coverageDate, substituteId, substituteName, absentTeacherId, absentTeacherName, classSectionCode, courseName, periodName, roomId, roomName, assignedAt); S8 calendar list returns the 5 seeded events (4 published + 1 draft Senior Prom) + 1 day-override row, `/calendar/day/2026-02-07` resolves to `{resolvedFrom: 'OVERRIDE', isSchoolDay: false, overrideReason: 'Winter storm closure'}`; S9 teacher books Library 2026-10-15 09:00–10:00 → 201, second overlapping booking → `409 — "Room is already booked for an overlapping window (booking …)"`, booking Room 101 during Period 1 → `409 — "Room is in use by the timetable during the requested window: 1 / Period 1"` (the app-layer assertNoConflicts gate); S10 6 permission denials (parent /timetable 403, student POST /room-bookings 403, teacher PATCH /coverage/:id/assign 403 admin-only at service layer, parent POST /calendar 403, parent /timetable/student/{unrelated} 404 row-scope, student POST /bell-schedules 403). **All 10 scenarios pass.** Cleanup section drops the 1 leave + 6 coverage rows + 1 substitution + 1 booking and resets Rivera's Sick balance back to seed defaults (`used=2.0, pending=0.0`) so the next run starts clean. **Known gap surfaced by the CAT (Phase 2 punch list, not Cycle 5 scope):** `sch.coverage.assigned` is emitted to Kafka with the full join shape inline, but no consumer yet wires the substitute IN_APP notification through the Cycle 3 pipeline — the substitute's bell badge does NOT increment when assigned. The Step 6 handoff already documented this as a future patch; the CAT confirms the emit lands cleanly so the wiring is a small downstream consumer addition rather than a re-architecture. **Cycle 5 ships clean to the post-cycle architecture review.**

**Step 9 lands the non-admin schedule views at `apps/web/src/app/(app)/my-schedule/` + `apps/web/src/app/(app)/children/[id]/schedule/`** — every persona can now see the schedule that affects them. One small backend extension was needed: parents and students don't hold `sch-001:read` so they can't hit `/timetable` directly. New endpoint `GET /timetable/student/:studentId` lands on `TimetableService` gated on `stu-001:read` (held by every relevant persona) with row-scope enforced by `StudentService.assertCanViewStudent` (admin / parent of / assigned-class teacher / the student themself — the same row-scope contract Cycle 1 already uses for `/students/:id/...` reads). Query joins `sis_enrollments → sch_timetable_slots` for the student's ACTIVE enrollments and reuses the existing `SELECT_SLOT_BASE` so the response shape matches the other timetable endpoints. `SchedulingModule` now imports `SisModule` to get `StudentService`. Total scheduling endpoints: 39. Web side: a new `useTimetableForStudent(studentId)` hook + two new components and two new routes. **`apps/web/src/components/scheduling/TimetableWeekView.tsx`** extracts the periods × Mon-Fri grid rendering from `/schedule/timetable` into a reusable component (reads the school's default bell schedule via `useBellSchedules`); accepts `slots` + state passthrough + `showTeacher` / `showRoom` toggles + an optional `substitutions` array that builds a `Map<${slotId}::${YYYY-MM-DD}, SubstitutionDto>` so cells whose date matches a substitution render with an amber border + struck-through absent teacher + substitute's name to the right. The grid header annotates each weekday column with `MM-DD` so the user can see which week is rendered. **`apps/web/src/components/scheduling/RoomAvailabilityChecker.tsx`** is the Step 9 plan's "quick check availability widget" mounted on `/schedule/room-bookings`: pick a date + period from the school's default bell schedule → calls `useRooms({availabilityDate, availabilityPeriodId})` → renders a tiny pill grid with green/amber dots per active room (powered by Step 5's `available: boolean | null` annotation, query enabled-when-period-picked). **`/my-schedule`** is a single shared route. STAFF branch: `useMyEmployee → useTimetableForTeacher(employeeId)` for the grid + `useSubstitutionsForTeacher(employeeId, {fromDate=today, toDate=+14d})` so the next-two-weeks of upcoming substitution coverage shows up as a list below the grid (and the matching cells in the grid get the amber-highlight treatment). STUDENT branch: `useMyStudent → useTimetableForStudent(studentId)`; teacher names render in each cell. Other personas get an empty state pointing parents at `/children`. **`/children/[id]/schedule`** is the parent route — `useStudent` for the header + `useTimetableForStudent(studentId)` for the grid; the new endpoint's row-scope rejects parents not linked via `sis_student_guardians`, surfaced as an empty state. **Navigation entry points wired:** `/classes` Teacher and Student persona views gain a `My Schedule` button routing to `/my-schedule`; `/children` ChildCard gains a `Schedule` button alongside Attendance / Grades / Report-absence. **Sidebar tile active-state fix (Steps 7+8 carry-over):** `AppDef` gains an optional `routePrefix` field; the Schedule tile sets `routePrefix='/schedule'` so navigating into `/schedule/coverage`, `/schedule/bell-schedules`, `/schedule/rooms`, `/schedule/coverage/history`, etc. all keep the Schedule tile highlighted. Sidebar match resolves to `app.routePrefix ?? app.href`. **Build clean** (API + web): `/my-schedule` 3.73 kB, `/children/[id]/schedule` 3.87 kB First Load JS. **Live verification on `tenant_demo` on 2026-04-28** (5 scenarios): parent (David Chen) `GET /timetable/student/{Maya}` 200 → 4 slots (Maya is enrolled in Periods 1–4 of Rivera's 6 classes per the seed) with full join shape; student (Maya) `GET /timetable/student/{self}` 200; teacher (Rivera) `GET /timetable/student/{Maya}` 200 (Rivera is the assigned teacher of Maya's enrolled classes); teacher `GET /timetable/teacher/{Rivera}` 200 → 6 slots; parent `GET /timetable/student/{unrelated student}` 404 (row-scope rejects). **Stale-API-instance trap caught during smoke:** `pnpm --filter @campusos/api start` doesn't auto-rebuild — first smoke run hit a stale `node dist/main` instance from before the rebuild and returned 404s on the new endpoint. Killing the PID + rebooting from the freshly built `dist/` resolved it. Worth flagging for downstream cycles. **Out of scope this step (deferred):** week-nav on `/my-schedule` (currently anchored on today's week); a date-range picker for the substitution list (currently fixed at next 14 days). (Step 10 of Cycle 5 — the end-to-end CAT — remains to ship; see `docs/campusos-cycle5-implementation-plan.html` and `HANDOFF-CYCLE5.md` for the plan.)

**Step 8 lands the calendar / coverage web surface at `apps/web/src/app/(app)/calendar/` + `apps/web/src/app/(app)/schedule/coverage/`** — 3 routes + 15 more React Query hooks (30 total scheduling hooks) + a second `Calendar` launchpad tile gated on `sch-003:read` (every persona since Step 4) routing to `/calendar`. Reuses the `CalendarIcon` from Step 7. No new backend or schema; Step 8 sits on the 16 endpoints from Step 6 (CalendarService + DayOverrideService + CoverageService + SubstitutionService) and triggers `sch.coverage.assigned` indirectly via the Assign Modal. **`/calendar`** is a Mon-leading 7×6 month-view grid (±6 weeks of events fetched so leading/trailing days from adjacent months render their badges); each cell shows the day number (today gets a campus-700 dot), an inline gray "Closed" pill when a `sch_calendar_day_overrides` row marks `isSchoolDay=false`, and one chip per overlapping calendar event colour-coded by `eventType` via `calendarEventChipClasses` (HOLIDAY rose / PD violet / EARLY_DISMISSAL amber / ASSEMBLY sky / EXAM_PERIOD orange / PARENT_EVENT emerald / FIELD_TRIP teal / CUSTOM gray). Drafts render with a dashed border + 70% opacity (admins only when `Show drafts` is checked). Click any chip → Modal with full event detail + author + bell-schedule override + Affects-attendance pill; admins see Publish-now (PATCH `isPublished:true`) + Delete. Admins also get a `New event` button that opens a multi-field Modal (title, eventType picker, start/end dates, all-day toggle that reveals start/end time inputs only when off, optional description, Affects-attendance toggle) with both `Save draft` (POST `isPublished:false`) and `Publish now` (POST `isPublished:true`) submit paths. **`/schedule/coverage`** is the day-of board: `<input type="date">` defaulted to today + a Today button + a small counter ("3 open · 1 assigned · 0 cancelled"); body groups rows into Open (red-tinted) / Assigned (amber) / Cancelled (gray) sections. Per row shows period + section + course + absent teacher + room + assigned sub + status pill; admins see Assign / Reassign / Cancel buttons. Assign Modal pre-loads `useEmployees` + `useRooms`, filters out the absent teacher and inactive employees from the substitute dropdown, accepts an optional room override (default = original slot's room) + notes, and PATCHes `/coverage/:id/assign` — the Step 6 backend writes the OPEN→ASSIGNED transition + matching `sch_substitution_timetable` row in one tx and emits `sch.coverage.assigned` so the substitute receives an IN_APP notification through the Cycle 3 pipeline (the hook also invalidates `['notifications']` so the substitute's bell badge updates on the next 30s poll). Cancel Modal accepts optional notes and PATCHes `/coverage/:id/cancel` (backend drops the substitution row in the same tx). **`/schedule/coverage/history`** is the reporting view: 5-control filter bar (from / to dates default to ±30 days around today, status dropdown, absent-teacher and substitute name fields — name filters are client-side substring matches against the joined names; status is server-side); rows grouped by `coverageDate` newest-first with `assignedAt` rendered as a localised timestamp. **15 new hooks** added to `use-scheduling.ts`: `useCalendarEvents(args)` / `useCalendarEvent(id)` / `useCalendarDayResolution(date)` / `useCreateCalendarEvent` / `useUpdateCalendarEvent(id)` / `useDeleteCalendarEvent` for events; `useDayOverrides(args)` / `useCreateDayOverride` / `useDeleteDayOverride` for overrides; `useCoverageRequests(args)` (refetch on focus) / `useCoverageRequest(id)` / `useAssignCoverage(id)` (also invalidates `['notifications']`) / `useCancelCoverage(id)` for coverage; `useSubstitutions(args)` / `useSubstitutionsForTeacher(employeeId, args)` for the future Step 9 teacher schedule view. `scheduling-format.ts` extended with `CALENDAR_EVENT_TYPES` const + `calendarEventTypeLabel` + `calendarEventChipClasses` + `coverageStatusLabel` + `coverageStatusPillClasses` + `todayIso()` + `addDaysIso(date, n)`. `apps/web/src/lib/types.ts` extended with `CalendarEventDto` / `CalendarEventType` / `CreateCalendarEventPayload` / `UpdateCalendarEventPayload` / `ListCalendarEventsArgs` / `DayOverrideDto` / `CreateDayOverridePayload` / `ListDayOverridesArgs` / `DayResolutionSource` / `CalendarDayResolutionDto` / `CoverageStatus` / `CoverageRequestDto` / `AssignCoveragePayload` / `CancelCoveragePayload` / `ListCoverageArgs` / `SubstitutionDto` / `ListSubstitutionsArgs`. **Build clean** (`pnpm --filter @campusos/web build`): `/calendar` 5.94 kB, `/schedule/coverage` 4.37 kB, `/schedule/coverage/history` 1.81 kB First Load JS. **Live smoke against `tenant_demo` on 2026-04-28** (8 scenarios): principal `GET /calendar?fromDate=2026-04-01&toDate=2026-06-30` 200 → 4 published events; principal `GET /coverage?fromDate=2026-03-09&toDate=2026-03-09` 200 → 1 ASSIGNED row Park-covers-Rivera-Period-1-Algebra; principal `GET /calendar/overrides` 200 → 1 Winter storm closure; principal `GET /substitutions?fromDate=2026-03-01&toDate=2026-03-31` 200 → matching substitution row; parent `GET /calendar` 200 (sch-003:read granted at Step 4); parent `GET /coverage` 403 (no sch-004:read); teacher `GET /coverage` 200 (sch-004:read granted at Step 4); parent `POST /calendar` 403 (sch-003:admin required). The end-to-end Assign / Cancel path was already verified live in Step 6's smoke; Step 8 just wires the same payload shape into Modal UI. **Out of scope this step (deferred to follow-ups):** week-view fallback for `/calendar` (month-only ships); a dedicated day-override admin Modal (the hooks are wired but no UI yet — the seed's 1 row is enough to demonstrate the resolution chain); ICS / iCal export; the same Sidebar tile active-state UX gap from Step 7 applies to `/schedule/coverage` since the `Schedule` tile's href is `/schedule/timetable` and routes are siblings rather than nested. Step 9 will revisit navigation holistically. (Steps 9–10 of Cycle 5 — the teacher / parent / student schedule views, the room availability checker widget, and the CAT — remain to ship; see `docs/campusos-cycle5-implementation-plan.html` and `HANDOFF-CYCLE5.md` for the plan.)

**Step 7 lands the first scheduling web surface at `apps/web/src/app/(app)/schedule/`** — 4 routes (5 pages) + 15 React Query hooks in `apps/web/src/hooks/use-scheduling.ts` + a single new `Schedule` launchpad tile gated on `sch-001:read` (every staff persona since Step 4) routing to `/schedule/timetable`, plus a new `CalendarIcon` in `apps/web/src/components/shell/icons.tsx`. No new backend or schema; Step 7 is purely additive on the web side, sitting on the 22 endpoints from Step 5. **`/schedule/bell-schedules`** lists each bell schedule with its `Default` pill + period count; admins can `Set default` any non-default schedule (POST `/bell-schedules/:id/set-default`) or open a Modal to `New schedule` (POST `/bell-schedules` with name + scheduleType + optional `isDefault`). **`/schedule/bell-schedules/[id]`** is the period-level editor — hydrates a draft from the schedule's existing periods, renders an inline table (#, name, start, end, type, day-of-week, remove) where each row uses `<input type="time">` + `<select>` for `period_type`, tracks `dirty` so `Save` is disabled unless something changed, validates `HH:MM` shape + `start < end` per row before POSTing the full set to `/bell-schedules/:id/periods` (the backend's bulk DELETE-all + INSERT-each in one tx makes partial saves impossible). Non-admins see read-only rendering. **`/schedule/timetable`** renders the week-view grid: rows = the periods of the school's default bell schedule, columns = Mon–Fri; slots with `dayOfWeek=null` render in every weekday column (the seed pattern). Each cell renders a campus-100 chip with `classSectionCode`, `courseName`, teacher name (or "TBD"), and room. Filter chips at the top (All / By teacher / By class / By room) drive `useTimetable({…})` switching to the matching server-side filter — the schema's two EXCLUSION constraints from Step 2 mean overlaps are impossible. **`/schedule/rooms`** lists rooms as a card grid with type / capacity / building+floor / Projector+AV pills; admins can filter inactive + open a Modal to `New room`. Each card carries a `View weekly schedule →` link to `/schedule/timetable?roomId=…` (URL handoff to the timetable filter). **`/schedule/room-bookings`** is a day-grouped booking calendar — filters by `from` date / room / status (default `CONFIRMED only`), grouped by local date of `startAt`. Anyone with `sch-005:write` can `New booking` (Modal with room dropdown, purpose, `<input type="datetime-local">` start/end, converted to `Date.toISOString()` before POST); the Step 5 `assertNoConflicts` gate (existing CONFIRMED bookings + active timetable slots whose period clock-time on the booking's ISO weekday overlaps the booking window) returns a 409 the page surfaces via Toast directly. Cancel button is offered on owned bookings + admins; PATCH `/room-bookings/:id/cancel`. **Helpers** live in `apps/web/src/lib/scheduling-format.ts` (`scheduleTypeLabel`, `periodTypeLabel`, `roomTypeLabel`, `dayOfWeekLabel`, `formatTime`, `bookingStatusLabel`, `changeRequestStatusLabel`, plus `ROOM_TYPES` / `PERIOD_TYPES` / `WEEKDAY_LABELS` const arrays). `apps/web/src/lib/types.ts` extended with the full Cycle 5 DTO surface (BellScheduleDto / PeriodDto / RoomDto / TimetableSlotDto / RoomBookingDto / RoomChangeRequestDto + payloads + filter args). The 15 hooks (`useBellSchedules`, `useBellSchedule`, `useCreateBellSchedule`, `useUpdateBellSchedule(id)`, `useUpsertPeriods(scheduleId)`, `useSetDefaultBellSchedule`, `useTimetable(args)`, `useTimetableForTeacher`, `useTimetableForClass`, `useTimetableForRoom`, `useCreateTimetableSlot`, `useUpdateTimetableSlot(id)`, `useDeleteTimetableSlot`, `useRooms(args)`, `useRoom`, `useCreateRoom`, `useUpdateRoom(id)`, `useRoomBookings(args)`, `useCreateRoomBooking`, `useCancelRoomBooking`, `useRoomChangeRequests(args)`, `useCreateRoomChangeRequest`, `useApproveRoomChangeRequest(id)`, `useRejectRoomChangeRequest(id)`) match the Step 5 endpoint shape verbatim, with mutation invalidations covering the affected query keys (`useUpsertPeriods` also invalidates `['scheduling', 'timetable']` because period-time changes shift grid columns). **Build clean** (`pnpm --filter @campusos/web build`): `/schedule/bell-schedules` 2.66 kB, `/schedule/bell-schedules/[id]` 2.92 kB, `/schedule/timetable` 3.05 kB, `/schedule/rooms` 2.99 kB, `/schedule/room-bookings` 3.31 kB First Load JS. **Live smoke against `tenant_demo` on 2026-04-28** (8 scenarios): principal `GET /bell-schedules` 200 → 2 seeded schedules; principal `GET /timetable` 200 → 6 Rivera slots with full join shape; principal `GET /rooms` 200 → 10 rooms; principal `GET /room-bookings` 200 → 1 seeded Main Hall booking; parent `GET /bell-schedules` 403; parent `GET /timetable` 403; teacher `GET /bell-schedules` 200 (sch-001:read granted in Step 4); teacher `GET /room-bookings` 200 (sch-005:read granted in Step 4). Mutation paths (create schedule / upsert periods / create room / create booking) are already verified end-to-end in Step 5's smoke and the page wiring uses the same payload shapes. **Out of scope this step (deferred to Step 8/9):** the admin slot CRUD modal on the timetable grid (hooks are ready — `useCreateTimetableSlot` / `useUpdateTimetableSlot` / `useDeleteTimetableSlot` — but no UI), the room change request page (hooks ready), and the Step 9 availability checker widget (the `useRooms({ availabilityDate, availabilityPeriodId })` hook is already wired). Sidebar `Schedule` tile active-state has a small UX gap — clicking into `/schedule/bell-schedules` doesn't keep the tile highlighted because the tile's href is `/schedule/timetable` and the Sidebar matches by href prefix. Punted to Step 9 where the navigation pattern can be revisited holistically. (Steps 8–10 of Cycle 5 — the calendar / coverage UI, the teacher / parent / student views, and the CAT — remain to ship; see `docs/campusos-cycle5-implementation-plan.html` and `HANDOFF-CYCLE5.md` for the plan.)

**Cycle 4 (HR & Workforce Core) is COMPLETE + APPROVED at `76ddf03` (REVIEW-CYCLE4-CHATGPT — final verdict). Round 1 against `efbcb44` returned REJECT pending 1 BLOCKING + 3 MAJOR; Round 2 against `76ddf03` returned APPROVED** after all four fixes (`bda8a16`) plus the CI-fix commit (TS2347 in seed-hr + Prettier drift, `76ddf03`) landed. Cycle 4 ships clean. **REVIEW-CYCLE4 fixes:** (1) BLOCKING — `LeaveService.{approve,reject,cancel}` now `SELECT … FOR UPDATE` the request row inside the same transaction that writes the balance + status, so concurrent admin approves serialise on the row lock and the second one re-reads `status='APPROVED'` and 400s. Verified live with 5 parallel approve curls — 1 won, 4 got 400, balance moved cleanly. (2) MAJOR — `ActorContextService.resolveEmployeeId` now resolves `employment_status IN ('ACTIVE','ON_LEAVE')`; TERMINATED + SUSPENDED stay excluded. ON_LEAVE employees keep portal access (own profile, balances, requests) — TERMINATED still 404s on `/employees/me`. (3) MAJOR — `TrainingComplianceService.getDashboard` accepts `actor.isSchoolAdmin` OR a tenant-scope-chain `hr-004:admin` check via `permissionCheckService.hasAnyPermissionInTenant`. The `apps.tsx` tile already gates on the same union, so UI + API agree. (4) MAJOR — `hr.leave.coverage_needed` republishes now use a deterministic event_id derived from `sha1(inbound_event_id + ':hr.leave.coverage_needed.v1')` formatted as a v5-shaped UUID, plus `correlationId = inbound.event_id`. A Kafka redelivery republish carries the exact same event_id, so Cycle 5's future consumer idempotency catches the dup before doing the work twice. Optional `eventId` field added to `EnvelopeOptions` + `EmitOptions` to enable this pattern for future republishers. Verified live — Python independently computed the expected hash and matched the wire envelope; v5 marker correctly set.

Step 0 retired the Cycle 2 DEVIATION 4 mapping; Steps 1–4 added 17 hr\_\* tables (74 tenant tables total); Step 5 seeded 4 employees + 5 positions + 4 certs + 4 compliance rows + 8 onboarding tasks; Steps 6–7 shipped 23 HR endpoints + 5 Kafka emits + the LeaveNotificationConsumer; Steps 8–9 shipped 7 HR web routes + 3 launchpad tiles (Staff / Leave / Compliance) + 16 hooks. **Step 10 lands `docs/cycle4-cat-script.md`** — the reproducible end-to-end vertical slice CAT, verified live against `tenant_demo` on 2026-04-28. The script wraps a 4-scenario bridge-verification block (Step 0 carry-over: 4 hr_employees rows, 0 orphans across the 4 bridged columns, 74 tenant base tables, 0 cross-schema FKs) around the 9 plan scenarios: directory list (4 employees), employee profile + certifications + balances, submit + Kafka envelope verified on the wire, admin approve + balance update from used 2 → 3 / pending 1 → 0, `hr.leave.coverage_needed` republish with all 6 of Rivera's classes inline (Algebra 1 → Chemistry — for Cycle 5 Scheduling to consume), 3 IN_APP notifications enqueued and SENT (2 admins on requested + Rivera on approved), compliance dashboard amber row for Rivera's Teaching Licence at 60 days, 6 permission denial paths covering both gate-tier (parent/student no HR codes) and admin-tier (teacher cannot approve / verify cert / read dashboard) checks. **All 16 checks pass.** No new code in the closeout commit; the seed inconsistency caught during the Step 7 smoke (Rivera PD pending=0 vs PENDING request for 1d) was fixed in Step 7's commit (`70b6cf3`), so the fresh provision running this CAT produces the correct shape from the start. **Cycle 4 ships clean to the post-cycle architecture review.**

**Step 9 closes the HR UI.** Two new launchpad tiles via the single-source `apps.tsx` catalogue: a **Leave** tile under `hr-003:read` (every staff persona) using the existing `AttendanceIcon`, and a **Compliance** tile under `sch-001:admin` OR `hr-004:admin` using the existing `CheckCircleIcon`. Four new pages: `/leave` (employee view — balance cards with `used + pending` two-tone progress bar / `available` headline; request history with status pills; per-row Cancel button on PENDING requests with a `window.confirm` guard, wired to `useCancelLeaveRequest`); `/leave/new` (request form — leave-type dropdown defaulted to the first active type, `?startDate`/`?endDate` `<input type="date">`, `daysRequested` auto-calc from the date range with half-day support and a manual override, optional reason, live balance hint for the selected type, amber warning when daysRequested exceeds available, submit via `useSubmitLeaveRequest` then `router.push('/leave')` + toast); `/leave/approvals` (admin queue gated on `sch-001:admin` — non-admins see an empty-state explaining admin-only; lists every PENDING via `useLeaveRequests({status:'PENDING'})`; per-row Approve / Reject buttons open a Modal with optional review-notes textarea; Approve modal uses primary CTA, Reject uses red CTA; Modal mutations via `useApproveLeaveRequest(req.id)` / `useRejectLeaveRequest(req.id)` then close + toast); `/compliance` (admin dashboard — 3-stat header card row Active employees / With gaps / Compliant %, three filter chips (All / Has gaps / Fully compliant), per-employee row with `compliantCount / amberCount / redCount` summary pills + a `View profile` link to `/staff/:id` + a Details toggle that expands the per-requirement rows with green/amber/red urgency pills inline). Build clean: `/leave` 5.66 kB, `/leave/new` 5.74 kB, `/leave/approvals` 6.06 kB, `/compliance` 5.65 kB First Load JS.

**Step 8 lands the first HR web surface.** A new "Staff" tile appears on the home launchpad and in the sidebar for any user with `hr-001:read` (driven by the single-source `apps/web/src/components/shell/apps.tsx::getAppsForUser`, using the existing `PeopleIcon`). Three pages: `/staff` (searchable directory list — avatar + full name + primary position title + employee number + email; admin-only `Include inactive` checkbox; client-side search wired to the API's `?search=` and `?includeInactive=` query string); `/staff/[id]` (tabbed profile — Info / Certifications / Leave / Documents; Info tab is always visible to anyone with `hr-001:read`; the other three tabs render only for the owning employee or for school admins, gated by comparing `useMyEmployee().data.id` to the route id and checking `sch-001:admin`); and `/staff/me` (client-side redirect that calls `useMyEmployee` then `router.replace('/staff/:id')`, with a clean "no employee record" empty state for non-staff personas). Certifications tab shows a 4-stat summary (Requirements / Compliant / Expiring / Non-compliant) plus a per-cert list with green/amber/red urgency pills derived from `daysUntilExpiry` + `verificationStatus`, and a per-requirement breakdown joined to the seeded compliance rows. Leave tab shows balances (own profile only — admin viewing someone else gets a hint message) and the request history (admin sees all rows for the target employee). Documents tab is a list view; uploads land in Step 9. **`apps/web/src/lib/types.ts` extended** with the full HR DTO surface (EmployeeDto, EmployeePositionDto, PositionDto, EmployeeDocumentDto, CertificationDto, LeaveTypeDto, LeaveBalanceDto, LeaveRequestDto, ComplianceRowDto, EmployeeComplianceDto, ComplianceDashboardDto + payload types). **`apps/web/src/hooks/use-hr.ts` (new)** exposes 16 hooks: `useEmployees` (refetch on focus), `useEmployee`, `useMyEmployee`, `usePositions` (5 min stale), `useEmployeeDocuments`, `useEmployeeCertifications`, `useExpiringCertifications` (60 s poll), `useEmployeeCompliance`, `useComplianceDashboard` (60 s poll), `useLeaveTypes` (5 min stale), `useMyLeaveBalances`, `useLeaveRequests`, plus 5 mutations (`useSubmitLeaveRequest`, `useApproveLeaveRequest`, `useRejectLeaveRequest`, `useCancelLeaveRequest`, `useVerifyCertification`) — every mutation invalidates the affected query keys. Each hook accepts an `enabled` boolean so persona gating doesn't fire 403 calls. Build clean: `pnpm --filter @campusos/web build` ships `/staff` 6.18 kB, `/staff/[id]` 6.49 kB, `/staff/me` 2.34 kB First Load JS.

**Step 7 lands the leave / cert / compliance API surface plus the first HR Kafka consumer.** `LeaveService` exposes 7 endpoints (`GET /leave-types`, `GET /leave/me/balances`, `GET /leave-requests`, `GET /leave-requests/:id`, `POST /leave-requests`, `PATCH /leave-requests/:id/{approve,reject,cancel}`) and emits 4 Kafka events on every transition (`hr.leave.requested`, `hr.leave.approved`, `hr.leave.rejected`, `hr.leave.cancelled`) with `sourceModule:'hr'`. Submit / approve / reject / cancel each run in `executeInTenantTransaction` — balance UPDATEs respect the non-negative CHECKs from migration 012, so a cancel that would underflow `pending` or `used` fails loudly with a 23514 instead of silently corrupting totals. Per-employee balance reads return one row per leave type (zeros for types with no balance row yet) and surface a derived `available = accrued - used - pending`. `CertificationService` exposes 4 endpoints (`GET /employees/:id/certifications`, `POST /employees/:id/certifications`, `GET /certifications/:id`, `PATCH /certifications/:id/verify`, plus admin `GET /certifications/expiring-soon`) with the same own-or-admin row-scope guard as documents and a `daysUntilExpiry` field computed from today; `verify` is admin-only and emits `hr.certification.verified`. `TrainingComplianceService` exposes 2 endpoints (`GET /employees/:id/compliance` and admin `GET /compliance/dashboard`) with a derived `urgency` tier (green / amber / red) computed from `is_compliant` + `days_until_due`. The dashboard always includes every ACTIVE employee even when they have zero compliance rows (LEFT-side roster). `LeaveNotificationConsumer` (consumer group `leave-notification-consumer`) subscribes to all 4 leave topics, notifies admins on `requested`, notifies the submitter on `approved` / `rejected`, and on `approved` republishes `hr.leave.coverage_needed` with the affected class ids resolved via `sis_class_teachers` for the Cycle 5 Scheduling consumer. **HR endpoint count is now 23** (12 from Step 6 + 11 from Step 7). Build clean, all routes mapped on boot, leave-notification-consumer subscribes successfully (after pre-creating the 6 dev `dev.hr.*` topics — the same auto-creation race that affects Cycle 3's audience-fan-out worker on a fresh broker). **Live verification on `tenant_demo` (22 scenarios):** 5 leave types listed; teacher's seeded balances show non-zero used/pending; admin queue surfaces the 1 PENDING PD request; teacher submits a 1.5-day Sick request → `pending` jumps to 1.5; admin approves → `pending` drops to 0 and `used` rises 2 → 3.5; `hr.leave.requested` notifies the 2 admin accounts (Mitchell + Platform Admin) and `hr.leave.approved` notifies Rivera, all 3 rows SENT to Redis ZADD inbox; **`hr.leave.coverage_needed` published to `dev.hr.leave.coverage_needed` with all 6 of Rivera's classes inline** (Algebra 1 / English 9 / Biology / World History / Geometry / Chemistry); teacher cancels the seeded PD request → `pending` correctly returns to 0; admin POST cert + admin PATCH verify works end-to-end; teacher denied cert verify with 403 (`hr-004:write` admin-only); parent denied with 403 INSUFFICIENT_PERMISSIONS at the gate; `/certifications/expiring-soon` returns Rivera's Teaching Licence with the dynamic 60-day expiry from Step 5's seed. **Bug caught and fixed by the smoke:** Step 5 seed had Rivera's PD balance row at `pending=0` while also seeding a PENDING PD request for 1 day — the schema's `pending_chk >= 0` correctly rejected the cancel underflow, exposing the seed inconsistency. `seed-hr.ts` updated to set Rivera PD `pending=1.0` to match the seeded request; the live demo tenant patched in-place so the smoke could complete. Personal Leave's stale `used=1, pending=1` (no matching seeded request) reset to zeros for the same reason.

**Step 6 lands the first HR API surface at `apps/api/src/hr/`** with three services and 12 endpoints. `EmployeeService` handles the staff directory list (`GET /employees`, joined to `iam_person` for first/last name + `platform_users` for email + the most recent `hr_employee_positions` row for the primary position title), single-record reads (`GET /employees/:id` and `GET /employees/me` which resolves the calling user via `actor.employeeId` and 404s if the actor is parent/student/Platform Admin), admin-only create (validates the supplied `personId` + `accountId` against the platform schema, links them to a freshly minted `hr_employees` row inside one `executeInTenantTransaction`, optionally seeds a primary `hr_employee_positions` assignment from `initialPositionId`), and admin-only patch (employee_number / employment_type / employment_status / termination_date — dynamic SET-clause builder so callers can update one field or many). `PositionService` is admin-only for writes (`POST` / `PATCH` gated on `hr-001:admin`), reads on `hr-001:read`; the list query returns each position with `activeAssignments` (count of `hr_employee_positions` rows where `effective_to IS NULL`) and the joined `sis_departments.name` for the soft `department_id` ref. `EmployeeDocumentService` enforces a row-scope guard (`assertCanAccess` — admin OR `actor.employeeId === employeeId`, otherwise 403); list returns non-archived rows joined to `hr_document_types` for the type label; create validates the document type is active + the employee exists; archive flips `is_archived=true` (soft delete) — hard delete is intentionally not exposed. The document column `uploaded_by` is set from `actor.accountId` so the audit trail records the platform user, not the employee record. **DTOs use `class-validator`** with `IsIn` arrays for the employment_type and employment_status enums (keeps the values in sync with the SQL CHECK constraints). The position list controller uses `class-transformer` `@Transform` to coerce `?includeInactive=true` from the query string into a boolean. `HrModule` imports `TenantModule` + `IamModule` and lives at index 4 in `AppModule.imports` between `ClassroomModule` and `NotificationsModule`. Build clean, all 12 routes mapped on boot. Live verification on `tenant_demo`: principal@ sees 4 employees in `/employees` (Mitchell/Rivera/Park/Hayes ordered by last name, each with employee_number + primary position title); teacher@ `/employees/me` resolves Rivera with `positions=1` and `primaryPositionTitle='Teacher'`; principal@ `/positions` returns 5 (incl. the unassigned Administrative Assistant with `activeAssignments=0`); parent@ blocked at the permission gate on `/employees` and `/employees/me` with `hr-001:read` required; student@ blocked similarly on `/positions`; counsellor@ trying to read Rivera's documents gets a clean 403 from the row-scope check (own documents OR admin only); teacher@ POST `/positions` returns 403 (`hr-001:admin` required); principal@ creates a "Smoke Position" then PATCHes `isActive=false` round-trip; principal@ POST `/employees` with bogus UUIDs returns 400 from the validators (proves admin path is reachable to admins).

**Step 5 layers data on top of the schema:** `seed-hr.ts` extended with idempotent layers (each gates on its own row count) — 5 positions (Teacher, Principal, VP, Counsellor, Administrative Assistant) + 4 position assignments mapping the 4 employees to their roles, 5 leave types (Sick / Personal / Bereavement / PD / Unpaid) + 20 per-(employee × type) balances for the current academic year (Rivera has used some sick + personal so the CAT shows non-zero used/pending), 2 sample leave requests for Rivera (1 APPROVED sick leave from March, 1 PENDING PD day for next month), 4 certifications (Rivera Texas Teaching Licence VERIFIED + First Aid + Safeguarding L1; Mitchell DBS Enhanced VERIFIED — **Rivera's licence is seeded with `expiry_date = today + 60 days` so it dynamically drives the CAT compliance dashboard amber row**), 4 training requirements (3 school-wide ANNUAL/BIENNIAL + 1 position-specific Teaching Licence Renewal), 4 pre-computed compliance rows (Rivera 2 compliant + 1 not-compliant linked to the expiring licence; Mitchell 1 not-compliant Safeguarding), 4 document types (Contract / Background Check / W-4 / Teaching Licence Copy with retention_days populated), and 1 "New Teacher Onboarding" template + 1 NOT_STARTED checklist for Rivera + 8 tasks (DOCUMENT × 4, TRAINING × 2, SYSTEM_ACCESS × 1, ORIENTATION × 1) covering onboarding from contract through classroom orientation. **`seed-iam.ts` updated:** Teacher role gains HR-001:read + HR-003:read+write + HR-004:read (4 perms, 27 → 31). Staff role same 4 grants (6 → 10). The role-assignment block was rewritten from "if any assignments exist, skip" to "per-user lookup-or-create" so the new `vp@` (Linda Park) and `counsellor@` (Marcus Hayes) staff accounts now get the Staff role + scope assignment without dropping existing assignments. After `tsx src/build-cache.ts`: cache holds 7 account-scope pairs (was 5) — admin/principal 444, teacher 31, student 15, parent 11, vp/counsellor 10. Tenant base table count unchanged at 74 (data only — no DDL).

**Step 4 lands `014_hr_onboarding.sql`** with 3 base tables: `hr_onboarding_templates` (school-scoped catalogue, optional `position_id` for position-specific templates, UNIQUE(school_id, name)), `hr_onboarding_checklists` (per-(employee, template) instance with status NOT_STARTED → IN_PROGRESS → COMPLETED enforced by a multi-column CHECK that started_at and completed_at stay in sync with status, UNIQUE(employee_id, template_id) so a hire can hold at most one checklist per template), `hr_onboarding_tasks` (5 category values DOCUMENT/TRAINING/SYSTEM_ACCESS/ORIENTATION/OTHER, 4 status values PENDING/IN_PROGRESS/COMPLETED/SKIPPED, `due_days_from_start INT >= 0` CHECK, multi-column CHECK that `completed_at` is set if and only if status is COMPLETED or SKIPPED). 3 intra-tenant FKs (1 to `hr_employees ON DELETE CASCADE`, 1 to `hr_onboarding_templates`, 1 to `hr_onboarding_checklists ON DELETE CASCADE`). Tenant logical base table count: 74 (was 71). Live verification on `tenant_demo`: all 8 CHECK violations rejected (status enums, status/started-at sync, category enum, due_days negative, completed-at sync), happy-path "New Teacher Onboarding" template with 3 tasks instantiated for Rivera + one task moved to COMPLETED + checklist moved to IN_PROGRESS, UNIQUE(employee_id, template_id) rejects duplicate, ON DELETE CASCADE drops all 3 tasks when the parent checklist is deleted.

**Step 3 lands `013_hr_certifications_and_training.sql`** with 5 base tables: `hr_staff_certifications` (10 certification types — TEACHING_LICENCE, FIRST_AID, SAFEGUARDING_LEVEL1/2, DBS_BASIC/ENHANCED, FOOD_HYGIENE, FIRE_SAFETY_WARDEN, SPECIALIST_SUBJECT, CUSTOM; 4 verification statuses — PENDING/VERIFIED/EXPIRED/REVOKED; ADR-015 followed — `reference_number` + `verification_status` only, never raw DBS payload), `hr_training_requirements` (school-scoped with optional `position_id` for position-specific mandates, frequency CHECK ONE_TIME/ANNUAL/BIENNIAL/TRIENNIAL/CUSTOM with a multi-column CHECK that `custom_frequency_months` is set if and only if frequency=CUSTOM), `hr_training_compliance` (UNIQUE(employee_id, requirement_id) materialised state with `linked_certification_id` FK to `hr_staff_certifications ON DELETE SET NULL` so a deleted cert leaves the compliance row defensible), `hr_cpd_requirements` (UNIQUE(school_id, position_id, academic_year_id) PD/credit-hour mandates), `hr_work_authorisation` (UNIQUE(employee_id) right-to-work with 6 document types, separate `expiry_date` and `reverification_due_date` because some EADs require re-verification on a different cadence). 7 intra-tenant FKs (4 to `hr_employees ON DELETE CASCADE`, 1 to `hr_training_requirements ON DELETE CASCADE`, 1 to `hr_staff_certifications ON DELETE SET NULL`, 1 each to `hr_positions` and `sis_academic_years` non-cascade). Tenant logical base table count: 71 (was 66). Live verification on `tenant_demo`: 7 CHECKs fire (cert type/status/dates, requirement frequency/custom, work-auth type, CPD pd_hours), happy path inserts succeed (Rivera Teaching Licence VERIFIED, ANNUAL renewal requirement, compliance row linked to the cert), ON DELETE SET NULL nulls `linked_certification_id` when the cert is deleted while keeping the compliance row, UNIQUE on `(employee_id, requirement_id)` and on `hr_work_authorisation.employee_id` rejects duplicates, ON DELETE CASCADE on `hr_employees` drops cert + compliance + work_auth rows for a temp employee.

**Step 2 lands `012_hr_leave_management.sql`** with 3 base tables: `hr_leave_types` (per-school catalogue with `is_paid` / `accrual_rate` / optional `max_balance`), `hr_leave_balances` (UNIQUE(employee_id, leave_type_id, academic_year_id) with `accrued`/`used`/`pending NUMERIC(5,2)` non-negative CHECKs, FK to `sis_academic_years`), and `hr_leave_requests` (status CHECK PENDING/APPROVED/REJECTED/CANCELLED, half-day `days_requested NUMERIC(4,1) > 0` CHECK, dates CHECK `end_date >= start_date`, an `is_hr_initiated` boolean with a multi-column CHECK that enforces `(is_hr_initiated, hr_initiated_by, hr_initiated_reason)` are all-set or all-null together for HR-initiated leaves like mandatory PD days). 5 intra-tenant FKs (2 to `hr_employees ON DELETE CASCADE`, 2 to `hr_leave_types`, 1 to `sis_academic_years` non-cascade). Tenant logical base table count: 66 (was 63). Approval flow is documented at the schema layer; the Step 7 LeaveService will own the state transitions and Kafka emits (`hr.leave.requested`, `hr.leave.approved`, `hr.leave.rejected`, `hr.leave.cancelled`). Live verification on `tenant_demo`: all 6 CHECKs fire (status, dates, days, hr-initiated mismatch, accrual_rate negative, balance accrued negative), UNIQUE on `(employee, type, year)` rejects duplicates, happy-path multi-insert across all 3 tables succeeds, ON DELETE CASCADE drops 1 balance + 1 request when a temp `hr_employees` row is deleted.

**Step 1 lands 5 base tables on top of `hr_employees`** in the same `011_hr_employees_and_positions.sql` migration: `hr_positions` (UNIQUE(school_id, title), soft FK `department_id` → `sis_departments`, `is_teaching_role` flag), `hr_employee_positions` (DB-enforced FKs to `hr_employees ON DELETE CASCADE` and `hr_positions`, `effective_from`/`effective_to` date range, partial UNIQUE index on `(employee_id) WHERE is_primary AND effective_to IS NULL` so an employee has at most one current primary position, FTE CHECK 0 < fte ≤ 1.000), `hr_emergency_contacts` (CASCADE on employee delete, partial UNIQUE on `is_primary`), `hr_document_types` (UNIQUE(school_id, name), retention_days CHECK > 0), `hr_employee_documents` (CASCADE on employee delete, FK to document_type, signed-URL pattern documented in COMMENT). 5 new intra-tenant FKs, 0 cross-schema FKs. Tenant logical base table count: 63 (was 58). Live verification on `tenant_demo`: every CHECK fires (dates, FTE > 1.0, retention 0, file_size_bytes -1), FK rejection returns clean ERROR, happy-path multi-insert across all 5 tables succeeds, ON DELETE CASCADE on a temporary `hr_employees` row drops all 3 child rows. **Splitter trap struck again** — even block-comment text containing `;` cuts the migration mid-comment because the splitter splits on every semicolon regardless of quoting context (block-comment, string literal, anywhere). The 011 migration's header now spells out "no semicolons" instead of using a literal `;` as an example.

`packages/database/prisma/tenant/migrations/011_hr_employees_and_positions.sql` lands the `hr_employees` table (soft FK to `platform.iam_person` via UNIQUE `person_id` + soft FK to `platform.platform_users` via UNIQUE `account_id`, CHECK constraints on `employment_type` + `employment_status`). `packages/database/src/seed-hr.ts` (idempotent, gated on `hr_employees` row count) inserts 4 employee rows for `tenant_demo` (Mitchell as `principal@`, Rivera as `teacher@`, Park as `vp@`, Hayes as `counsellor@`; the `admin@` Platform Admin persona is intentionally NOT bridged), then runs four `UPDATE … FROM hr_employees WHERE e.person_id = t.column` bridge statements inside one `executeInTenantTransaction` to re-point `sis_class_teachers.teacher_employee_id`, `cls_grades.teacher_id`, `cls_lessons.teacher_id`, and `cls_student_progress_notes.author_id` from `iam_person.id` to `hr_employees.id`. Live verification: 4 rows inserted, 6 + 62 + 0 + 1 rows bridged across the four columns, 0 orphans. `ActorContextService.resolveActor(...)` now populates `actor.employeeId: string | null` via a tenant-scoped lookup against `hr_employees.person_id`. All staff-side service code substituted: `attendance.service.ts::assertCanWriteClassAttendance`, `assignment.service.ts::{assertCanWriteClass,canReadClass STAFF,isClassManager}`, `gradebook.service.ts::{assertCanViewStudent STAFF,isClassManager}`, `submission.service.ts::{isManagerOfClass,canSeeSubmission STAFF}`, `grade.service.ts::{batchGrade,upsertGrade}` (4 INSERT/UPDATE sites; `ForbiddenException` guard at entry of both write paths since `cls_grades.teacher_id` is NOT NULL), `progress-note.service.ts::{upsert,listForStudent STAFF,assertCanViewStudent STAFF}` (2 INSERT/UPDATE sites + `ForbiddenException` guard), `student.service.ts::visibilityClause STAFF`, `class.service.ts::listForTeacherEmployee` (renamed from `listForTeacherPerson`), `class.service.ts::loadTeachersForClasses` (now joins through `hr_employees` to `iam_person`), `class.controller.ts::my` (resolves actor + passes `actor.employeeId`), `audience-fan-out.worker.ts::audienceClass` (joins through `hr_employees.account_id` to `platform_users` for the CLASS-audience teacher branch). `IamModule` now imports `TenantModule` so `ActorContextService` can do the tenant-scoped lookup. Live smoke against `tenant_demo` confirmed `GET /classes/my` for `teacher@` returns the same 6 classes as before the bridge, `GET /students` returns 15 students via the new STAFF predicate, `GET /classes/:id/gradebook` returns 3 students for the P1 class. The four `COMMENT ON COLUMN` annotations on the bridged columns were rewritten to retire the "TEMPORARY HR-EMPLOYEE IDENTITY MAPPING" wording and adopt the permanent "Soft FK to hr_employees(id) per ADR-055" convention. **Migration gotcha re-confirmed:** the SQL splitter in `provision-tenant.ts` cuts on `;` even inside string literals — the `COMMENT ON COLUMN` strings cannot contain `;`. Use commas / "and" / em-dashes instead. Tripped this on the first re-provision attempt; the four updated COMMENTs and the new `hr_employees.school_id` COMMENT all use punctuation that survives the splitter. (Steps 1–10 of Cycle 4 — schemas for positions, leave, certifications, training compliance, onboarding, and the corresponding NestJS modules + UI + CAT — remain to ship; see `docs/campusos-cycle4-implementation-plan.html` and `HANDOFF-CYCLE4.md` for the plan.)

Cycle 0 (Platform Foundation) is COMPLETE. **Cycle 1 (SIS Core + Attendance) is COMPLETE — all 11 steps done. Post-cycle architecture review fixes (REVIEW-CYCLE1) landed.** Schema, seed, SIS API, Attendance API + Kafka emits, web UI shell, Teacher Dashboard, Attendance Taking UI with confirm modal + batch submit, Parent Dashboard + child attendance calendar + absence-request form, AdminDashboard (school-wide overview + pending-absence queue), and a verified end-to-end vertical slice (`docs/cycle1-cat-script.md`). The post-cycle review tightened tenant isolation (`SET LOCAL search_path` inside an interactive tx), added row-level authorization to student reads, gated attendance writes by `sis_class_teachers` membership, and replaced cross-scope admin checks with a tenant-scope-chain check.

**Cycle 3 (Communications) is COMPLETE — all 11 steps done. Post-cycle architecture review APPROVED at commit `592d366` (REVIEW-CYCLE3-CHATGPT — final verdict).** Round 1 at SHA `7c7e3d4` returned REJECT pending two reliability/security fixes; Round 2 at SHA `592d366` returned APPROVED to proceed. The three findings (BLOCKING 1 Kafka consumer error swallowing, BLOCKING 2 NotificationDeliveryWorker SENT-as-in-flight, MAJOR overly-broad `isManager()` scope) were all fixed in `592d366`.

**Reviewer's carry-over for the next cycle:** verify `platform.platform_dlq_messages` is in migrations + available in every environment since the consumer now depends on it. **Already satisfied at the schema level** — the table ships in the platform Prisma migration `20260427211003_add_communications_platform_tables` (Cycle 3 Step 3), and the CI workflow runs `npx prisma migrate deploy --schema=prisma/platform/schema.prisma` so the table is created in CI + every environment that follows the deploy pipeline. The remaining work is operational: wire a DLQ-row dashboard / alert into Phase 2 so a parked poison message gets human attention, not just a quiet row in a table. **REVIEW-CYCLE3 fixes (2026-04-28):** (1) `apps/api/src/kafka/kafka-consumer.service.ts` no longer swallows handler failures — it rethrows so KafkaJS retains the offset, with an in-memory `(group, topic, partition, offset) → attempts` map bounded by `MAX_HANDLER_ATTEMPTS=5` so a poison message gets parked into `platform.platform_dlq_messages` (with `event_id`, `tenant_id`, headers, payload, error_class/message, retry_count) and committed instead of looping forever. Malformed JSON is parked directly without retry since it can't recover. (2) `apps/api/src/notifications/notification-delivery.worker.ts` now uses a proper `PROCESSING` in-flight state — Phase 0 stale-row sweep recovers PROCESSING > 5 minutes back to PENDING (preserving attempts so MAX_ATTEMPTS still bounds total retries), Phase 1 SELECT FOR UPDATE SKIP LOCKED + UPDATE PROCESSING + processing_started_at, Phase 2 deliver outside the claim tx then UPDATE SENT (only after Redis ZADD + `msg_notification_log` write succeed). New tenant migration `010_msg_queue_processing_state.sql` extends the queue status CHECK to include PROCESSING and adds a `processing_started_at TIMESTAMPTZ` column + partial index. The migration uses `/* */` block-comment style — line `--` headers cause the SQL splitter to filter the entire first statement. Tenant base table count: still 57 (only constraint + column added). (3) `apps/api/src/announcements/announcement.service.ts::isManager()` no longer returns true for any actor with `personType === 'STAFF'` — it now calls `permissionCheckService.hasAnyPermissionInTenant(accountId, schoolId, ['com-002:write'])` so manager status follows the actual permission grant. The IAM seed gives Staff just `com-002:read`, so a staff member is correctly treated as a reader (audience-row-required, no draft visibility) instead of a manager. School admins still bypass via `actor.isSchoolAdmin`. All 4 call sites in the service await the new async helper. CAT smoke (all 7 scenarios) re-verified live against `tenant_demo` after the fixes; queue inspection post-run shows zero rows stuck in PROCESSING (80/80 SENT). Step 1 lands `packages/database/prisma/tenant/migrations/007_msg_messaging.sql` — 6 base messaging tables (`msg_thread_types`, `msg_threads`, `msg_thread_participants`, `msg_messages`, `msg_message_attachments`, `msg_message_reads`) plus 64 HASH partitions of `msg_threads` (per ADR-047, partition key `school_id`) and 24 monthly RANGE partitions of `msg_messages` covering 2025-08 → 2027-08. Soft UUID refs to `platform.platform_users` per ADR-001/020. Composite PKs `(id, school_id)` for `msg_threads` and `(id, created_at)` for `msg_messages` (the partition columns must appear in the unique constraint). FKs from child tables to the partitioned parents follow the `sis_attendance_evidence` precedent — denormalise the partition key onto the child + add an index, no DB-enforced FK. **Step 2 lands `packages/database/prisma/tenant/migrations/008_msg_notifications_and_announcements.sql` — 7 base tables (`msg_alert_types`, `msg_notification_queue`, `msg_notification_preferences`, `msg_notification_log`, `msg_announcements`, `msg_announcement_audiences`, `msg_announcement_reads`) plus 24 monthly RANGE partitions of `msg_notification_log` (sent_at, 2025-08 → 2027-08, matching the messages window). `msg_notification_log` uses composite PK `(id, sent_at)` so the partition column is in the unique constraint. The notification queue does NOT carry a UNIQUE constraint on `idempotency_key` — the partial index on it is for read-side dedup investigation only; authoritative idempotency is Redis SET NX (per the plan, to avoid deadlocks during emergency fan-out). 3 intra-tenant DB-enforced FKs (`msg_announcements.alert_type_id → msg_alert_types`, `msg_announcement_audiences.announcement_id → msg_announcements ON DELETE CASCADE`, `msg_announcement_reads.announcement_id → msg_announcements ON DELETE CASCADE`); all other cross-table refs (queue→user, log→queue, audiences→user) are soft per ADR-001/020. `msg_announcements.audience_ref TEXT` is a polymorphic target field interpreted by the AudienceFanOutWorker (Step 7) based on `audience_type` (CLASS = sis_classes.id as text, YEAR_GROUP = grade label, ROLE = iam role name, ALL_SCHOOL = NULL).** **Step 3 lands `packages/database/prisma/tenant/migrations/009_msg_moderation.sql` (6 base tenant tables — `msg_moderation_policies`, `msg_moderation_log`, `msg_tags`, `msg_user_tags`, `msg_user_blocks`, `msg_admin_access_log` — plus 24 monthly RANGE partitions of `msg_moderation_log` matching the 2025-08 → 2027-08 window) AND a Prisma migration `20260427211003_add_communications_platform_tables` that adds the first platform-schema tables since Cycle 0: `platform_push_tokens` (per-(user, device) push registration with `PushPlatform` enum IOS/ANDROID/WEB) and `platform_dlq_messages` (Kafka dead letter queue with topic/partition/kafka_offset/payload/headers/retry_count/error fields). The moderation policy table is three-tier (PLATFORM/DISTRICT/BUILDING) with each tenant carrying its own copy of the higher-tier policies that apply to it, so the moderation interceptor consults a single tenant table without cross-schema reads. The moderation log holds a soft denormalised `(message_id, message_created_at)` ref to the partitioned `msg_messages` (matches the `msg_message_attachments` precedent from Step 1) plus a DB-enforced `policy_id → msg_moderation_policies(id)` FK that PostgreSQL replicates onto each of the 24 monthly partitions (25 FK rows in `pg_constraint`, expected). `msg_user_tags.tag_id → msg_tags(id) ON DELETE CASCADE` is the second DB-enforced FK; `msg_user_blocks` carries a `blocker_id <> blocked_id` self-block CHECK; `msg_admin_access_log.reason` is NOT NULL because every admin read of a non-participant private thread must be FERPA-justified.** **Step 4 lands `packages/database/src/seed-messaging.ts` (idempotent, gated on `msg_thread_types` row count) — populates the demo tenant with 4 thread types (TEACHER_PARENT, CLASS_DISCUSSION, ADMIN_STAFF, SYSTEM_NOTIFICATION), 3 sample threads + 6 participants + 10 messages + 4 message reads (Rivera↔David Chen Maya progress check-in, P1 Algebra Q&A class discussion, Sarah↔Rivera staff thread), 3 alert types (GENERAL_ANNOUNCEMENT, PARENT_INFORMATIONAL, WEATHER_CLOSURE), 40 notification preferences (5 test users × 8 notification types — `attendance.{tardy,absent}`, `grade.published`, `progress_note.published`, `absence.{requested,reviewed}`, `message.posted`, `announcement.published` — with EMAIL channel enabled for `grade.published` + `attendance.tardy`, and 22:00–07:00 quiet hours on the parent), 2 moderation policies (PLATFORM-scope BLOCK profanity policy + BUILDING-scope FLAG_FOR_REVIEW policy for substance/bullying keywords), 2 announcements (ALL_SCHOOL "Welcome Back" + ROLE=PARENT "Parent-Teacher Conference Dates", both published) with 15 audience rows + 4 read marks, and 3 notification queue rows (1 SENT tardy notification for David, 1 SENT grade.published for Maya, 1 PENDING message-posted for David) plus 2 corresponding notification_log delivery rows. Adds `seed:messaging` to `packages/database/package.json`. The seed targets `tenant_demo` only (matching `seed-sis` / `seed-classroom` precedent — `tenant_test` stays empty for tests). `seed-iam.ts` was extended to add `COM-002:read` to the Student and Staff roles (previously held only by Teacher/Parent/School Admin/Platform Admin) — closes the plan's "Announcements read to all + write to Teacher/Admin" requirement; COM-001 read/write was already on Teacher/Parent/Student, COM-003 already on Platform/School Admin via `everyFunction`. Effective access cache rebuilt: Student now 15 perms (was 14), Staff now 6 (was 5).** **Step 5 lands the notification pipeline at `apps/api/src/notifications/`: 5 Kafka consumers (AttendanceNotificationConsumer for `att.student.marked_tardy` + `att.student.marked_absent`, GradeNotificationConsumer for `cls.grade.published`, ProgressNoteNotificationConsumer for `cls.progress_note.published`, AbsenceRequestNotificationConsumer for `att.absence.requested` + `att.absence.reviewed`, and the forward-compatible MessageNotificationConsumer for `msg.message.posted` whose producer ships in Step 6) + `NotificationQueueService` + `NotificationDeliveryWorker` + `RedisService` (first ioredis usage in the API). Each consumer follows the Cycle 2 GradebookSnapshotWorker pattern: read the ADR-057 envelope (with header fallback), `IdempotencyService.isClaimed` read-only check on arrival, resolve recipients inside `runWithTenantContextAsync`, enqueue via `NotificationQueueService`, then `claim()` after success (REVIEW-CYCLE2 BLOCKING 2 at-least-once). Recipient resolution: attendance/grade/progress-note consumers fan out to portal-enabled guardians via `sis_student_guardians` (`portal_access=true AND receives_reports=true AND g.account_id IS NOT NULL`); grade + progress-note also notify the student themself via the `sis_students → platform_students → platform_users` lookup; absence-request fans out to school admins (resolved via the `sch-001:admin` row in `iam_effective_access_cache` joined to `iam_scope`/`iam_scope_type`, school + platform scopes per ADR-036) on submission and to the original submitter (`sis_absence_requests.submitted_by`) on review; message-posted notifies every active non-sender thread participant + bumps the per-(user, thread) Redis HASH unread counter that Step 6's UnreadCountService reads. `NotificationQueueService.enqueue()` does Redis SET NX on `notif:idem:{subdomain}:{key}` (7-day TTL — authoritative dedup per ADR-001/020 since the DB table has no UNIQUE on `idempotency_key`), reads `msg_notification_preferences` for the (recipient, type) pair (default IN_APP if missing), checks 22:00–07:00-style quiet hours (wraps midnight; defers `scheduled_for` to the next quiet-end boundary), and INSERTs a PENDING row with the supplied JSON payload + `correlation_id`. `NotificationDeliveryWorker` polls every 10s (configurable via `NOTIFICATION_POLL_INTERVAL_MS`) — for every active row in `platform.schools` it enters tenant context, runs `SELECT … FOR UPDATE SKIP LOCKED LIMIT 25` on `msg_notification_queue` for `status='PENDING' AND scheduled_for <= now()`, flips rows to SENT in the same tx (atomic in-flight flag), then per-row delivers IN_APP via Redis ZADD on `notif:inapp:{accountId}` (capped at 100 entries via ZREMRANGEBYRANK) and stubs EMAIL/PUSH/SMS with a `[stub-deliver]` log line. Every delivery (real or stub) writes a `msg_notification_log` row with `status=DELIVERED` for IN_APP and `SENT` for the stubbed channels. Failure path: bumps `attempts`, flips back to PENDING with a 30-second `scheduled_for` backoff for up to MAX_ATTEMPTS=5 retries before settling on FAILED. Best-effort connection: RedisService and KafkaConsumerService both no-op gracefully when Redis or Kafka are unreachable in dev (matching the existing KafkaProducerService pattern). NotificationsModule wired into AppModule between ClassroomModule and the global guards. Build verified clean (`pnpm --filter @campusos/api build`); live boot confirmed all 5 consumer subscriptions + worker poll registration + Redis connect on 2026-04-27.** **Step 6 lands the messaging request-path module at `apps/api/src/messaging/`: `ThreadService` (create / list / read / archive — validates participants against `msg_thread_types.allowed_participant_roles` by mapping each account's IAM role names through `iam_role_assignment + iam_role + iam_scope` to the synthetic role tokens used by the seed; auto-adds the creator as OWNER; refuses pairs that have a `msg_user_blocks` row in either direction; writes a `msg_admin_access_log` row whenever a school admin reads a thread they aren't a participant in — FERPA audit), `MessageService` (post / edit / soft-delete / list — every post runs through `ContentModerationService.evaluate()` BEFORE insert; BLOCKED → HTTP 422 with the generic policy message + `msg_moderation_log` row + no `msg_messages` row; FLAGGED / ESCALATED → message persists with the matching `moderation_status` + `msg_moderation_log` row; CLEAN → status='CLEAN'; on success bumps `msg_threads.last_message_at`, auto-inserts a self-read receipt for the sender so they don't see a badge for their own message, increments the per-other-participant Redis HASH `inbox:{accountId}` synchronously, then emits `msg.message.posted` so the Step 5 MessageNotificationConsumer fans out IN*APP notifications + a second HASH bump (idempotent — same key)), `UnreadCountService` (Redis-backed: increment / clearThread / getBadgeCount / getByThread — never queries `msg_messages`), `ContentModerationService` (loads every active `msg_moderation_policies` row in one tenant-scoped query; whole-word case-insensitive keyword matching with regex escape — Scunthorpe-safe; "most restrictive wins" = BLOCKED > ESCALATED > FLAGGED > CLEAN; on a non-CLEAN verdict writes a `msg_moderation_log` row with `flag_type` + `severity` + `matched_keywords` + `review_outcome='PENDING'`; degrades gracefully to CLEAN on lookup failure so a transient platform DB blip can't shut messaging down). 10 endpoints under `com-001:read` / `com-001:write`: `GET /threads`, `GET /threads/:id`, `POST /threads` (with optional `initialMessage`), `POST /threads/:id/read` (idempotent — inserts `msg_message_reads` rows + bumps `last_read_at` + clears the per-thread Redis HASH entry), `PATCH /threads/:id/archive`, `GET /threads/:threadId/messages` (newest-first keyset pagination via `before` cursor), `POST /threads/:threadId/messages`, `PATCH /messages/:id` (author-only, 15-minute window — re-runs moderation), `DELETE /messages/:id` (sender or school admin; idempotent soft-delete; the `body` is blanked in `rowToDto` so the timeline reads "Message deleted" without breaking flow), `GET /notifications/unread-count` (top-bar badge — sums Redis HASH; `com-001:read OR com-002:read` so the bell renders for every persona that holds either communications read tier). `RedisService` was extended with `clearUnread`, `sumUnread`, and `listUnreadByThread`. `ThreadService.create()` validates that every account holds at least one of the thread type's `allowed_participant_roles` via the `iam_role_assignment + iam_role + iam_scope_type` join filtered to the school + platform scopes per ADR-036; the role-name token mapping is `name.toUpperCase().replace(/\s+/g, '*')`so`"School Admin"`→`"SCHOOL_ADMIN"`matches the seed shape. Build verified clean; live smoke against`tenant_demo`on 2026-04-27 confirmed: clean post returns 201 with`senderName="James Rivera"`(joined through`platform.platform_users → iam_person`); BLOCK keyword `shit`returns 422 with "This message was not sent because it contains content that violates school policy." and lands a`BLOCKED / URGENT / {shit}`row in`msg_moderation_log`; FLAG keyword `weed`returns 201 with`moderationStatus="FLAGGED"`plus a`FLAGGED / WARNING / {weed}`log row; multi-word BLOCK phrase`kill you`triggers 422 +`BLOCKED / URGENT / {"kill you"}`; parent's `GET /threads`shows`unread=6`for the TEACHER_PARENT thread;`POST /threads/:id/read`returns`marked=4`; `GET /notifications/unread-count`drops to`total=0` immediately after.** **Step 7 lands the announcements module at `apps/api/src/announcements/`: `AnnouncementService` (list / get / create / patch-draft / mark-read / stats — manager scope = School Admin OR `personType=STAFF`, reader scope = audience-row-required; published one-way once flipped from draft so the audience fan-out is stable), `AudienceFanOutWorker` (Kafka consumer on `msg.announcement.published` under group `audience-fan-out-worker`; resolves audience by `audience_type` and writes one row per recipient into `msg_announcement_audiences` with `delivery_status='DELIVERED'` ON CONFLICT DO NOTHING, then enqueues an `announcement.published` notification per recipient via the Step 5 `NotificationQueueService`), and a 6-endpoint `AnnouncementController` under `com-002:read` / `com-002:write`: `GET /announcements`, `GET /announcements/:id`, `POST /announcements` (draft or publish-now), `PATCH /announcements/:id` (edit a draft; flipping `isPublished=true` publishes + emits), `POST /announcements/:id/read` (idempotent — inserts `msg_announcement_reads` row, also flips the matching `msg_announcement_audiences` row PENDING→DELIVERED), `GET /announcements/:id/stats` (author-or-admin only — total audience, read count, read %, delivery breakdown). Audience resolution per `audience_type`: ALL*SCHOOL = every account with any active `iam_role_assignment` in the school+platform scope chain; CLASS = active enrollees + their portal-enabled guardians + assigned teachers via `sis_class_teachers` (with the Cycle 2 `teacher_employee_id = iam_person.id` mapping); YEAR_GROUP = students with `sis_students.grade_level = audience_ref` + their portal-enabled guardians; ROLE = accounts whose IAM role token (`UPPER(REGEXP_REPLACE(name, '\s+', '*', 'g'))`) equals `audience_ref`; CUSTOM is reserved for the deferred Communication Groups feature and logs+drops. The worker reuses the Step 5 `unwrapEnvelope`+`processWithIdempotency`helpers (claim-after-success, REVIEW-CYCLE2 BLOCKING 2). The publish path is one-way at the request layer: editing an already-published announcement returns 400, and PATCH cannot un-publish. Build verified clean; live smoke against`tenant_demo`on 2026-04-27 confirmed: parent sees only published + audience-row-matched announcements; student is denied 404 on a ROLE=PARENT announcement; non-author parent gets 403 on`/stats`; ALL_SCHOOL fan-out resolved to 5 recipients (the 5 seeded test users), CLASS fan-out to 10 recipients (students + their guardians + teachers), YEAR_GROUP/9 fan-out to 9 recipients, ROLE=PARENT fan-out to 1 recipient; `msg_announcement_audiences.delivery_status`lands as`DELIVERED`on insert and`msg_notification_queue`row is created per audience member; mark-read returns`newlyRead=true`then`false`(idempotent); audience+role-token CHECK +`audienceRef`validation rejects`ALL_SCHOOL`with`audienceRef`set and`ROLE`without`audienceRef`(both 400). The first kafka topic auto-creation race that affects gradebook-snapshot-worker on a fresh dev cluster also affects the new worker; pre-creating`dev.msg.announcement.published`once with`kafka-topics.sh --create` is the dev workaround.** **Step 11 lands the Cycle 3 CAT at `docs/cycle3-cat-script.md` — a 7-scenario reproducible end-to-end walkthrough verified live against `tenant_demo`: tardy mark → AttendanceNotificationConsumer → queue → NotificationDeliveryWorker → parent's bell (`unreadCount=15` with `attendance.tardy student=Maya Chen status=TARDY` at the top); grade publish → GradeNotificationConsumer → fan-out to student + portal-enabled guardians (parent gets `Quadratics Homework Set score=88`); direct messaging compose + reply + mark-read across personas; BLOCK keyword `shit` → 422 with the exact UI policy string + `BLOCKED / URGENT / {shit}` row in `msg_moderation_log`; ALL_SCHOOL announcement publish → AudienceFanOutWorker resolves 5 recipients → mark-read flips `readCount=1 readPercentage=20`; `is_enabled=false` pref disables enqueue (BEFORE=3 AFTER=3 unchanged); 3 permission denials (403 com-002:write, 403 method-tier on stats, 404 row-scope on non-participant thread). The script also documents and ships a fix for a latent Step 5 bug caught by this run — `GradeNotificationConsumer.loadContext`, `AttendanceNotificationConsumer.loadContext`, and `ProgressNoteNotificationConsumer.loadContext` all referenced a `c.title` column on `sis_classes` that doesn't exist (the actual class display name comes from `sis_courses.name` joined through `course_id`); all three SQL queries fixed to JOIN `sis_courses co ON co.id = c.course_id` and read `co.name || ' (' || c.section_code || ')'`. The bug never tripped before because Steps 5/6/7/8's smoke tests didn't exercise an end-to-end Kafka event through one of these three consumers — Step 5 verified consumer subscriptions, Steps 6/7 used different consumers, and Step 8's bell drove from PRE-EXISTING seeded queue rows. Documents David Chen's seeded 22:00–07:00 UTC quiet hours as the reason the CAT clears + restores them for Scenarios 1–5 (Scenario 6 covers preference honoring via `is_enabled=false` instead, which is the cleaner assertion).** **Step 10 lands the announcements UI: 3 routes under `/announcements` plus a hooks file. Backend was already complete in Step 7 (6 endpoints under `com-002:read` / `com-002:write`); Step 10 is purely additive on the web side. `apps/web/src/lib/types.ts` adds `AudienceType` (`'ALL_SCHOOL' | 'CLASS' | 'YEAR_GROUP' | 'ROLE' | 'CUSTOM'`), `AnnouncementDto`, `AnnouncementStatsDto`, `CreateAnnouncementPayload`, `UpdateAnnouncementPayload`, `MarkAnnouncementReadResponse`. `apps/web/src/hooks/use-announcements.ts` (new file) exposes 6 hooks: `useAnnouncements({includeDrafts, includeExpired})` (30s poll, refetch-on-focus), `useAnnouncement(id)` (one-shot), `useAnnouncementStats(id)` (30s poll, only enabled when `id` is non-null — the detail page only enables this for authors/admins so non-managers don't 403 the worker), `useCreateAnnouncement`, `useUpdateAnnouncement(id)`, `useMarkAnnouncementRead`. Sidebar gains an `Announcements` entry under `com-002:read` (every persona since Step 4 added the perm to Student/Staff) using the existing `MegaphoneIcon`. Three pages: `/announcements` (feed — newest-first list with severity-tinted icon bubble, audience pill, optional alert-type pill, "Draft" / "Expired" pills, line-clamp-2 body preview, unread-row tinting; 4 filter chips: All / Unread, plus Drafts / Expired for users with `com-002:write` only — Drafts/Expired chips drive `includeDrafts=true` / `includeExpired=true` server-side and a client-side filter to narrow the result); `/announcements/new` (compose — title, body, audience-type pill picker for ALL_SCHOOL/CLASS/YEAR_GROUP/ROLE; conditional `audienceRef` picker per type — CLASS uses `useClasses` and shows `sectionCode — course.name` options sorted by course; YEAR_GROUP hard-codes 9–12 (the seed uses 9 and 10; 11–12 are exposed for forward compatibility — a dedicated `/grades` distinct-list endpoint can ship in a later cycle if schools pin different conventions); ROLE hard-codes the four canonical tokens TEACHER / STUDENT / PARENT / SCHOOL_ADMIN; optional `expiresAt` `datetime-local` input; two submit buttons "Save draft" → POST with `isPublished=false` and "Publish now" → POST with `isPublished=true`. Uses the existing Toast for success/error feedback. Permission-gated on `com-002:write` — non-managers see an EmptyState explaining they can't publish); `/announcements/[id]` (detail — header has back link, severity-tinted icon, title, audience pill + alert-type pill + Draft/Expired pills, author + relative publish-or-create timestamp + relative expiry. Body rendered in `whitespace-pre-wrap`. Auto-marks-read on first mount when published-and-unread, tracked via `lastMarkedReadFor` ref so the cache invalidation poll doesn't re-fire it. Drafts get a "Publish now" header button for managers — PATCHes `isPublished=true` via `useUpdateAnnouncement`. Stats panel below the article renders for authors and school admins only — total-audience / read-count + % / delivered / pending+failed metrics plus a campus-600 progress bar. The stats query is gated by passing `null` to `useAnnouncementStats` when the caller can't see them — the hook's `enabled` flag short-circuits and never fires the 403). Build verified clean (`pnpm --filter @campusos/web build` — `/announcements` 2.83 kB, `/announcements/[id]` 3.59 kB, `/announcements/new` 5.96 kB First Load JS); live smoke against `tenant_demo` on 2026-04-27 confirmed all 13 assertions: parent sees 5 audience-targeted announcements on the feed, teacher creates a CLASS draft, draft is hidden from parent on the default feed, teacher PATCHes `isPublished=true` and the AudienceFanOutWorker fans out to 10 recipients (CLASS audience = students + guardians + teachers), parent now sees the published row, mark-read returns `newlyRead=true` then `newlyRead=false` (idempotent), stats panel reflects the read (`readCount=1 readPercentage=10`), student blocked from POST `/announcements` (403 com-002:write), parent blocked from non-author stats (403 com-002:write).** **Step 9 lands the messaging UI: 3 routes under `/messages` (inbox, compose, thread view) plus 2 small additive backend endpoints. Backend: `ThreadResponseDto` gains optional `lastMessagePreview` (LEFT(body, 80) on the most recent non-deleted message via `LEFT JOIN LATERAL`) and `lastSenderName` — populated by `GET /threads` only, null on `getById`. Two new endpoints land on `ThreadController`: `GET /threads/types` (active thread types in this tenant — non-admins are filtered to non-system rows) returns `{id, name, description, allowedRoles, isSystem}` for the compose UI's thread-type chips; `GET /threads/recipients?threadTypeId=:id` (`com-001:write`) returns `{platformUserId, displayName, email, roles[]}` for every active platform user in the school whose IAM role token (UPPER + spaces→underscores) matches the thread type's `allowed_participant_roles`, excluding self and excluding any user with a `msg_user_blocks` row in either direction. Both reuse the same `iam_role_assignment + roles + iam_scope + iam_scope_type` join shape as the existing `loadRoleTokens` (school + platform scope chain per ADR-036). **Latent bug found and fixed during smoke**: both `loadRoleTokens` and the new `listRecipients` referenced `platform.iam_role` (doesn't exist — actual table is `platform.roles`); both queries now point at the right relation. The dormant bug never tripped before because Step 6 only invoked `loadRoleTokens` from `ThreadService.create()`, and the seeded threads were created via the seed script (which writes directly), not via the API. Web side: `apps/web/src/lib/types.ts` adds `ThreadDto`, `ThreadParticipantDto`, `ThreadTypeDto`, `MessagingRecipientDto`, `MessageDto`, `CreateThreadPayload`, `PostMessagePayload`, `MarkThreadReadResponse`. `apps/web/src/hooks/use-messaging.ts` exposes `useThreads` (15s poll), `useThread` (30s poll), `useThreadMessages` (15s poll), `useThreadTypes`, `useMessagingRecipients`, `useCreateThread`, `usePostMessage`, `useMarkThreadRead`, `useArchiveThread`. Sidebar gains a `Messages` entry under any user with `com-001:read`, using the existing `ChatBubbleIcon`. Three pages: `/messages` (inbox — avatar of first other-participant, headline of all other participants, subject (or thread-type label fallback), the LATERAL-derived preview prefixed with the last sender's name, unread badge capped at "99+", `formatRelative` timestamp; `Show archived` checkbox toggles `?includeArchived=true`); `/messages/new` (thread-type selector chips for non-system types; recipient picker is a search-filterable multi-select checklist driven by `useMessagingRecipients(threadTypeId)`; subject is optional; submit POSTs `/threads` with `initialMessage`; on `ApiError.status === 422` the UI surfaces "This message was not sent because it contains content that violates school policy." — generic, no keyword detail per the plan); `/messages/[threadId]` (thread header with subject + threadtype label + archive toggle that routes back to inbox when archiving; message list rendered oldest-first with bubble styling — own messages right-aligned + campus-600 background, others left-aligned + gray-100; soft-deleted rows render as italic "Message deleted"; FLAGGED/ESCALATED rows show a muted "Flagged for review" hint to the participant; reply textarea below the list when the user is an active participant + `com-001:write` + thread is not archived; auto-marks the thread as read on first mount when `unreadCount > 0` (refs-tracked so polls don't re-fire); auto-scrolls to the latest message on every poll; admin viewing a non-participant thread gets read-only with a "Read-only — admins can review this thread but cannot post into it." footer). Build verified clean (`pnpm --filter @campusos/api build` + `pnpm --filter @campusos/web build` — `/messages` 2.85 kB, `/messages/[threadId]` 4.21 kB, `/messages/new` 6.32 kB First Load JS); live smoke against `tenant_demo` on 2026-04-27 confirmed all 10 assertions: teacher inbox shows 3 seeded threads with truncated previews + sender names; parent inbox shows the 1 thread they participate in + 3 thread types with allowed-roles loaded; recipients endpoint correctly filters TEACHER_PARENT to (Teacher Rivera, Admin Mitchell) for the parent (no other parents listed); parent creates new thread `019dd186-eb3c-7666-80d3-75f432e0bc10` with initialMessage; teacher's inbox immediately reflects it with `preview='Hello, this is a smoke test' sender='David Chen' unread=1`; clean reply returns 201; BLOCK keyword `shit` returns 422 with the exact policy string the UI surfaces; mark-read returns `marked=2 unreadCount=0`; student creating a TEACHER_PARENT thread is rejected 400 with the role-validation message (STUDENT not in [TEACHER, PARENT, SCHOOL_ADMIN]); archive toggle flips `isArchived=true` cleanly.** **Step 8 lands the NotificationBell + dropdown + `/notifications` page: extends `RedisService` with `listInAppNotifications` (ZREVRANGE on `notif:inapp:{accountId}`), `countInAppSince` (ZCOUNT exclusive of lastReadAt), and `getNotificationLastReadAt` / `setNotificationLastReadAt` (`notif:lastread:{accountId}` epoch-ms key, no TTL — bounded by active-user count); adds `NotificationInboxService` (read side) + `NotificationInboxController` with three endpoints under `com-001:read OR com-002:read`: `GET /notifications/inbox?limit=10` (returns `{ unreadCount, items, lastReadAt }` for the bell — items come from the Redis sorted set populated by `NotificationDeliveryWorker`, score=epoch ms, items have `isRead=score<=lastReadAt`), `GET /notifications/history?limit=25&type=…&before=…` (paginated history straight from `msg_notification_queue`, keyset cursor on `COALESCE(sent_at, created_at)`, server-side type filter is exact-match on `notification_type`), and `POST /notifications/mark-all-read` (bumps `notif:lastread:{accountId}` to `Date.now()` — idempotent, single-call clear; per-item dismissal not in scope this phase). The legacy `GET /notifications/unread-count` (Step 6) stays put — it returns the per-thread messaging unread map for the messaging UI's inbox-row badges; the bell uses `/inbox` for the unified count instead. Web side: `useNotificationInbox` polls every 30s + refetches on focus; `useNotificationHistory` is a one-shot read; `useMarkAllNotificationsRead` invalidates `['notifications']`. `NotificationBell` mounts in `TopBar` for any user with `com-001:read OR com-002:read` (every seeded persona qualifies), renders the badge (capped at "99+") and a 22rem dropdown of the last 10 items; each item runs through `describeNotification(item)` (per-type title + subtitle from the consumer-shaped payload — message_posted reads `sender_name` + `thread_subject` + `preview`, grade.published reads `assignment_title` + `student_name` + `percentage`, attendance.tardy reads `student_name` + `class_name` + `period`, etc.) and `resolveDeepLink(item, user)` (persona-aware for grade.published — STUDENT → `deep_link_student`, GUARDIAN → `deep_link_guardian`; falls back to `payload.deep_link` for the other types, with a per-type fallback for legacy-shaped payloads that lack the field — e.g. message.posted falls back to `/messages/{thread_id}`). Type-specific icons (`AttendanceIcon`, `GradeIcon`, `ChatBubbleIcon`, `MegaphoneIcon`, `CheckCircleIcon`, `BellIcon`) and tone-mapped pill colors are added to `apps/web/src/components/shell/icons.tsx`. The `/notifications` page lives at `apps/web/src/app/(app)/notifications/page.tsx` — full paginated list with 6 client-side filter chips (All / Attendance + Absence / Grades / Progress notes / Messages / Announcements — grouped chips that don't map 1:1 to a single notification_type filter on the backend), `Mark all read` action in the page header, unread items highlighted with a blue dot + tinted row background. Build verified clean (`pnpm --filter @campusos/api build` + `pnpm --filter @campusos/web build` — `/notifications` ships as a static route at 1.98 kB First Load JS); live smoke against `tenant_demo` on 2026-04-27 confirmed parent (David Chen) sees 1 unread on first load (the seeded PENDING `message.posted` queue row, delivered to Redis by the worker), `mark-all-read` returns `{ lastReadAt: 1777331319341 }` and the next `/inbox` poll returns `unreadCount=0` with `isRead=true`; type filter `?type=message.posted` returns 4 items all of that type; type filter `?type=announcement.published` returns 4; cursor pagination with `before=...` returns subsequent pages with newer-first ordering preserved.** Tenant base table count: 57 (was 51). Provisioned cleanly + idempotently to demo + test on 2026-04-27. CHECK constraints (`role`, `moderation_status`, `severity`, queue `status`, log `channel`, log `status`, `audience_type`, audience `delivery_status`, mod-policy `scope`, mod-policy `keyword_action`, sensitivity_threshold `0–100`, mod-log `flag_type`, mod-log `severity`, mod-log `review_outcome`, user-block self-block) verified live; 0 cross-schema FKs. `apps/api/src/kafka/event-envelope.ts` defines the `EventEnvelope` interface (`event_id`, `event_type`, `event_version`, `occurred_at`, `published_at`, `tenant_id`, `source_module`, `correlation_id`, `payload`) plus the `prefixedTopic(name)` helper that applies the `KAFKA_TOPIC_ENV` prefix (default `dev`) to every topic on the wire. `KafkaProducerService.emit()` was rewritten to take an `EmitOptions` object — wraps the domain payload in the envelope, env-prefixes the topic, and continues to set the three legacy transport headers (`event-id`, `tenant-id`, `tenant-subdomain`) plus a new `event-type` header for backward compatibility. All 12 existing emit sites migrated (`grade.service.ts` × 2, `submission.service.ts` × 1, `progress-note.service.ts` × 1, `attendance.service.ts` × 6, `absence-request.service.ts` × 2). `GradebookSnapshotWorker.handle()` now reads `event_id` / `tenant_id` from the envelope first, falls back to transport headers for any in-flight legacy messages; subscribes to `prefixedTopic('cls.grade.published')` / `…unpublished`. Every Cycle 3 consumer that lands in Steps 5+ will read directly from the envelope. See `HANDOFF-CYCLE3.md` for the Step 0 verification record and Steps 1–11 plan.

**Cycle 2 (Classroom + Assignments + Grading) is COMPLETE — all 10 steps done. Post-cycle architecture review APPROVED at commit `d876e86` (REVIEW-CYCLE2-CHATGPT — final verdict).** The post-cycle review tightened `GET /classes/:classId/gradebook` to manager-only (permission gate switched from `tch-003:read` → `tch-003:write` and row-scope from `assertCanReadClass` → `assertCanWriteClass`, closing a latent grade-data leak to students/parents who held `tch-003:read`); rewrote the GradebookSnapshotWorker idempotency from at-most-once to at-least-once (read-only `IdempotencyService.isClaimed` on arrival, debounce-entry tracks contributing event-ids, `claim()` only fires AFTER a successful recompute, transient DB failures retry on the next event); and documented the temporary HR-employee identity mapping that columns named `*_employee_id` / `cls_grades.teacher_id` / `cls_lessons.teacher_id` / `cls_student_progress_notes.author_id` currently hold `iam_person.id` directly until the HR module ships — annotated in the migration SQL via `COMMENT ON COLUMN`. The ADR-057 event envelope is deferred to Cycle 3 (Communications) where multiple producers and consumers will arrive together; the grade-emit headers (`event-id`, `tenant-id`, `tenant-subdomain`) already carry the three fields the worker reads, so the migration is additive. Migrations `005_cls_lessons_and_assignments.sql` (7 tables) and `006_cls_submissions_and_grading.sql` (8 tables) add the full M21 Classroom schema — 15 tenant tables total. ADR-010 (gradebook snapshots are async-only) is enforced at the schema layout level: `cls_grades` and `cls_gradebook_snapshots` are physically separate tables with no FK or trigger linking them; the Step 5 grade write path was end-to-end-verified to never touch the snapshot table. AI / human boundary: `cls_ai_grading_jobs` and `cls_submission_question_grades.ai_*` columns store AI suggestions; `cls_grades` has zero `ai_*` columns and is written only by teacher action. 28 intra-tenant FKs across the two migrations; zero cross-schema FKs. Step 3 (`seed-classroom.ts`) seeds 1 grading scale, 5 assignment types, 18 per-class categories (Homework 30 / Assessments 50 / Participation 20), 12 assignments, 80 submissions, 62 grades (53 published), 41 gradebook snapshots, and 1 progress note for Maya — and adds TCH-002:read (Parent), TCH-004:read (Student), TCH-004:read+write (Teacher) to the role-permission map. Step 4 lands AssignmentService + CategoryService + their controllers — 7 endpoints under `tch-002:read/write` with the same row-level auth pattern as Cycle 1 (admin → all; teacher → assigned classes via `sis_class_teachers`; student → enrolled classes; parent → linked-child classes). Step 5 lands SubmissionService + GradeService + GradebookService + ProgressNoteService + 4 controllers — 12 new endpoints (3 `tch-002:*` + 9 `tch-003:*`). Submissions are idempotent upserts by `(assignment, student)`; resubmitting flips status back to `SUBMITTED`. Grade writes (single + batch + publish + unpublish + publish-all) emit `cls.grade.published` / `cls.grade.unpublished` via `KafkaProducerService` and never write to `cls_gradebook_snapshots`. Draft grades are filtered out of student / parent payloads (`rowToDto(row, includeDraftGrade)` flag — manager teacher / admin sees them; everyone else does not). Gradebook reads (`/classes/:id/gradebook`, `/students/:id/gradebook`) join roster ↔ snapshot per resolved term (default = current term). Progress notes upsert by `(class, student, term)` with persona-scoped reads (admins all; teachers their classes; students/parents only `is_*_visible=true` AND `published_at IS NOT NULL`). **Step 6 lands the first Kafka consumer in the system — `GradebookSnapshotWorker` (in `apps/api/src/classroom/`) — backed by `KafkaConsumerService` and `IdempotencyService` (in `apps/api/src/kafka/`). The worker subscribes to `cls.grade.published` and `cls.grade.unpublished` under consumer group `gradebook-snapshot-worker`, debounces 30 seconds per `(schoolId, classId, studentId)`, and recomputes `cls_gradebook_snapshots` using the exact weighted-average algorithm from `seed-classroom.ts` (per-category mean, weighted by category, renormalised over participating categories only). Idempotency is via `platform.platform_event_consumer_idempotency` keyed on `(consumer_group, event_id)`; redelivered events fail the `INSERT` (SQLSTATE 23505) and are dropped before they can reset the debounce. Tenant context is reconstructed at flush time from Kafka headers (`event-id`, `tenant-id`, `tenant-subdomain`) using `runWithTenantContextAsync`, so the worker reuses the same `TenantPrismaService` helpers as the request path. The grade emit (`grade.service.ts::tenantHeaders`) injects these three headers from `getCurrentTenant()` plus a fresh UUIDv7 — forward-compatible with the ADR-057 envelope that lands in Cycle 3.** **Step 7 lands the teacher Assignments UI: a `ClassTabs` shell at `/classes/:id/{attendance,assignments}` (Gradebook tab hidden until Step 8), an assignments list page with type filter / draft visibility for managers / soft-delete confirm, shared `AssignmentForm` for create + edit, and `CategoryWeightModal` that mirrors the API's PUT-by-name semantics with a live "weights must sum to 100" check. New `GET /assignment-types` endpoint exposes the school's type catalogue. Pre-existing lint / Suspense issues in the Cycle 1 dashboards + `/login` were fixed so the web app builds clean — these were transitive (only surfaced once Step 7 needed `next build` to succeed).** **Step 8 lands the teacher Grading UI: a spreadsheet-style gradebook grid at `/classes/:id/gradebook` with color-coded cells × inline editor × per-cell publish toggle × per-assignment "Publish all", a submissions queue at `/assignments/:id/submissions` sorted by grading priority (SUBMITTED first), a single-submission detail page at `/submissions/:id` with grade entry + feedback + publish controls, and a per-student `ProgressNoteModal` launched from the gradebook (auto-loads existing note for the (class, term) pair, upserts on save). The Gradebook tab is now visible across all `/classes/:id/*` routes. **Side fix: `grade.service.ts` now emits `cls.grade.published` on updates to already-published grades** (Step 5 emit gap — only draft↔published transitions were emitting before, so a re-grade of a published row left the snapshot stale). Both `upsertGrade` (single) and `batchGrade` paths now flag rows for emit if they end up published, regardless of prior state. The snapshot worker is idempotent so the redundant emit is harmless.** **Step 9 lands the student & parent grade views: a new student `StudentDashboard` (upcoming-assignment list + per-class average cards), a student `/assignments` inbox across all enrolled classes (filter by class / overdue / next-14-days), `/assignments/:id` student detail + submit form (idempotent resubmit, published-grade reveal), `/grades` per-class average list, `/grades/:classId` per-assignment breakdown, and the parent equivalents — a Grades section on each child card on `ParentDashboard`, `/children/:id/grades` per-child average list, and `/children/:id/grades/:classId` per-class breakdown (incl. parent-visible progress notes). Two new backend endpoints back this: `GET /students/me` (bootstrap the calling student's `studentId` without scanning the list) and `GET /students/:studentId/classes/:classId/grades` (per-(student, class) breakdown — assignments + own submission + own grade, only published rows for non-managers). Sidebar surfaces `Assignments` and `Grades` for STUDENT persona.** **Step 10 lands the Cycle 2 CAT at `docs/cycle2-cat-script.md` — a 9-step reproducible end-to-end walkthrough verified live against `tenant_demo`: teacher creates an assignment → student bootstraps via `/students/me` and submits → teacher grades draft → draft is hidden from the student → teacher publish-all emits `cls.grade.published` → 30-second debounce + idempotent recompute updates `cls_gradebook_snapshots` (worker log captured) → student sees grade via the new per-class endpoint → parent sees the same data through `sis_student_guardians` → 3 permission denials (student grading, parent submitting, teacher writing to a class they don't teach) plus a draft-after-unpublish bonus assertion. All assertions pass; demo state restored at end of run.**

See `docs/campusos-cycle1-implementation-plan.html`, `docs/campusos-cycle2-implementation-plan.html`, and `docs/campusos-cycle3-implementation-plan.html` for step-by-step plans; `HANDOFF-CYCLE1.md`, `HANDOFF-CYCLE2.md`, and `HANDOFF-CYCLE3.md` for current build state and known gaps; `REVIEW-CYCLE1-CHATGPT.md` / `REVIEW-CYCLE1-CLAUDE.md` / `REVIEW-CYCLE1-FIXES.md` for the Cycle 1 architecture review record; `REVIEW-CYCLE2-CHATGPT.md` for the Cycle 2 review + fixes-applied log; `REVIEW-CYCLE3-CHATGPT.md` for the Cycle 3 review (Round 1 REJECT → Round 2 APPROVED with the fix log inline).

## Delivery Plan (Revised)

**Phase 1: Build the Core — COMPLETE**

- Cycle 0: Platform Foundation — COMPLETE
- Cycle 1: SIS Core + Attendance — COMPLETE (reviewed, fixes applied)
- Cycle 2: Classroom + Assignments + Grading — COMPLETE
- Cycle 3: Communications — COMPLETE + APPROVED at `592d366` (post-cycle review; see `REVIEW-CYCLE3-CHATGPT.md`). Vertical-slice CAT lives at `docs/cycle3-cat-script.md`; the closeout commit also ships 3 latent consumer-SQL fixes (`c.title` → `co.name`) and the 3 review fixes (Kafka consumer retry/DLQ, delivery worker PROCESSING state, announcement `isManager()` scope).

**Phase 2: Test and Refine (current — pending post-cycle review)**

- Walk through every workflow as each persona (teacher, parent, student, admin)
- Refine UI design, navigation, and interaction patterns
- Create UI design guide (`docs/ui-design-guide.md`) for future cycles
- Test edge cases and identify missing features within existing modules
- Fix issues and polish before expanding

**Phase 3: Expand (current — Cycle 4 APPROVED at `76ddf03`; Cycle 5 in progress, Steps 1–4 done)**

- Cycle 4: HR & Workforce Core — **COMPLETE + APPROVED at `76ddf03`.** All 10 steps done; post-cycle architecture review APPROVED on Round 2 (Round 1 at `efbcb44` REJECT'd on 1 BLOCKING + 3 MAJOR; all four fixes in `bda8a16`; CI fix in `76ddf03`). Total tenant base tables: 74 (was 57 after Cycle 3, +17 hr\_\* tables). 23 HR endpoints, 5 Kafka emits + 1 republish (`hr.leave.coverage_needed`), 7 HR web routes, 3 launchpad tiles. Vertical-slice CAT at `docs/cycle4-cat-script.md` verified live on 2026-04-28 — all 16 checks (4 bridge + 12 plan scenarios) pass. Cycle 2 DEVIATION 4 retired by Step 0. See `HANDOFF-CYCLE4.md` and `REVIEW-CYCLE4-CHATGPT.md`.
- Cycle 5: Scheduling & Calendar — **COMPLETE + APPROVED at `1650497`.** All 10 steps done; post-cycle architecture review APPROVED on Round 2 (Round 1 at `e214bdc` REJECT'd on 2 BLOCKING + 2 actionable MAJOR; all four fixes in `653fd4c`; verdict-trail backfill in `c0402c9`; prettier follow-up in `1650497`). 10 scheduling base tables: bell schedules + periods + rooms in Step 1; timetable slots with btree_gist EXCLUSION constraints + room bookings + room change requests in Step 2; calendar events + day overrides + coverage requests + substitution timetable in Step 3; tenant base table count 84, was 74 after Cycle 4. `seed:scheduling` in Step 4 — 2 bell schedules + 19 periods + 10 rooms + 6 Rivera timetable slots + 5 calendar events + 1 day override + 1 ASSIGNED coverage row + 1 substitution row + 1 room booking; SCH-001/004/005 grants extended to Teacher and Staff; effective access cache rebuilt to 34/14 perms for Teacher/Staff. Step 5 — first scheduling NestJS module: BellScheduleService + TimetableService + RoomService + RoomBookingService + RoomChangeRequestService; 22 endpoints; EXCLUSION 23P01 → 409 Conflict translation with conflicting actor name; emits `sch.timetable.updated`; build clean, all routes mapped on boot, live smoke against `tenant_demo` covered all 10 scenarios. Step 6 — calendar / coverage NestJS side: CalendarService + DayOverrideService + CoverageService + SubstitutionService; 16 endpoints; CoverageConsumer subscribes to `hr.leave.coverage_needed` under group `coverage-consumer` and creates one `sch_coverage_requests` row per (active slot, weekday-in-range) tuple; UNIQUE(timetable_slot_id, coverage_date) is the schema-side dedup, deterministic event_id from REVIEW-CYCLE4 MAJOR 3 is the primary gate via `processWithIdempotency`; emits `sch.coverage.needed` (after consumer creates rows) and `sch.coverage.assigned` (after admin assigns); end-to-end live test: Rivera submits leave → admin approves → 6 OPEN coverage rows created in 1 second → admin assigns Park to Period 1 Algebra → matching `sch_substitution_timetable` row materialises → admin cancels → substitution row dropped. Step 7 — first scheduling web surface: `Schedule` launchpad tile gated on `sch-001:read`, 4 routes (5 pages — `/schedule/bell-schedules` + `[id]` period editor, `/schedule/timetable` week-view grid, `/schedule/rooms`, `/schedule/room-bookings`), 15 React Query hooks in `use-scheduling.ts`, `scheduling-format.ts` helpers, full Cycle 5 DTO types in `apps/web/src/lib/types.ts`; build clean, live smoke against `tenant_demo` covered all 8 read-path scenarios. Step 8 — calendar / coverage web surface: second `Calendar` launchpad tile gated on `sch-003:read`, 3 routes (`/calendar` month-view grid with event-type colour coding + draft toggle + draft-or-publish create flow, `/schedule/coverage` day-of board with Open/Assigned/Cancelled sections + admin Assign/Cancel modals, `/schedule/coverage/history` filterable reporting), 15 more React Query hooks (30 total scheduling hooks), `scheduling-format.ts` extended with calendar-event-chip / coverage-status pill helpers + ISO date utilities; build clean, all 8 smoke scenarios pass. Step 9 — non-admin schedule views: new `GET /timetable/student/:studentId` row-scoped backend endpoint (39 total scheduling endpoints; SchedulingModule now imports SisModule); `useTimetableForStudent` hook; reusable `TimetableWeekView` + `RoomAvailabilityChecker` components; `/my-schedule` shared route (teacher branch with substitution-coverage highlight + upcoming-coverage list, student branch via the new endpoint); `/children/[id]/schedule` parent route; navigation entry points on `/classes` (My Schedule button) and `/children` ChildCard (Schedule link); Sidebar `routePrefix` cleanup so any `/schedule/*` route highlights the Schedule tile. Step 10 — vertical-slice CAT at `docs/cycle5-cat-script.md` (10 plan scenarios + 4-check schema preamble); end-to-end leave→coverage→substitute→Kafka envelope flow verified live on `tenant_demo` 2026-04-28; cleanup restores tenant to seed state. The `hr.leave.coverage_needed` topic Cycle 4 publishes is the first event Cycle 5's CoverageConsumer will subscribe to (Step 6); the deterministic event_id pattern from REVIEW-CYCLE4 MAJOR 3 makes the consumer's idempotency catch redeliveries cleanly, and the new `sch_coverage_requests` UNIQUE(timetable_slot_id, coverage_date) is the schema-side dedup. See `HANDOFF-CYCLE5.md`.
- Cycle 6: Enrollment & Payments — **COMPLETE + APPROVED at `64993a8` (REVIEW-CYCLE6-CHATGPT — final verdict).** Round 1 against `f7db6a5` returned REJECT pending 8 actionable fixes (3 reviewer claims wrong, 5 accepted DEVIATIONs, 8 valid bugs); Round 2 against `64993a8` returned APPROVED after all 8 fixes landed (attendance read row-scope + manager-only writes; gradebook STAFF view filters classes to assigned set; invoice cancel writes compensating ADJUSTMENT entry; refund-aware invoice paid formula + invoice-first lock order in RefundService + invoice status reconcile; generate-from-schedule advisory lock + in-tx existence check; capacity recompute advisory lock per (period, grade); offer.issued reads guardian_person_id from locked application snapshot; account number allocation advisory lock per school; tenant resolver JSDoc rewrite). 6 fixes verified live end-to-end on `tenant_demo`, 2 by build-artifact + happy-path, 1 doc-only. See `REVIEW-CYCLE6-CHATGPT.md` for the triage table + verification trail. Steps 1–12 done — Step 1 `018_enr_enrollment_and_applications.sql` (8 base tables: enr*enrollment_periods, enr_admission_streams, enr_intake_capacities, enr_capacity_summary, enr_applications with multi-column submitted_chk + reviewed_chk, enr_application_screening_responses with JSONB response_value, enr_application_documents, enr_application_notes); Step 2 `019_enr_offers_and_waitlist.sql` (enr_offers with multi-column conditions_chk using `cardinality()` not `array_length(*, 1)`after smoke caught the empty-array slip-through, response_pair_chk, deferred_chk; enr_waitlist_entries with offered_chk + priority/position bounds CHECKs); Step 3`020*pay_family_accounts_and_fees.sql`(5 base tables: pay_family_accounts with UNIQUE(school, holder) + UNIQUE(school, account_number) + 3-state status + 2-state payment_authorisation_policy, pay_family_account_students link table CASCADE on both family_account and sis_students, pay_fee_categories with is_active flag, pay_fee_schedules with no-cascade FKs to sis_academic_years + pay_fee_categories + amount NUMERIC(10,2) >= 0 CHECK + 5-value recurrence CHECK, pay_stripe_accounts stubbed with UNIQUE on both school_id and stripe_account_id); **Step 4`021_pay_invoices_payments_ledger.sql`** (7 base tables: pay_invoices with multi-column sent_chk + 6-state lifecycle, pay_invoice_line_items CASCADE on invoice + nullable no-cascade fee_schedule attribution, pay_payments with 5-method enum + 4-state status + multi-column paid_chk + no-cascade FKs preserving audit, **pay_ledger_entries IMMUTABLE RANGE-partitioned by created_at annually 2025–2030 with composite PK (id, created_at) and DB-enforced FK family_account_id replicating onto each of the 6 partitions**, pay_refunds with 6-category enum + 3-state status + completed_chk + NOT NULL authorised_by/authorised_at, pay_payment_plans with UNIQUE(invoice_id) + 2-frequency + 4-state status, pay_payment_plan_installments CASCADE on plan + nullable no-cascade payment_id + multi-column paid_chk). Tenant base table count after Step 4: **106\*\* (was 99 after Step 3; +7 logical pay*\_ tables — the 6 ledger partitions are not counted as logical base tables). 30 conceptual intra-tenant FKs across `enr\__`+`pay*\*`(10 + 4 + 4 + 12), **36 rows in pg_constraint** because the partitioned ledger FK lives once on the parent and once on each of the 6 partitions. 0 cross-schema FKs. All four schema steps' smoke verified live in single BEGIN…ROLLBACK transactions with savepoints. **Step 7 lands`apps/api/src/payments/`** — keystone Cycle 6 step. 7 services (LedgerService / FeeScheduleService / FamilyAccountService / InvoiceService / PaymentService / RefundService / PaymentPlanService) + 6 controllers + **24 endpoints** + 3 Kafka emit topics (pay.invoice.created / pay.payment.received / pay.refund.issued) + the **`PaymentAccountWorker`** Kafka consumer subscribed to `dev.enr.student.enrolled`under group`payment-account-worker`— closes the enrollment→payments event loop. Stripe stubbed (CARD payments auto-COMPLETE with`pi_dev*<uuid>`; refunds with `re*dev*<uuid>`). Locked-read concurrency: invoice send + payment apply + refund issue + payment-plan create all run inside `executeInTenantTransaction`with`SELECT … FOR UPDATE` on the relevant row. RedisService extended with three ledger-balance helpers (`getLedgerBalance`/`setLedgerBalance`/`invalidateLedgerBalance`keyed`ledger:balance:{accountId}`TTL=30s). seed-iam.ts updated mid-smoke: Parent role gains FIN-001:write (parent now 15 perms). **Step 6 lands`apps/api/src/enrollment/`** — 5 services (EnrollmentPeriodService, ApplicationService, OfferService, WaitlistService, CapacitySummaryService) + 4 controllers + **17 endpoints** + 5 Kafka emit topics including the load-bearing `enr.student.enrolled`(the future Step 7 PaymentAccountWorker subscription). Row-scope: parents see only their own applications + offers (matched on`enr*applications.guardian_person_id = actor.personId`); admins see all; teachers and students get 403 at the permission gate. State-machine concurrency: `OfferService.respond`locks both`enr_offers`AND`enr_applications`FOR UPDATE in the same tx that flips status to ACCEPTED — the Cycle 5 review carry-over from CLAUDE.md Conventions.`enr.student.enrolled`envelope verified live on`dev.enr.student.enrolled`. EnrollmentModule wired between SchedulingModule and NotificationsModule. **Step 5 lands `seed-enrollment.ts`+`seed-payments.ts`** — 27 enrollment rows across 9 tables (1 enrollment period + 2 streams + 2 capacities + 2 capacity_summary + 4 applications + 10 screening + 3 notes + 2 offers + 1 waitlist) + 19 payments rows across 9 tables (4 categories + 4 schedules + 1 stripe acct + 1 family acct + 1 link + 2 invoices + 2 line items + 1 payment + 3 ledger entries with balance $400.00). The new `2026-2027` `sis_academic_years`row is created by the enrollment seed (is_current=false).`seed-iam.ts`extended Parent role with STU-003:read+write + FIN-001:read; cache rebuild reports parent 14 / vp+counsellor 14 / student 15 / teacher 34 / admin+principal 444. **Step 9 lands the Parent Application & Enrollment UI** at`apps/web/src/app/(app)/apply/`+`apps/web/src/app/(app)/offers/[id]/` — 3 routes (`/apply`parent landing combining tracker + Start-new entry,`/apply/new`single-page form,`/offers/[id]`Accept/Decline/Defer with inline Welcome confirmation banner on Accept). New parent`Apply`tile gated on`personType=GUARDIAN`AND`stu-003:write`. **No new API endpoints** — sits entirely on Step 6's enrollment surface. Web build clean. Live smoke against `tenant_demo`(5 reads + 1 end-to-end submit + 1 row-scope 404 + cleanup) all pass with the guardian_person_id stamping working as designed. **Step 8 lands the Enrollment Admin UI** at`apps/web/src/app/(app)/admissions/` — 5 routes (`/admissions/applications`pipeline default landing,`/admissions/applications/:id`review with offer panel + status transitions + notes timeline,`/admissions/periods`list with capacity rollup + Open/Close transitions,`/admissions/periods/:id`detail with per-grade capacity dashboard + admission streams + add-stream/add-capacity modals,`/admissions/waitlist`ordered by grade + position with Offer-this-spot promotion modal). Admissions launchpad tile gated on`stu-003:admin`. New `AcademicCapIcon`. New `apps/web/src/hooks/use-enrollment.ts`(16 hooks). New`apps/web/src/lib/admissions-format.ts`. One small additive API endpoint: `GET /academic-years`on a new`AcademicYearController`wired into`SisModule`(gated on`stu-001:read`). Web build clean, API build clean. Live smoke against `tenant_demo`(8 read scenarios + 4 permission denials) all pass with the seeded shape. **Step 10 lands the Billing Admin UI** at`apps/web/src/app/(app)/billing/`— 4 routes (5 pages —`/billing/accounts`list +`/billing/accounts/[id]`detail with linked-students + invoices + payments + ledger panels,`/billing/invoices`list with status chips and`Generate from schedule`Modal +`/billing/invoices/[id]`detail with Send/Cancel admin buttons + line-items + payment history,`/billing/payments`global tracker with method+status filters and per-row admin Refund Modal,`/billing/fees`fee categories + schedules with create + edit Modals and a client-side per-grade affected-families counter). New`Billing`launchpad tile gated on`fin-001:write`. New `BanknotesIcon`. New `apps/web/src/hooks/use-billing.ts`(23 hooks). New`apps/web/src/lib/billing-format.ts`(currency formatting + status pills + recurrence/method/category labels). Cycle 6 payments DTO surface added to`apps/web/src/lib/types.ts`. **No new API endpoints** — sits entirely on Step 7's 24-endpoint payments surface. Web build clean. Live smoke against `tenant_demo`(13 read-only scenarios) all pass; tenant remains at seed state since Step 10 makes no mutations. **Step 11 lands the Parent Billing UI** at`apps/web/src/app/(app)/billing/page.tsx`(parent dashboard) +`apps/web/src/app/(app)/billing/pay/[invoiceId]/page.tsx`(payment form) +`apps/web/src/app/(app)/billing/ledger/page.tsx`(parent running-balance ledger view). The shared`Billing`tile in`apps.tsx`now branches on`personType=GUARDIAN`to route guardians to`/billing`(parent dashboard) instead of`/billing/accounts` (admin landing). Admin invoice detail (`/billing/invoices/[id]`) gains a Pay-now button visible to anyone with `fin-001:write`whenever`balanceDue > 0`AND status not DRAFT/CANCELLED. **No new hooks, no new types, no new API endpoints** — sits entirely on Step 10's 23 hooks and Step 7's 24-endpoint payments surface. Web build clean. Live smoke against`tenant_demo`(12 scenarios — 5 read-paths + 4 mutation paths including end-to-end pay $200→$200 + 3 permission denials, with cleanup) all pass; final balance read confirms`$400 cached:true`so the next CAT run starts clean. **Step 12 lands`docs/cycle6-cat-script.md`** — reproducible end-to-end vertical slice CAT, verified live against `tenant_demo` 2026-04-29. 4-check schema preamble (106 tenant base tables / 10 enr\* tables / 12 pay\_ tables / 0 cross-schema FKs) + 12 plan scenarios (admin pipeline → parent submit → admin transition → admin offer → parent ACCEPT firing `enr.student.enrolled` + PaymentAccountWorker UPSERT idempotency + graceful student-link skip → admin billing reads → idempotent generate-from-schedule → parent pays $200+$200 → admin partial-refund $50 → 6 permission denials across all 3 tiers). 3 ADR-057 envelopes captured live: `enr.student.enrolled`, `pay.payment.received` ×2, `pay.refund.issued`. Cleanup restores tenant to seed state ($400 balance). Cycle 6 ships clean to the post-cycle architecture review. See `HANDOFF-CYCLE6.md` and `docs/cycle6-cat-script.md`.
- **Cycle 6.1 — Profile & Household — COMPLETE + APPROVED at `29bf6a2` (REVIEW-CYCLE6.1-CHATGPT — final verdict, 2026-04-29). Round 1 against `e72525e` REJECT'd on 3 BLOCKING + 4 DEVIATION; all 7 fixes in `29bf6a2`; Round 2 returned APPROVED. Tagged `cycle6.1-complete` (`e72525e`) + `cycle6.1-approved` (`29bf6a2`). The reviewer's Round 2 non-blocking note about whitespace-only `previousNames` was addressed in the closeout commit via `@Matches(/\S/)`:** Cross-cutting Phase 2 polish slotted between Cycle 6 and Cycle 7. Plan at `docs/campusos-cycle6.1-implementation-plan.html`; handoff at `HANDOFF-CYCLE6.1.md`. **Step 1 complete:** Prisma migration `20260429065233_add_profile_household_fields` extends `platform.iam_person` with 12 personal columns (middle*name, suffix, previous_names TEXT[], 3 phones + 2 phone_type CHECK constraints, preferred_language default 'en', personal_email, notes, profile_updated_at — `preferred_name` and `date_of_birth` already existed); extends the existing `platform.platform_families` with 17 shared-household columns (full address, home_phone, home_language default 'en', mailing_address_same default true with 6 mailing variants, notes, updated_at); upgrades the existing `platform.platform_family_members` with `joined_at` + `updated_at` columns + UNIQUE on `person_id` (one household per person) + ON DELETE CASCADE on the `family_id` FK + partial UNIQUE INDEX on `(family_id) WHERE is_primary_contact = true` (declared via raw SQL since Prisma can't express it natively); extends the `MemberRole` enum with 5 new values (HEAD_OF_HOUSEHOLD, SPOUSE, CHILD, GRANDPARENT, OTHER_GUARDIAN — original 5 stay for backwards compat with the cross-school sibling-detection scaffolding). Live verification: 5 constraint smoke tests all behave correctly (LANDLINE rejected by phone_type CHECK, MOBILE accepted, second `is_primary_contact=true` in same family rejected by partial UNIQUE, duplicate `person_id` rejected by composite UNIQUE, `HEAD_OF_HOUSEHOLD` enum value accepted). 0 cross-schema FKs introduced. Existing Chen Family + 2 members (David PARENT primary-contact, Maya STUDENT) survive untouched; Step 4 seed will migrate them to HEAD_OF_HOUSEHOLD / CHILD and populate the address. **Step 2 complete:** Tenant migration `022_sis_student_demographics_and_guardian_employment.sql` adds `sis_student_demographics` (10 columns; `student_id` FK CASCADE on `sis_students`; UNIQUE INDEX on `student_id`) + 4 employment columns on the existing `sis_guardians` (employer, employer_phone, occupation, work_address — all nullable, no CHECKs since values are free-form TEXT). Tenant logical base table count: **107** (was 106 after Cycle 6 Step 4). 0 cross-schema FKs. Splitter `;`-in-string trap struck again on the first provision attempt — block-comment text containing `;` cut the migration mid-comment per the documented Cycles 4-6 trap; rewritten with periods + "and" before successful re-provision. Constraint smoke (5 tests in single rollback transaction) verified live on `tenant_demo`: happy-path INSERT, UNIQUE rejects 2nd row for same student, FK rejects bogus student_id, `pg_constraint.confdeltype='c'` confirms CASCADE in the catalog, `sis_guardians` accepts all 4 employment fields. Idempotent re-provision: column counts stable. Both `tenant_demo` and `tenant_test` provisioned cleanly. **Step 3 complete:** New function `USR-001 (Profile Management)` added to `packages/database/data/permissions.json` under group "User Profile & Household". Catalogue total: **149 functions × 3 tiers = 447 permissions** (was 444). `seed-iam.ts` rolePermsSpec updated — `USR-001:read+write` granted to Teacher (36 perms total, +2), Parent (17, +2), Student (17, +2), Staff (16, +2 — covers VP and counsellor). School Admin and Platform Admin get all three tiers via `everyFunction` (447 each, +3). Effective access cache rebuilt for all 7 demo accounts; live verification via `unnest(permission_codes)` confirms each persona has exactly the expected `usr-001:*`codes (admins get read+write+admin; non-admins get read+write). Pre-existing cosmetic bug in`seed-iam.ts` closing log line (`var`shadowing of`tiers`from inner loop overwrites the outer array, prints 298 instead of 447) — not Step 3 scope, harmless. **Step 4 complete:** New`packages/database/src/seed-profile.ts`(idempotent, gated on whether Chen Family already has a HEAD_OF_HOUSEHOLD member; wired as`seed:profile`in package.json). Five sections in one script: A) Chen Family`platform_families`row updated with`address_line1='1234 Oak Street'`, Springfield IL 62701, home_phone, home_language='en', mailing_address_same=true; B) Chen members migrated PARENT→HEAD_OF_HOUSEHOLD (David, primary_contact preserved) and STUDENT→CHILD (Maya); C) iam_person personal fields on 6 accounts (David/Sarah/James/Linda/Marcus + Maya) with preferredName (Dave/Sarah/Jim/Linda/Marc/Maya), unique +1-217-555-01XX primary_phone, phone_type_primary='MOBILE' (exercises the Step 1 CHECK live), work_phone for the 4 staff, personal_email, preferred_language='en', profile_updated_at; Maya also gets date_of_birth='2011-03-15'; D) 15 sis_student_demographics rows with primary_language='English' (Maya additionally gets gender='Female') joined through sis_students.platform_student_id → platform_students.person_id to identify her; ON CONFLICT (student_id) DO NOTHING for row-level idempotency; E) David Chen's sis_guardians row gets employer='Chen Engineering LLC', employer_phone='+1-217-555-0177', occupation='Mechanical Engineer', work_address='100 Engineering Blvd, Springfield, IL 62701' (intentionally different physical address from home so the Employment vs Household tab split is visually distinct). Idempotent re-run logs `Chen Family already migrated to household roles. Skipping.`Live verification: 6 personas with preferred_name + phone_type_primary='MOBILE', 5 with personal_email (Maya intentionally null — students don't have personal email yet), 15 demographics rows (1 with gender), 1 sis_guardians row with employer. **Step 5 complete:** ProfileModule lands at`apps/api/src/profile/`(ProfileService + ProfileController + DTOs in`dto/profile.dto.ts`) and is wired into AppModule after AnnouncementsModule. 4 endpoints: `GET /profile/me`and`PATCH /profile/me`gated on`usr-001:read`/`write`; `GET /profile/:personId`and`PATCH /profile/:personId`gated on`usr-001:admin`. **Catalog correction caught during smoke:** the plan called for `iam-001:read/write`on the admin endpoints, but`IAM-001`does NOT exist in the catalogue (never has). Admin endpoints corrected to`usr-001:admin`, which Step 3 already granted exclusively to School Admin + Platform Admin via `everyFunction`. ProfileService composes platform iam_person + login email + household membership (always platform) with persona-conditional tenant data — `sis_student_demographics`for STUDENT (joined via sis_students.platform_student_id → platform_students.person_id),`sis_guardians` employment for GUARDIAN, dual-table emergency contact (`hr_emergency_contacts`keyed via`hr_employees.person_id`for STAFF;`sis_emergency_contacts`keyed via`sis_students.platform_student_id → platform_students.person_id`for STUDENT;`null`for everyone else — guardians have no emergency-contact schema home today, intentionally Phase 2 polish). PATCH allow-list excludes identity fields (firstName/lastName/dateOfBirth post-set) on the self-service path; admin path adds them. Service tx model: platform writes (iam_person.update) implicit Prisma tx, tenant writes (sis_student_demographics + sis_guardians + sis_emergency_contacts/hr_emergency_contacts) all run inside one`executeInTenantTransaction`callback for atomicity, the two transactions are sequential not nested. **Class-hoisting bug caught during build:**`UpdateEmergencyContactDto`was declared after`UpdateMyProfileDto`even though the latter's`@ApiPropertyOptional({ type: () => UpdateEmergencyContactDto })`decorator evaluates eagerly at class-definition time; reordered to declare it first. Live smoke (8 scenarios on tenant_demo): David parent GET own returns full shape with HEAD_OF_HOUSEHOLD household + Chen Engineering employment; Maya student GET own returns demographics inline (gender Female, primaryLanguage English, dateOfBirth 2011-03-15); Jim teacher GET own returns STAFF without household; David PATCH preferredName + secondaryPhone with phone_type='HOME' (CHECK fires and passes); David PATCH firstName 400 by ValidationPipe forbidNonWhitelisted; Jim PATCH emergencyContact lands in hr_emergency_contacts with is_primary=true and source='EMPLOYEE'; Sarah admin PATCH Maya firstName via /profile/:personId works on usr-001:admin; David PATCH /profile/:Sarah's personId 403 INSUFFICIENT_PERMISSIONS. Cleanup restores tenant to post-Step-4 seed state. **Step 6 complete:** HouseholdsModule lands at`apps/api/src/households/`with 6 endpoints (the plan said 5; added`GET /households/:id`for arbitrary-by-id reads with row-scope to member-or-admin). Self-service household editing gated on`usr-001:write`plus service-layer`assertCanEditHousehold(familyId, actor)`— short-circuits true if`actor.isSchoolAdmin`OR caller has`usr-001:admin`, otherwise reads `platform_family_members`for the (family, person) pair and accepts HEAD_OF_HOUSEHOLD or SPOUSE only. **Transactional discipline:** every state-change opens a regular`prisma.$transaction` (NOT `executeInTenantTransaction` — household tables live in the platform schema), takes `SELECT ... FOR UPDATE` on `platform_families.id`, runs the membership gate inside the same tx, then mutates. Atomic primary-contact promotion: when `isPrimaryContact=true` lands, the service explicitly clears any existing primary in the same tx so the partial UNIQUE INDEX `(family_id) WHERE is_primary_contact=true` (Step 1) never fires; only the final UPDATE flips the new primary on. Refuses last-HEAD demotion + self-eviction without admin. Member-side mutations emit `iam.household.member_changed` via `KafkaProducerService.emit({sourceModule:'iam'})`; address-only PATCH does NOT emit. TypeScript fix during build: `tx` inside `$transaction`is`Prisma.TransactionClient`, not full `PrismaClient`. Live smoke (10 scenarios on tenant_demo): David parent GET /my canEdit=true; Maya GET /my canEdit=false; Maya PATCH 403 "Only the head of household or spouse..."; David PATCH addressLine1 200; David adds Sarah as SPOUSE 200; David promotes Sarah to primary → David's primary atomically cleared in same tx, Sarah is now sole primary; David tries to demote himself (last HEAD) 400; David self-eviction 400; David removes test SPOUSE Sarah 200; Sarah admin PATCH any household 200 (admin override). 4 envelopes captured live on `dev.iam.household.member_changed`with full ADR-057 shape (event_type, source_module='iam', tenant_id, fresh event_id/correlation_id UUIDv7s, event_version=1; payloads cover ADDED / UPDATED / REMOVED / UPDATED). Cleanup restores tenant to post-Step-4 seed state. **Step 7 complete:** Profile UI lands at`apps/web/src/app/(app)/profile/page.tsx`(self-service, 987 B) +`apps/web/src/app/(app)/profile/[personId]/page.tsx`(admin, 1.23 kB) with shared 6-tab module`apps/web/src/components/profile/ProfileTabs.tsx`(Personal Info / My Household / Emergency Contact / Demographics for STUDENT / Employment for GUARDIAN / Account). Tabs are persona-conditional via`profileTabs(personType)`from new`apps/web/src/lib/profile-format.ts`(also exports`profileCompleteness(profile)`0-100% formula based on required fields like primary_phone/first_name/last_name/household-role weight 1.0 + recommended fields like preferred_name/emergency-contact/employer/primary_language weight 0.5; and PHONE_TYPES + HOUSEHOLD_ROLES + label maps). New`apps/web/src/hooks/use-profile.ts`with 9 hooks: useMyProfile / useProfile(personId) / useUpdateMyProfile / useUpdateProfile(personId) / useMyHousehold / useHousehold(id) / useUpdateHousehold(id) / useAddHouseholdMember(id) / useUpdateHouseholdMember(id, memberId) / useRemoveHouseholdMember(id). 30s staleTime on reads. 13 new DTO interfaces in`apps/web/src/lib/types.ts`. Avatar dropdown in `TopBar.tsx`gains "My Profile" link above "Sign out" for any user with`usr-001:read`(= every persona). No new launchpad tile per the design principle ("launchpad, not dashboard"). Household tab uses`canEdit`from the API to drive editability; primary-contact promotion via inline per-member actions. Add-member modal accepts raw UUID for now (directory picker is future polish). Demographics tab greys out admin-only fields (gender, ethnicity, etc.) for self-service STUDENT view; admin`/profile/[personId]`lets admins edit them. Two build issues caught: Tailwind shorthand classes`.input`/`.btn-primary`/`.btn-secondary`don't exist in`globals.css`(project uses explicit class strings throughout) — swapped to the`/apply/new`form pattern; unused`useUpdateHouseholdMember`alias left in`HouseholdEditor`— removed. Both APIs underlying the routes verified live for parent + student personas (David`GET /profile/me`returns GUARDIAN+employment+household.role=HEAD_OF_HOUSEHOLD; Maya`GET /profile/me`returns STUDENT+demographics+household.role=CHILD; David`GET /households/my`returns canEdit=true; Maya`GET /households/my`returns canEdit=false). Production build's static prerender succeeded for`/profile`confirming no runtime errors at compile time. Full browser smoke (click-through save buttons, modal open/close, primary-contact promotion) is the Step 8 CAT's concern. **Transactional convention (clarification):** Household endpoints (Step 6) write only to platform-schema tables and so use a regular Prisma`$transaction`, NOT `executeInTenantTransaction`— the tenant`search_path`is irrelevant for platform writes. Profile endpoints (Step 5) compose: a single`PATCH /profile/me`may write`iam_person`(platform tx) AND`sis_guardians`/`sis_emergency_contacts`/`sis_student_demographics`(tenant tx) as separate transactions in order. **Step 8 complete:** Vertical-slice CAT lands at`docs/cycle6.1-cat-script.md`— schema preamble (12 checks all green: 22 iam_person columns + 20 platform_families columns + 2 platform_family_members new cols + 10 MemberRole enum values + partial UNIQUE on is_primary_contact + 10 sis_student_demographics columns + 4 sis_guardians employment columns + 3 USR-001 perm rows + 447 catalog total + Chen Family seed shape + 15 demographics rows + 6 personas with preferred_name) + 11 plan scenarios all pass on`tenant_demo`(S1 GUARDIAN composition; S2 phone_type CHECK passes on HOME; S3 ValidationPipe forbidNonWhitelisted on firstName; S4 HEAD_OF_HOUSEHOLD address PATCH; S5 CHILD canEdit=false + 403; S6 demographics self-service/admin-only split; S7 dual-table emergency contact lands in hr_emergency_contacts source=EMPLOYEE; S8 admin override on usr-001:admin; S9 full member lifecycle ADD→promote→demote-self refused→REMOVE→primary-restore with 4 envelopes captured; S10 permission denials parent/teacher 403, student row-scope 404; S11 web build static prerender succeeds for both /profile + /profile/[personId]). 4 ADR-057 envelopes captured live on`dev.iam.household.member_changed`with`source_module='iam'`+ tenant_id populated + fresh UUIDv7 event_id/correlation_id + event_version=1; payloads cover ADDED / UPDATED-promote / REMOVED / UPDATED-restore. Cleanup restores`tenant_demo`to post-Step-4 seed state. **Cycle 6.1 ships clean to the post-cycle architecture review.** Git tag pattern:`cycle6.1-complete`after this commit + CI green;`cycle6.1-approved`after the post-cycle architecture review verdict. Review prompt scaffold lives at`REVIEW-CYCLE6.1-CHATGPT.md`. **REVIEW-CYCLE6.1 (2026-04-29):** Round 1 at `e72525e`returned **REJECT** with 3 BLOCKING + 4 DEVIATION findings, all VALID. Fix commit lands all 7 with live verification: (BLOCKING 1)`assertTargetInCurrentTenant(personId)`added to ProfileService — admin profile endpoints`GET/PATCH /profile/:personId`now require the target person to have a`sis_students`/`sis_guardians`/`hr_employees`row in the calling tenant; school admin from school A can no longer read or edit a person from school B by guessing the UUID; verified live (Sarah GETs Maya 200, Sarah GETs admin@ 404, Sarah PATCHes admin@ 404). (BLOCKING 2)`HouseholdsService.hasAdmin(actor, familyId)`now ANDs the IAM permission with`householdAffiliatedWithCurrentTenant(familyId)`— at least one household member must have a`sis\*\*`/`hr_employees`row in the calling tenant; school admin from school A can no longer mutate school B's household via platform_families UUID; verified live (Sarah PATCHes Chen Family 200, Sarah GETs/PATCHes/POSTs phantom family 404 across the board). Companion 404-leak tightening:`assertCanEditHousehold`now returns 404 (not 403) for non-member non-admin actors, matching BLOCKING 2's "don't leak existence" intent. (BLOCKING 3)`upsertEmergencyContact` for STAFF + STUDENT now locks the parent row (`hr_employees`/`sis_students`) with `SELECT … FOR UPDATE`before any reads/writes; partial UNIQUE INDEX`(employee_id) WHERE is_primary=true`23505 leaks now caught by`isUniqueViolation`helper and translated to 409; verified live with 5 parallel PATCHes serialising cleanly (5×200, exactly 1 hr_emergency_contacts row remaining, is_primary=true). (DEVIATION 4)`previousNames`DTO gains`@MinLength(1, { each: true })`+`@MaxLength(100, { each: true })`so`[''']`is rejected with 400. (DEVIATION 5)`apps/web/src/lib/types.ts`adds`LegacyHouseholdRole`(PARENT/GUARDIAN/STUDENT) +`AnyHouseholdRole`union;`HOUSEHOLD_ROLE_LABELS`extended to all 10 enum values with "(legacy)" suffix on the 3 deprecated ones so a leaked legacy row never renders as`undefined`. (DEVIATION 6) `applyUpdate`moved iam_person.update INTO the existing`executeInTenantTransaction`callback — both schemas now write atomically in a single tx, eliminating the partial-save failure mode where iam_person committed but tenant tx failed; static verification: 0`this.platform.iamPerson.update`call sites in applyUpdate, 1`tx.iamPerson.update`inside the tenant tx. (DEVIATION 7)`isUniqueViolation(err)`helper in both ProfileService + HouseholdsService checks`err.code === 'P2010'`OR`err.meta?.code === '23505'`OR message regex — the SQLSTATE check is driver-version-stable. Live smoke confirms all 7 fixes;`pnpm format:check`clean;`pnpm --filter @campusos/api build` clean.
- Cycle 7: Tasks & Approval Workflows — **IN PROGRESS, Step 1 done.** Migration `026_tsk_tasks_and_auto_rules.sql` lands 6 logical M1 Task Management tables (tenant base table count 108 → 114). 12-assertion constraint smoke verified live on `tenant_demo`; idempotent re-provision verified. See `HANDOFF-CYCLE7.md` for full Step 1 detail and Steps 2–10 plan.
- Cycle 8: Helpdesk + cross-module integration hardening.
- Each cycle follows validated patterns from Phase 1
- UI matches the design guide established in Phase 2

## UI Design Principles

These are foundational decisions for the web app. New views must follow them; existing views are migrated as they are touched.

- **Less is more.** No screen should feel cluttered. When in doubt, remove information rather than add it. The previous persona-specific dashboards (TeacherDashboard, ParentDashboard, StudentDashboard, AdminDashboard) packed stats, tables, and queues onto the home page; they have been deleted in favour of a clean launchpad.
- **Home page is a launchpad, not a dashboard.** `/dashboard` is a Google-style centered page: `CampusOS` logo, persona-aware greeting (`Good morning/afternoon/evening, {Name}`), a search input ("What would you like to do today?"), and a grid of App tiles. No stats, no tables, no lists. Each App owns its own detail views.
- **Apps are the primary navigation unit.** The current apps are **Classes** (or **My Classes** for students, **My Children** for parents), **Messages**, and **Announcements**. The sidebar lists exactly the same apps as the home grid — the two surfaces stay in sync via `apps/web/src/components/shell/apps.tsx::getAppsForUser(user)`. Adding a new app is one edit: add it to the catalogue and it shows up in both surfaces with the right persona gating.
- **Persona-aware app grid.** Driven by IAM permissions and `personType`, not by role names:
  - `sch-001:admin` OR `personType=STAFF` → Classes tile pointing at `/classes`
  - `personType=STUDENT` → My Classes tile (same `/classes` route, student variant)
  - `personType=GUARDIAN` → My Children tile pointing at `/children`
  - Anyone with `com-001:read` → Messages tile
  - Anyone with `com-002:read` → Announcements tile
- **iOS-style unread badges.** Red circle, white number, capped at "99+", in the top-right of an app tile and trailing-aligned on a sidebar row. Wired through `apps/web/src/hooks/use-app-badges.ts`:
  - **Messages** badge = sum of `ThreadDto.unreadCount` from `useThreads()` (15s poll).
  - **Announcements** badge = count of `useAnnouncements()` rows where `isPublished && !isRead` (30s poll).
  - The hooks are gated on permissions via an optional `enabled` argument so a STUDENT without `com-001:read` doesn't 403 on `/threads`. Both queries already poll, so badges refresh without extra plumbing.
  - The top-bar `NotificationBell` is the unified cross-app notifier and is independent of these per-app badges.
- **Logo is sans-serif.** The "CampusOS" wordmark uses the Tailwind `font-sans` stack (DM Sans / Inter / system) with `font-semibold` and `tracking-tight`. Never `font-display` (DM Serif Display) for the wordmark. The default text wordmark is a placeholder until a school uploads its own logo. `font-display` is still allowed for in-page article titles like an announcement detail headline; it must not be used for the brand mark.
- **App pages are minimal lists, not dashboards.** `/classes` and `/children` are flat grids of cards (one card per class / child) with one or two primary actions per row. Recent activity, queues, and aggregate stats belong in their own future apps (Attendance, Absences, etc.), not bolted onto a launchpad or a list page.

## Architecture

- **840 tables** across 38 modules, governed by 76 ADRs (Architecture Decision Records)
- Schema-per-tenant multi-tenancy (PostgreSQL `search_path` switching)
- Modular monolith (NestJS) with planned extraction of 6 services
- Event-driven via Kafka
- Phased delivery plan (see "Delivery Plan (Revised)" above): build core (Cycles 0–3) → test & refine → expand (Cycles 4–8)

## Tech Stack

- **Backend:** NestJS 10 (TypeScript strict), Node.js 22 (CI + production image)
- **Frontend:** Next.js 14 (App Router, Tailwind CSS, React Query, Zustand)
- **Database:** PostgreSQL 16 (Prisma ORM, schema-per-tenant)
- **Cache:** Redis 7 (ioredis)
- **Events:** Apache Kafka (KafkaJS)
- **Auth:** External IdP via OIDC (Keycloak for dev). CampusOS never stores passwords.
- **Monorepo:** pnpm + Turborepo

## Project Structure

```
apps/api/                → NestJS backend (modular monolith)
apps/api/src/auth/       → AuthGuard (JWT), PermissionGuard, @Public, @RequirePermission
apps/api/src/tenant/     → TenantResolverMiddleware, TenantGuard, TenantPrismaService, AsyncLocalStorage
apps/api/src/iam/        → Roles, permissions, assignments, effective access cache, ActorContextService (resolves caller persona + isSchoolAdmin + employeeId per tenant — Cycle 4 Step 0 added the tenant-scoped hr_employees lookup keyed on iam_person.id)
apps/api/src/platform/   → M0 Platform Core
apps/api/src/sis/        → M20 SIS Core (Cycle 1 Step 5): students, classes, families, guardians; /classes/my includes todayAttendance summary (Step 8); /students/me added in Cycle 2 Step 9 to bootstrap a STUDENT persona's own studentId; AcademicYearController (Cycle 6 Step 8) exposing GET /academic-years (read-only catalogue lookup ordered by start_date DESC, gated on stu-001:read) — backs the create-period dropdown in the Admissions admin UI.
apps/api/src/attendance/ → ATT-001..005 (Cycle 1 Step 6): attendance + absence requests + Kafka emits
apps/api/src/payments/   → M84 Family Billing (Cycle 6 Step 7). Seven services + six controllers + 24 endpoints + one Kafka consumer (PaymentAccountWorker). LedgerService (internal-only `recordEntry(tx, ...)` called inside open tenant transactions; Redis-cached balance at `ledger:balance:{accountId}` TTL=30s with cache invalidation in same write tx; pay_ledger_entries IMMUTABLE per ADR-010 — no UPDATE/DELETE service methods). FeeScheduleService (admin-only fee category + schedule CRUD on fin-001:admin; reads on fin-001:read). FamilyAccountService (list / getById / listStudents / balance / ledger; row-scoped on account_holder_id = actor.personId for parents). InvoiceService (create DRAFT / list / get / send locks invoice FOR UPDATE + writes CHARGE ledger entry + emits pay.invoice.created / cancel refuses on PAID / generate-from-schedule bulk idempotent on existing fee_schedule_id attribution). PaymentService (list / get / pay locks invoice FOR UPDATE + writes PAYMENT ledger entry + recomputes invoice status PARTIAL/PAID + emits pay.payment.received; Stripe stubbed — CARD auto-COMPLETE with mock pi_dev_<uuid>; refuses overpay vs balance-due pre-check). RefundService (list admin-only at service layer / issue locks payment FOR UPDATE + writes REFUND ledger entry + marks payment REFUNDED if full-amount + emits pay.refund.issued; refund cap pre-check; Stripe stubbed re_dev_<uuid>). PaymentPlanService (admin-only POST creates plan + auto-generates installment rows in one tx; due dates = startDate + n × frequency; last installment absorbs sub-cent residue). PaymentAccountWorker (Kafka consumer on enr.student.enrolled under group payment-account-worker; reuses unwrapEnvelope + processWithIdempotency claim-after-success; UPSERTs pay_family_accounts on (school, account_holder_id) + auto-allocates FA-#### account_number from MAX+1 + idempotently links sis_students; gracefully skips when guardian_person_id is null or sis_students doesn't exist yet). PaymentsModule imports TenantModule + IamModule + KafkaModule + NotificationsModule (for RedisService).
apps/api/src/enrollment/ → M81 Enrollment (Cycle 6 Step 6). Five services + four controllers + 17 endpoints under stu-003:read/write/admin. EnrollmentPeriodService (period CRUD + nested stream + capacity create; admin writes lock the row FOR UPDATE; window CHECK re-validated on PATCH against merged state). ApplicationService (parent submission stamps guardian_person_id from actor.personId; admin status transitions with assertTransitionAllowed function rejecting illegal moves at the service layer; admin patch locks row FOR UPDATE and recomputes capacity_summary in same tx; non-admin reads strip is_confidential=true notes). OfferService (admin issue; CONDITIONAL conditions verify; respond locks BOTH enr_offers AND enr_applications FOR UPDATE in same tx — Cycle 5 review carry-over satisfied; on ACCEPT, application transitions to ENROLLED + capacity_summary recomputes + enr.student.enrolled fires). WaitlistService (admin-only list; offer-from-waitlist locks waitlist + parent app FOR UPDATE in one tx). CapacitySummaryService (internal-only UPSERT helper called inside open tenant transactions on every status flip; aggregates intake_capacities + applications + offers and recomputes the per-(period, grade) row). Emits enr.application.submitted, enr.application.status_changed, enr.offer.issued, enr.offer.responded, enr.student.enrolled (the last is the Step 7 PaymentAccountWorker subscription topic). EnrollmentModule imports TenantModule + IamModule + KafkaModule and lives between SchedulingModule and NotificationsModule.
apps/api/src/scheduling/ → M22 Academic Scheduling (Cycle 5 Steps 5 + 6). Step 5: BellScheduleService (6 endpoints — list / get-with-periods / create / patch / bulk upsert periods / set-default; admin gates on sch-001:admin; set-default atomically clears any other default in the same tx so the partial UNIQUE INDEX(school_id) WHERE is_default never rejects the flip). TimetableService (7 endpoints under sch-001:read with sch-001:admin writes; reads return the full join shape — sis_classes/sis_courses/sch_periods/sch_rooms/hr_employees/iam_person; the translateConflict helper decodes SQLSTATE 23P01 into a friendly 409 with the conflicting actor name, looking up sch_rooms.name / sch_periods.name / hr_employees+iam_person on the failure path; emits sch.timetable.updated on every write). RoomService (4 endpoints under sch-005:read/admin; list endpoint accepts availabilityDate + availabilityPeriodId and annotates each row with available:boolean|null by intersecting against active timetable slots that match the period for that date). RoomBookingService (4 endpoints under sch-005:read/write; assertNoConflicts checks both existing CONFIRMED bookings whose time range overlaps AND active timetable slots whose period clock-time on the booking's ISO weekday overlaps the booking window — schema does not enforce booking-vs-slot conflict, this is the app-layer gate per the Step 2 out-of-scope decision). RoomChangeRequestService (5 endpoints under sch-005:read/write; non-admins see only their own rows via row-scope filter on requested_by = actor.employeeId; submit refuses if the slot's teacher_id is set and not the actor; approve/reject admin-only with reviewed_chk multi-column CHECK enforced by the schema). Step 6: CalendarService (5 endpoints under sch-003:read/write — list / getById / GET /calendar/day/:date day-resolution with order override → event-with-bell_schedule_id → default → none / create / patch / delete; non-admins see only published, admins with includeDrafts=true see drafts; PATCH that toggles allDay clears times in the same UPDATE so the time-consistency CHECK never fires mid-flight). DayOverrideService (3 endpoints under sch-003:read/admin — list / create / delete-by-date; pre-checks for an existing row to surface a friendly 409 instead of letting the schema raise). CoverageService (4 endpoints under sch-004:read/write — list / getById with row-scope (admin OR absent OR assigned) / assign locks the row FOR UPDATE then writes both the status flip and the matching sch_substitution_timetable row in one tx + emits sch.coverage.assigned outside the tx / cancel drops the substitution row in the same tx then flips status=CANCELLED). SubstitutionService (2 read-only endpoints — substitution rows are written exclusively by CoverageService.assign). CoverageConsumer (Kafka consumer on hr.leave.coverage_needed under group coverage-consumer; reuses unwrapEnvelope + processWithIdempotency from notification-consumer-base; per inbound event enumerates weekdays Mon–Fri in [startDate, endDate], joins through sch_timetable_slots → sch_periods for each affectedClasses[].classId, filters slots whose date range covers the date AND whose period day_of_week is NULL or matches the date's ISO weekday, INSERTs sch_coverage_requests with status=OPEN — UNIQUE(timetable_slot_id, coverage_date) is the schema-side belt-and-braces dedup; emits sch.coverage.needed once with createdCoverageIds[] when new rows land). SchedulingModule imports TenantModule + IamModule + KafkaModule + (Cycle 5 Step 9) SisModule. Step 9 adds TimetableService.listForStudent(studentId, actor) which routes through StudentService.assertCanViewStudent for the row-scope check and joins sis_enrollments → sch_timetable_slots for the student's ACTIVE enrollments. New endpoint GET /timetable/student/:studentId gated on stu-001:read (every persona) — the row-scope at the service layer is the actual access gate, since parents and students don't hold sch-001:read.
apps/api/src/hr/         → M80 HR/Workforce (Cycle 4 Steps 6 + 7). Step 6: EmployeeService, PositionService, EmployeeDocumentService (12 endpoints under hr-001:read / write / admin; row-scope guard on documents). Step 7: LeaveService (7 endpoints + 4 Kafka emits — hr.leave.requested/approved/rejected/cancelled — under hr-003:read/write; balance updates inside executeInTenantTransaction so the non-negative CHECKs from migration 012 fail loudly on underflow), CertificationService (4 endpoints under hr-004:read/write; admin-only verify with hr.certification.verified emit + admin /certifications/expiring-soon sweep returning the partial-index window), TrainingComplianceService (per-employee + admin school-wide dashboard; derived green/amber/red urgency from is_compliant + days_until_due), LeaveNotificationConsumer (group leave-notification-consumer; subscribes to all 4 leave topics; notifies admins on requested + submitter on approved/rejected; on approved also republishes hr.leave.coverage_needed with affected class ids resolved via sis_class_teachers for the Cycle 5 Scheduling consumer). DTOs use class-validator with IsIn arrays kept in sync with SQL CHECK constraints. Imports KafkaModule + NotificationsModule for the Step 7 wiring.
apps/api/src/classroom/  → M21 Classroom (Cycle 2): assignment + category CRUD (Step 4) + submissions, grading (single/batch/publish), gradebook reads, and progress notes (Step 5) + GradebookSnapshotWorker (Step 6 — first Kafka consumer; subscribes to cls.grade.{published,unpublished} under group `gradebook-snapshot-worker`, debounces 30s per (class,student), recomputes cls_gradebook_snapshots). Step 9 adds GET /students/:studentId/classes/:classId/grades (per-(student, class) assignment breakdown — published-only for non-managers; reuses GradebookService.assertCanViewStudent + AssignmentService.assertCanReadClass for row scope). 4 services + 1 worker + 6 controllers; emits cls.submission.submitted, cls.grade.published, cls.grade.unpublished, cls.progress_note.published.
apps/api/src/kafka/      → KafkaProducerService (best-effort emit; ADR-057 envelope wraps every payload — Cycle 3 Step 0; takes EmitOptions {topic, key, payload, sourceModule, ...}) + KafkaConsumerService (best-effort consumer registry, Cycle 2 Step 6) + IdempotencyService (claim against platform.platform_event_consumer_idempotency) + event-envelope.ts (EventEnvelope interface, envelopeFromOptions, prefixedTopic helper for KAFKA_TOPIC_ENV — Cycle 3 Step 0)
apps/api/src/notifications/ → M40 notification pipeline (Cycle 3 Step 5; Step 8 read-side). RedisService (best-effort ioredis: SET NX idempotency, ZADD on `notif:inapp:{accountId}`, HINCRBY/HDEL/HVALS/HGETALL on `inbox:{accountId}` for per-thread unread; the HDEL/HVALS/HGETALL helpers were added in Step 6 for UnreadCountService; the Step 8 helpers `listInAppNotifications` (ZREVRANGE), `countInAppSince` (ZCOUNT exclusive), and `get/setNotificationLastReadAt` (`notif:lastread:{accountId}` epoch-ms key, no TTL) back the bell). NotificationQueueService.enqueue() (Redis SET NX → preference + quiet-hours check → INSERT into msg_notification_queue PENDING). NotificationDeliveryWorker (10s poll across all platform.schools; SELECT FOR UPDATE SKIP LOCKED LIMIT 25; IN_APP delivery via Redis ZADD + msg_notification_log row; EMAIL/PUSH/SMS stubbed; up-to-5 retries with 30s backoff before status=FAILED). NotificationInboxService + NotificationInboxController (Step 8 — 3 endpoints under com-001:read OR com-002:read: GET /notifications/inbox returns `{ unreadCount, items, lastReadAt }` from the Redis sorted set, GET /notifications/history paginates msg_notification_queue with keyset cursor on COALESCE(sent_at, created_at) and an optional exact `type` filter, POST /notifications/mark-all-read bumps the lastread key to Date.now()). 5 Kafka consumers under `consumers/`: AttendanceNotificationConsumer (att.student.marked_{tardy,absent} → guardians via sis_student_guardians), GradeNotificationConsumer (cls.grade.published → student + guardians), ProgressNoteNotificationConsumer (cls.progress_note.published → guardians if is_parent_visible + student if is_student_visible), AbsenceRequestNotificationConsumer (att.absence.requested → school admins via iam_effective_access_cache; att.absence.reviewed → submitter), MessageNotificationConsumer (msg.message.posted → non-sender thread participants + Redis unread bump; producer ships in Step 6). Shared base helper `unwrapEnvelope()` + `processWithIdempotency()` (claim-after-success, REVIEW-CYCLE2 BLOCKING 2).
apps/api/src/announcements/ → M40 announcements (Cycle 3 Step 7). AnnouncementService (list / get / create / patch / mark-read / stats; manager scope = School Admin OR personType=STAFF, reader scope = msg_announcement_audiences row required; published is one-way), AudienceFanOutWorker (Kafka consumer on msg.announcement.published, group `audience-fan-out-worker`; resolves audience by audience_type — ALL_SCHOOL/CLASS/YEAR_GROUP/ROLE — into msg_announcement_audiences with delivery_status='DELIVERED', then enqueues `announcement.published` per recipient via NotificationQueueService; reuses unwrapEnvelope + processWithIdempotency for claim-after-success). 6 endpoints under com-002:read / com-002:write: GET /announcements, GET /announcements/:id, POST /announcements (draft or publish-now), PATCH /announcements/:id (edit draft, flip to published), POST /announcements/:id/read (idempotent — also flips audience row PENDING→DELIVERED), GET /announcements/:id/stats (author-or-admin only — total audience + read count + read % + delivery breakdown). Emits msg.announcement.published.
apps/api/src/messaging/  → M40 messaging core (Cycle 3 Step 6; Step 9 adds the compose-helper endpoints). ThreadService (create / list / read / archive — validates participants vs msg_thread_types.allowed_participant_roles via iam_role_assignment + roles + iam_scope, refuses pairs with msg_user_blocks rows, writes msg_admin_access_log when an admin reads a non-participant thread for FERPA audit; Step 9 added listThreadTypes + listRecipients and surfaces lastMessagePreview + lastSenderName on `list` via LATERAL JOIN on msg_messages — null on getById), MessageService (post + emit msg.message.posted + Redis HASH bump + content-moderation gate; 15-min author-only edit window; soft-delete that blanks the body in rowToDto for non-author readers), UnreadCountService (Redis-backed: increment / clearThread / getBadgeCount / getByThread — never queries msg_messages), ContentModerationService (loads active msg_moderation_policies in one tenant query; whole-word case-insensitive keyword match — Scunthorpe-safe; "most restrictive wins" = BLOCKED > ESCALATED > FLAGGED > CLEAN; writes msg_moderation_log on every non-CLEAN verdict). 12 endpoints (10 from Step 6 + 2 from Step 9) all under com-001:read / com-001:write: GET/POST /threads, GET /threads/types (Step 9), GET /threads/recipients (Step 9), GET /threads/:id, POST /threads/:id/read, PATCH /threads/:id/archive, GET/POST /threads/:threadId/messages, PATCH/DELETE /messages/:id, GET /notifications/unread-count (com-001:read OR com-002:read so the badge renders for any persona that holds either communications read tier).
apps/web/                → Next.js 14 frontend (App Router, Tailwind, React Query, Zustand)
apps/web/src/lib/        → api-client (Bearer + X-Tenant-Subdomain, single-flight 401→refresh), auth-store (Zustand), auth-context, query-client, shared TS types. Cycle 5 Step 7: scheduling-format.ts — formatting helpers (scheduleTypeLabel, periodTypeLabel, roomTypeLabel, dayOfWeekLabel, formatTime, bookingStatusLabel, changeRequestStatusLabel) plus ROOM_TYPES / PERIOD_TYPES / WEEKDAY_LABELS const arrays so the Schedule pages keep formatting consistent. Cycle 5 Step 8 extends the same module with CALENDAR_EVENT_TYPES + calendarEventTypeLabel + calendarEventChipClasses (per-eventType Tailwind chip palette used by both the calendar grid and the event detail modal) + coverageStatusLabel + coverageStatusPillClasses + todayIso() + addDaysIso(date, n) ISO date utilities. Cycle 6 Step 8: admissions-format.ts — status labels and pill class maps for periods/applications/offers/waitlist + NOTE_TYPE_LABELS + formatStudentName + formatDateOnly + formatDateTime + formatRelativeDeadline (renders responseDeadline as "in N days" / "in N weeks" / "expired YYYY-MM-DD") + todayIso + addDaysIso. Cycle 6 Step 10: billing-format.ts — RECURRENCE_OPTIONS / INVOICE_STATUSES / PAYMENT_METHODS / PAYMENT_STATUSES / REFUND_CATEGORIES const arrays + per-enum label maps + per-status pill class maps for invoices/payments/family-accounts/refunds/ledger-entry-types + formatCurrency (Intl.NumberFormat USD) + formatSignedCurrency (positive `+`, negative `−` for the ledger running-balance view) + formatDateOnly / formatDateTime mirrors so admissions-format.ts and billing-format.ts stay independent.
apps/web/src/components/ui/        → Avatar, StatusBadge, LoadingSpinner, EmptyState, PageHeader, Modal, Toast (provider+useToast), DataTable, cn helper
apps/web/src/components/shell/     → AppLayout (responsive drawer), Sidebar (persona + permission-driven; tile active-state uses AppDef.routePrefix when set, falling back to AppDef.href — added Cycle 5 Step 9 so /schedule/coverage etc. keep the Schedule tile lit), TopBar (avatar menu, sign-out, NotificationBell when user holds com-001:read OR com-002:read), inline SVG icons (Cycle 3 Step 8 adds BellIcon, ChatBubbleIcon, GradeIcon, MegaphoneIcon, CheckCircleIcon; Cycle 5 Step 7 adds CalendarIcon for the Schedule app tile; Cycle 6 Step 8 adds AcademicCapIcon for the Admissions app tile; Cycle 6 Step 10 adds BanknotesIcon for the Billing app tile)
apps/web/src/components/notifications/ → NotificationBell (top-bar bell + badge + dropdown; polls /notifications/inbox every 30s; uses describeNotification(item) for per-type title/subtitle and resolveDeepLink(item, user) for persona-aware link routing — exports both helpers for the /notifications page to reuse) — Cycle 3 Step 8
apps/web/src/components/dashboard/ → TeacherDashboard (Step 8), ParentDashboard (Cycle 1 Step 10; Cycle 2 Step 9 adds the per-child Grades section + "View grades" button), AdminDashboard (Step 11), StudentDashboard (Cycle 2 Step 9 — upcoming-assignment list + per-class average cards)
apps/web/src/components/classroom/ → ClassTabs (shared header for /classes/:id/* routes), AssignmentForm (create + edit), CategoryWeightModal (Step 7), GradeCellEditor (gradebook inline editor — Step 8), ProgressNoteModal (per-student upsert by class+term — Step 8), StudentClassGradesView (Step 9 — shared per-class breakdown UI used by both student and parent routes)
apps/web/src/components/scheduling/ → TimetableWeekView (Cycle 5 Step 9 — reusable periods × Mon-Fri grid driven by useBellSchedules; accepts slots + optional substitutions array for amber-highlight overrides; showTeacher / showRoom toggles), RoomAvailabilityChecker (Cycle 5 Step 9 — compact widget on /schedule/room-bookings powered by Step 5's `available: boolean | null` annotation on /rooms)
apps/web/src/hooks/      → React Query hooks. Cycle 1: useMyClasses, useClasses, useClass, useClassAttendance, useBatchSubmitAttendance, useAbsenceRequests, useMyChildren, useStudent, useStudentAttendance, useSubmitAbsenceRequest. Cycle 2 Step 7 (use-classroom.ts): useAssignmentTypes, useAssignments, useAssignment, useCreateAssignment, useUpdateAssignment, useDeleteAssignment, useCategories, useUpsertCategories. Step 8 (same file): useClassGradebook, useStudentGradebook, useSubmissionsForAssignment, useSubmission, useGradeSubmission, useBatchGrade, usePublishGrade, useUnpublishGrade, usePublishAllGrades, useStudentProgressNotes, useUpsertProgressNote. Step 9 (same file): useMyStudent (GET /students/me), useStudentClassGrades (GET /students/:studentId/classes/:classId/grades), useMySubmission (GET /assignments/:id/submissions/mine), useSubmitAssignment (POST /assignments/:id/submit). Cycle 3 Step 8 (use-notifications.ts): useNotificationInbox (polls /notifications/inbox every 30s, refetches on focus), useNotificationHistory ({ limit, type, before } params), useMarkAllNotificationsRead (invalidates ['notifications']). Cycle 3 Step 9 (use-messaging.ts): useThreads (15s poll), useThread (30s poll), useThreadMessages (15s poll), useThreadTypes (5min stale), useMessagingRecipients, useCreateThread, usePostMessage, useMarkThreadRead, useArchiveThread. Cycle 3 Step 10 (use-announcements.ts): useAnnouncements ({includeDrafts, includeExpired}, 30s poll), useAnnouncement, useAnnouncementStats (30s poll, gated by id-null), useCreateAnnouncement, useUpdateAnnouncement, useMarkAnnouncementRead. Cycle 4 Step 8 (use-hr.ts): 16 hooks — useEmployees (refetch on focus), useEmployee, useMyEmployee, usePositions (5min stale), useEmployeeDocuments, useEmployeeCertifications, useExpiringCertifications (60s poll), useEmployeeCompliance, useComplianceDashboard (60s poll), useLeaveTypes (5min stale), useMyLeaveBalances, useLeaveRequests, plus 5 mutations: useSubmitLeaveRequest, useApproveLeaveRequest, useRejectLeaveRequest, useCancelLeaveRequest, useVerifyCertification. Cycle 5 Step 7 (use-scheduling.ts): 15 hooks — useBellSchedules (60s stale), useBellSchedule, useCreateBellSchedule, useUpdateBellSchedule(id), useUpsertPeriods(scheduleId), useSetDefaultBellSchedule; useTimetable(args), useTimetableForTeacher, useTimetableForClass, useTimetableForRoom, useCreateTimetableSlot, useUpdateTimetableSlot(id), useDeleteTimetableSlot; useRooms(args, 30s stale), useRoom, useCreateRoom, useUpdateRoom(id); useRoomBookings(args), useCreateRoomBooking, useCancelRoomBooking; useRoomChangeRequests(args), useCreateRoomChangeRequest, useApproveRoomChangeRequest(id), useRejectRoomChangeRequest(id). Cycle 5 Step 8 (same file): 15 more hooks — useCalendarEvents(args), useCalendarEvent(id), useCalendarDayResolution(date), useCreateCalendarEvent, useUpdateCalendarEvent(id), useDeleteCalendarEvent; useDayOverrides(args), useCreateDayOverride, useDeleteDayOverride; useCoverageRequests(args, refetch on focus), useCoverageRequest(id), useAssignCoverage(id) (also invalidates ['notifications']), useCancelCoverage(id); useSubstitutions(args), useSubstitutionsForTeacher(employeeId, args). Cycle 5 Step 9 (same file): useTimetableForStudent(studentId) (calls the new row-scoped /timetable/student/:studentId endpoint). 31 total scheduling hooks. Cycle 6 Step 8 (use-enrollment.ts): 16 hooks — useAcademicYears (5min stale, GET /academic-years); useEnrollmentPeriods, useEnrollmentPeriod(id), useCreateEnrollmentPeriod, useUpdateEnrollmentPeriod(id), useCreateAdmissionStream(periodId), useCreateIntakeCapacity(periodId); useApplications(args, refetch on focus), useApplication(id), useCreateApplication, useUpdateApplicationStatus(id), useAddApplicationNote(id); useOffers (refetch on focus), useOffer(id), useIssueOffer(applicationId), useSetOfferConditionsMet(id), useRespondToOffer(id) (also invalidates ['notifications']); useWaitlist(args), useOfferFromWaitlist(id) (invalidates waitlist + applications + offers + periods because all four denormalisations change atomically). Cycle 6 Step 10 (use-billing.ts): 23 hooks — useFeeCategories (5min stale), useCreateFeeCategory; useFeeSchedules, useFeeSchedule(id), useCreateFeeSchedule, useUpdateFeeSchedule(id); useFamilyAccounts (refetch on focus), useFamilyAccount(id), useFamilyAccountBalance(id) (30s stale), useFamilyAccountLedger(id, args); useInvoices(args, refetch on focus), useInvoice(id), useCreateInvoice, useSendInvoice(id) (invalidates family-accounts + family-account because the CHARGE ledger entry shifts the balance), useCancelInvoice(id), useGenerateFromSchedule; usePayments(args, refetch on focus), usePayment(id), usePayInvoice(invoiceId) (invalidates invoice + invoices + payments + family-accounts + family-account); useRefunds(args), useIssueRefund(paymentId) (invalidates payments + payment + refunds + family-accounts + family-account); usePaymentPlan(id), useCreatePaymentPlan(invoiceId).
apps/web/src/app/        → Next.js routes: /login, /(app)/dashboard (persona-aware: sch-001:admin→Admin, STAFF→Teacher, GUARDIAN→Parent, STUDENT→Student), /(app)/classes/[id]/attendance, /(app)/classes/[id]/assignments + /new + /[assignmentId]/edit (Step 7), /(app)/classes/[id]/gradebook (Step 8 — spreadsheet grid + ProgressNoteModal), /(app)/assignments (Step 9 — student inbox across all enrolled classes), /(app)/assignments/[assignmentId] (Step 9 — student detail + submit form), /(app)/assignments/[assignmentId]/submissions (Step 8 — teacher grading queue), /(app)/submissions/[id] (Step 8 — single-submission detail with grade entry), /(app)/grades + /(app)/grades/[classId] (Step 9 — student gradebook + per-class breakdown), /(app)/children/[id]/attendance, /(app)/children/[id]/absence-request, /(app)/children/[id]/grades + /(app)/children/[id]/grades/[classId] (Step 9 — parent per-child gradebook + per-class breakdown w/ progress notes), /(app)/notifications (Cycle 3 Step 8 — full paginated notification history with 6 filter chips and mark-all-read), /(app)/messages + /(app)/messages/new + /(app)/messages/[threadId] (Cycle 3 Step 9 — inbox, compose, thread view with reply input, mark-read on mount, moderation feedback), /(app)/announcements + /(app)/announcements/new + /(app)/announcements/[id] (Cycle 3 Step 10 — audience-filtered feed with 4 filter chips, compose with audience-type picker, detail page with mark-read on mount + stats panel for authors/admins + Publish-now button for drafts), /(app)/staff + /(app)/staff/[id] + /(app)/staff/me (Cycle 4 Step 8 — searchable employee directory; tabbed profile with Info / Certifications / Leave / Documents tabs gated to own profile or admin; /staff/me client-redirect), /(app)/leave + /(app)/leave/new + /(app)/leave/approvals + /(app)/compliance (Cycle 4 Step 9 — employee leave with balance bars + cancel button on PENDING; admin-only approval queue with Approve/Reject Modal; admin compliance dashboard with green/amber/red filter chips and per-employee detail expand), /(app)/my-schedule + /(app)/children/[id]/schedule (Cycle 5 Step 9 — teacher / student / parent non-admin schedule views; /my-schedule branches on personType, calls useTimetableForTeacher + useSubstitutionsForTeacher for STAFF or useTimetableForStudent for STUDENT, both feed the shared TimetableWeekView component; /children/[id]/schedule is the parent route via the new /timetable/student/:studentId row-scoped endpoint), /(app)/schedule/bell-schedules + /(app)/schedule/bell-schedules/[id] + /(app)/schedule/timetable + /(app)/schedule/rooms + /(app)/schedule/room-bookings (Cycle 5 Step 7 — Schedule app tile gated on sch-001:read; bell schedules list + period editor with HH:MM time inputs + period_type select + bulk upsert via the Step 5 DELETE-all+INSERT-each transactional endpoint; timetable week-view grid with periods × Mon-Fri columns and By teacher / class / room filter chips; room directory cards with type / capacity / amenity pills; day-grouped room booking calendar with from-date / room / status filters and Modal-driven New booking flow that surfaces the Step 5 assertNoConflicts 409 via Toast), /(app)/calendar + /(app)/schedule/coverage + /(app)/schedule/coverage/history (Cycle 5 Step 8 — Calendar app tile gated on sch-003:read; Mon-leading 7×6 month grid colour-coded by event type with admin Show-drafts toggle and Modal-driven event detail (Publish/Delete) + draft-or-publish create flow; day-of coverage board with Open/Assigned/Cancelled sections, admin Assign Modal whose substitute dropdown excludes the absent teacher and inactive employees, Cancel Modal that drops the matching substitution row in the Step 6 backend tx; coverage history with from/to date + status + name-substring filters grouped by coverage date newest-first), /(app)/admissions/applications + /(app)/admissions/applications/[id] + /(app)/admissions/periods + /(app)/admissions/periods/[id] + /(app)/admissions/waitlist (Cycle 6 Step 8 — Admissions app tile gated on stu-003:admin; pipeline view grouping ApplicationDto by status across 6 columns with period+grade filters; review page with status transitions + OfferPanel inline (CONDITIONAL conditions Mark-met/failed + Accept/Decline proxy buttons gated on conditions resolved) + NoteComposer with confidential toggle; periods list with capacity rollup + Open/Close transitions + New-period modal; period detail with per-grade Capacity Dashboard colour-coded green/amber/red + Add-stream / Add-capacity modals; waitlist ordered by grade and position with Offer-this-spot promotion modal that PATCHes /waitlist/:id/offer), /(app)/apply + /(app)/apply/new + /(app)/offers/[id] (Cycle 6 Step 9 — parent Apply tile gated on personType=GUARDIAN + stu-003:write; landing combines own-applications tracker + Start-new entry; single-page application form auto-selecting the only OPEN period and deriving grade dropdown from the period's intake_capacities; offer response page with Accept/Decline/Defer where Accept is disabled while a CONDITIONAL offer's conditionsMet !== true and the inline emerald Welcome banner renders on Accept once status flips to ACCEPTED + application=ENROLLED), /(app)/billing + /(app)/billing/pay/[invoiceId] + /(app)/billing/ledger (Cycle 6 Step 11 — parent surfaces; the shared Billing tile branches on personType=GUARDIAN to route guardians to /billing instead of /billing/accounts. /billing dashboard reads useFamilyAccounts (row-scoped backend, returns own family) + useFamilyAccountBalance (Redis-cached, 30s stale) + useInvoices + usePayments; renders 3-stat header + Outstanding-invoices section sorted by due-date asc with Pay-now buttons + 5-row Recent payments + View-ledger header button. /billing/pay/[invoiceId] payment form: amount input defaults to balanceDue via useEffect, Pay-full-balance quick-fill, method picker constrained to CARD+BANK_TRANSFER for non-admins (matches backend assertSelfServiceMethod) and full 5-method menu for admins, CARD+BANK_TRANSFER hint cards, optional notes (500-char), client-side amount validation matching backend overpay pre-check, on success Toast + router.push back to invoice detail. /billing/ledger parent running-balance view: anchors at current balance and walks newest-first ledger backwards via single useMemo so each row's runningBalance reflects state immediately AFTER that entry was posted; 5-column table (When / Type / Description / Amount via formatSignedCurrency / Balance via formatCurrency); header card subtitle copy switches between Outstanding / Credit on file / Settled. Admin invoice detail (/billing/invoices/[id]) gains a Pay-now button visible to anyone with fin-001:write whenever balanceDue > 0 AND status not DRAFT/CANCELLED, routing to /billing/pay/[invoiceId]. No new hooks, no new types, no new API endpoints), /(app)/billing/accounts + /(app)/billing/accounts/[id] + /(app)/billing/invoices + /(app)/billing/invoices/[id] + /(app)/billing/payments + /(app)/billing/fees (Cycle 6 Step 10 — Billing app tile gated on fin-001:write; family-account list with 3-stat header (Active / Outstanding / Total) + search across number/holder/email/student names; account detail with linked-students cards + invoices/payments/ledger panels (signed amounts via formatSignedCurrency, ledger newest-first with type pills); global invoice list with 6+1 status chips + account dropdown + admin `Generate from schedule` Modal that POSTs /invoices/generate-from-schedule (returns {created, skipped, invoiceIds}); invoice detail with admin Send + Cancel buttons + line-items table with fee-schedule attribution + payment-history table; global payment tracker with method+status filters and per-row admin Refund Modal forcing 6-value refundCategory + reason; fee categories grid + admin New-category Modal + fee schedules table with per-grade affected-families counter computed client-side from the family-accounts list)
packages/database/       → Prisma schema, tenant SQL migrations, provisioning, seed scripts. `build` script chains `prisma generate` before tsc so CI/Docker builds are self-sufficient.
packages/shared/         → Shared TypeScript types and constants
```

## Key Design Contracts

- **Identity (ADR-055):** `iam_person` is the canonical FK for human identity. `platform_users` is ONLY for auth/audit columns. Domain projections (`sis_staff`, `sis_guardians`) carry direct `person_id` refs to `iam_person`. `sis_students` is a transitive projection — its identity path is `sis_students → platform_students.person_id → iam_person.id` (`platform_students` exists for cross-school student portability).
- **Soft cross-schema refs (ADR-001/020/028):** Tenant tables MUST NOT have DB-enforced FK constraints to `platform.*` tables. UUID columns + app-layer Prisma validation only. Cross-schema joins on the read path are fine; FK constraints are not. Health monitoring of soft refs is a future concern (`platform_reference_health`).
- **Permissions:** 444 permission codes (148 functions × 3 tiers: read/write/admin). Check codes, never role names. Use `@RequirePermission('att-001:write')`. Catalogue is reconciled from `packages/database/data/permissions.json` by `seed-iam.ts` — adds new codes, removes stale ones.
- **Tenancy (ADR-001):** Every tenant query uses `search_path = tenant_<id>, platform, public`. Platform tables are shared. Tenant tables are isolated. Schema-per-tenant — never store tenant_id columns on tenant-scoped tables.
- **UUIDs (ADR-002):** All PKs are UUIDv7, generated in the application layer via `generateId()` from `@campusos/database`.
- **Attendance partitioning (ADR-007):** `sis_attendance_records` is composite-partitioned `RANGE(school_year) → HASH(class_id) MODULUS 8`. Composite PK `(id, school_year, class_id)`. Queries should include `class_id` and `date` (or `school_year`) in the predicate to enable partition pruning. Year partitions cover 2024-08 through 2028-08; rotation is a future M0 job.
- **Frozen state (ADR-031):** `is_frozen=true` blocks all writes. Reads still work.
- **Guard order (Auth → Tenant → Permission):** All three guards are registered as `APP_GUARD` in `AppModule` to make order deterministic. `PermissionGuard` fails closed if `request.user` is missing.
- **Scope inheritance (ADR-036, partial):** `PermissionCheckService.resolveScopeChain` checks SCHOOL scope first, then PLATFORM scope. Used by both `PermissionGuard` (endpoint gates) and `hasAnyPermissionInTenant` (admin-status checks). Lets Platform Admins act against any tenant without per-school role assignments. Full district/department/class traversal is future work.
- **Tenant isolation under pooling:** `executeInTenantContext` and `executeInTenantTransaction` both wrap their callback in a Prisma `$transaction` that runs `SET LOCAL search_path TO "tenant_X", platform, public`. SET LOCAL is mandatory — a session-level SET on a pooled client can leak between concurrent requests and serve another tenant's data.
- **Row-level authorization:** Endpoint permission gates (`@RequirePermission`) are necessary but not sufficient. Multi-persona reads (e.g. `stu-001:read` is held by parents, students, teachers, and admins) MUST also apply a row filter via `ActorContextService.resolveActor(...)` + a per-personType visibility predicate. Pattern lives in `apps/api/src/sis/student.service.ts::visibilityClause`. Writes that are bound to a class (e.g. attendance) MUST verify caller membership in the relevant link table (`sis_class_teachers`) before mutating; admins bypass.
- **Admin checks are tenant-scoped, not cross-scope.** Use `permissionCheckService.hasAnyPermissionInTenant(accountId, schoolId, codes)` or read `actor.isSchoolAdmin` from `ActorContextService.resolveActor(...)`. NEVER scan `iam_effective_access_cache` across all scopes — that leaks admin status from school A into a request scoped to school B.
- **No implicit access:** Guardian access derived from `iam_relationship_access_rule`, never assumed.
- **Manager-only roster reads:** Endpoints that return roster-wide grade or assessment data (e.g. `GET /classes/:classId/gradebook`) MUST gate on a _manager_ permission tier (`tch-003:write`, not `tch-003:read`) AND a row-scope manager check (`assertCanWriteClass` — admin OR `sis_class_teachers` membership). `*:read` codes are typically held by students and parents, so they cannot be the gate for cross-roster views. Per-student endpoints (`/students/:id/...`) stay on `*:read` because they are already row-scoped to the linked person. (REVIEW-CYCLE2 BLOCKING 1.)
- **Kafka consumer idempotency must be claim-after-success.** Workers MUST NOT claim `platform_event_consumer_idempotency` on message arrival, because a recompute failure after the claim is at-most-once and silently drops the work. The pattern is: read-only `IdempotencyService.isClaimed(group, eventId)` on arrival → process → on success, `claim(group, eventId)` for every event-id that contributed to the flush. Recompute paths must be idempotent (UPSERT) so duplicate redelivery after an unclaimed failure is harmless. Pattern lives in `apps/api/src/classroom/gradebook-snapshot-worker.service.ts`. (REVIEW-CYCLE2 BLOCKING 2.)
- **Staff identity (resolved in Cycle 4 Step 0):** `sis_class_teachers.teacher_employee_id`, `cls_grades.teacher_id`, `cls_lessons.teacher_id`, and `cls_student_progress_notes.author_id` reference `hr_employees(id)` (soft FK per ADR-055 / ADR-001/020). Resolve the calling employee via `actor.employeeId` from `ActorContextService.resolveActor(...)` — null-safe; non-staff personas and the synthetic Platform Admin (which has no `hr_employees` row) get `null` and the row-scope check should short-circuit. The Cycle 2 DEVIATION 4 temporary mapping (column held `iam_person.id` directly) was retired by `seed-hr.ts`'s bridge UPDATE, which re-pointed every existing row to its corresponding `hr_employees.id`. The convention is annotated on each column via `COMMENT ON COLUMN`, so it is discoverable from the live schema.
- **Platform-scoped admin endpoints must verify tenant affiliation.** Admin endpoints that read/write platform-schema tables (`iam_person`, `platform_families`) must verify the target entity has a projection in the current tenant before allowing access. Use `assertTargetInCurrentTenant(personId)` for person lookups (target must have `sis_students` / `sis_guardians` / `hr_employees` row in the calling tenant) and `householdAffiliatedWithCurrentTenant(familyId)` for household lookups (at least one member must satisfy the same). Return 404 (not 403) on failure to avoid leaking the existence of cross-tenant data. The IAM permission gate alone is insufficient — `usr-001:admin` (and `sch-001:admin`) inherit through the platform scope chain so a school admin in tenant A can hold the perm against tenant B's data; the tenant-affiliation check is the actual access gate. Pattern established in Cycle 6.1 (REVIEW-CYCLE6.1 BLOCKING 1 + 2). Helpers live in `apps/api/src/profile/profile.service.ts::assertTargetInCurrentTenant` and `apps/api/src/households/households.service.ts::householdAffiliatedWithCurrentTenant`.

## Guard Chain (every request)

TenantResolverMiddleware → AuthGuard (JWT) → TenantGuard (frozen check) → PermissionGuard (@RequirePermission)

## Commands

```bash
# Start local services
docker compose up -d

# Start API (dev mode, port 4000, watch)
pnpm --filter @campusos/api dev

# Start web (dev mode, port 3000)
pnpm --filter @campusos/web dev

# Run tests
pnpm test

# Database migrations (platform schema, Prisma)
pnpm --filter @campusos/database migrate

# Tenant schema migrations
# Add SQL file to packages/database/prisma/tenant/migrations/ (numbered: 005_*.sql, 006_*.sql, ...)
# Then re-provision:
pnpm --filter @campusos/database provision --subdomain=demo
pnpm --filter @campusos/database provision --subdomain=test

# Seed pipeline (run in order)
pnpm --filter @campusos/database seed                       # platform: org, school, 5 test users, Chen family, provisions tenant_demo
pnpm --filter @campusos/database exec tsx src/seed-iam.ts   # 444 permissions, 6 roles, role-permission mappings, role assignments
pnpm --filter @campusos/database seed:sis                   # 15 students, 10 guardians, 8 families, 41 enrollments + attendance
pnpm --filter @campusos/database seed:classroom             # 12 assignments, 80 submissions, 62 grades, 41 gradebook snapshots, 1 progress note
pnpm --filter @campusos/database seed:messaging             # 4 thread types, 3 threads, 10 messages, 40 notification prefs, 2 moderation policies, 2 announcements, 3 queue rows
pnpm --filter @campusos/database seed:hr                    # Cycle 4 Step 0: 4 hr_employees rows (Mitchell/Rivera/Park/Hayes) + bridge UPDATEs that re-point soft FKs from iam_person.id to hr_employees.id
pnpm --filter @campusos/database seed:scheduling            # Cycle 5 Step 4: 2 bell schedules, 19 periods, 10 rooms, 6 timetable slots, 5 calendar events, 1 day override, 1 coverage request, 1 substitution row, 1 room booking
pnpm --filter @campusos/database seed:enrollment            # Cycle 6 Step 5: 2026-2027 academic year, 1 OPEN enrollment period, 2 streams, 2 capacities, 2 capacity_summary rows, 4 applications, 10 screening, 3 notes, 2 offers, 1 waitlist
pnpm --filter @campusos/database seed:payments              # Cycle 6 Step 5: 4 fee categories, 4 fee schedules, 1 stub Stripe acct, Chen family account FA-1001, 2 invoices (PAID + SENT), 1 CARD payment, 3 ledger entries (balance $400)
pnpm --filter @campusos/database seed:profile               # Cycle 6.1 Step 4: Chen Family household address+phone, role migration PARENT→HEAD_OF_HOUSEHOLD + STUDENT→CHILD, personal fields on 6 personas, 15 sis_student_demographics rows, David Chen guardian employment
pnpm --filter @campusos/database exec tsx src/build-cache.ts  # rebuild iam_effective_access_cache (run after any role/permission change)

# Rebuild from corrupted state (drops and re-provisions tenant schemas)
docker exec campusos-postgres psql -U campusos -d campusos_dev -c "DROP SCHEMA IF EXISTS tenant_demo CASCADE; DROP SCHEMA IF EXISTS tenant_test CASCADE;"
pnpm --filter @campusos/database provision --subdomain=demo
pnpm --filter @campusos/database provision --subdomain=test
pnpm --filter @campusos/database seed:sis        # idempotent: lookup-or-create on platform identities
pnpm --filter @campusos/database seed:classroom  # idempotent: skips if cls_assignments already populated
pnpm --filter @campusos/database seed:messaging  # idempotent: skips if msg_thread_types already populated
pnpm --filter @campusos/database seed:hr         # idempotent: skips per-employee if hr_employees row already exists; bridge UPDATEs are naturally no-ops once columns hold hr_employees.id
pnpm --filter @campusos/database seed:scheduling # idempotent: skips if sch_bell_schedules already populated
pnpm --filter @campusos/database seed:enrollment # idempotent: skips if enr_enrollment_periods already populated; creates 2026-2027 academic year if missing
pnpm --filter @campusos/database seed:payments   # idempotent: skips if pay_fee_categories already populated
pnpm --filter @campusos/database seed:profile     # idempotent: skips if Chen Family already has a HEAD_OF_HOUSEHOLD member
pnpm --filter @campusos/database exec tsx src/build-cache.ts

# Prisma studio (visual DB browser, platform schema only)
pnpm --filter @campusos/database studio
```

## Database

- **Platform schema** (~29 tables after Cycle 3 Step 3): organisations, schools, iam_person, platform_users, platform_students, platform_families, roles, permissions (**444 codes**), iam_scope, iam_role_assignment, iam_effective_access_cache, **platform_push_tokens (Cycle 3 Step 3)**, **platform_dlq_messages (Cycle 3 Step 3)**, and more. Managed by Prisma at `packages/database/prisma/platform/schema.prisma`.
- **Tenant schema** (114 base tables after Cycle 7 Step 1 — added 6 logical tsk_* tables (`tsk_acknowledgements`, `tsk_auto_task_rules`, `tsk_auto_task_conditions`, `tsk_auto_task_actions`, `tsk_tasks` RANGE-partitioned monthly 24 partitions, `tsk_tasks_archive` RANGE-partitioned annually 3 partitions — neither set of partition leaves counts as a logical base table); 108 after Phase 2 polish — added `sch_calendar_event_rsvps` and `sis_child_link_requests`; 106 after Cycle 6 Step 4 — added `pay_invoices`, `pay_invoice_line_items`, `pay_payments`, `pay_ledger_entries` (RANGE-partitioned annually 2025–2030 — 6 partitions, not counted as logical base tables), `pay_refunds`, `pay_payment_plans`, `pay_payment_plan_installments` in Cycle 6 Step 4; previous 99 base tables after Cycle 6 Step 3 — added `pay_family_accounts`, `pay_family_account_students`, `pay_fee_categories`, `pay_fee_schedules`, `pay_stripe_accounts` in Cycle 6 Step 3; previous 94 base tables after Cycle 6 Step 2 — added `enr_offers` and `enr_waitlist_entries` in Cycle 6 Step 2; previous 92 after Cycle 6 Step 1 — added `enr_enrollment_periods`, `enr_admission_streams`, `enr_intake_capacities`, `enr_capacity_summary`, `enr_applications`, `enr_application_screening_responses`, `enr_application_documents`, `enr_application_notes` in Cycle 6 Step 1; previous 84 base tables after Cycle 5 Step 3 — added `sch_calendar_events`, `sch_calendar_day_overrides`, `sch_coverage_requests`, `sch_substitution_timetable` in Cycle 5 Step 3; 80 after Step 2 with `sch_timetable_slots`, `sch_room_bookings`, `sch_room_change_requests` (plus the `btree_gist` extension); 77 after Step 1 with `sch_bell_schedules`, `sch_periods`, `sch_rooms`; previous 74 from Cycle 4 Step 4 with `hr_employees` in Step 0; `hr_positions`, `hr_employee_positions`, `hr_emergency_contacts`, `hr_document_types`, `hr_employee_documents` in Step 1; `hr_leave_types`, `hr_leave_balances`, `hr_leave_requests` in Step 2; `hr_staff_certifications`, `hr_training_requirements`, `hr_training_compliance`, `hr_cpd_requirements`, `hr_work_authorisation` in Step 3; `hr_onboarding_templates`, `hr_onboarding_checklists`, `hr_onboarding_tasks` in Step 4): 5 from Cycle 0 foundation (school_config, school_feature_flags, grading_scales, custom_field_definitions, custom_field_values) + 18 SIS tables from Cycle 1 (sis_academic_years, sis_terms, sis_departments, sis_courses, sis_classes, sis_class_teachers, sis_enrollments, sis_families, sis_students, sis_staff, sis_guardians, sis_student_guardians, sis_family_members, sis_emergency_contacts, sis_student_notes, sis_absence_requests, sis_attendance_records, sis_attendance_evidence) + 15 Classroom tables from Cycle 2 (cls_lesson_types, cls_lessons, cls_assignment_types, cls_assignment_categories, cls_assignments, cls_assignment_questions, cls_answer_key_entries, cls_submissions, cls_submission_question_grades, cls_ai_grading_jobs, cls_grades, cls_gradebook_snapshots, cls_report_cards, cls_report_card_entries, cls_student_progress_notes) + 6 Communications messaging tables from Cycle 3 Step 1 (msg_thread_types, msg_threads, msg_thread_participants, msg_messages, msg_message_attachments, msg_message_reads) + 7 Communications notification/announcement tables from Cycle 3 Step 2 (msg_alert_types, msg_notification_queue, msg_notification_preferences, msg_notification_log, msg_announcements, msg_announcement_audiences, msg_announcement_reads) + 6 Communications moderation/support tables from Cycle 3 Step 3 (msg_moderation_policies, msg_moderation_log, msg_tags, msg_user_tags, msg_user_blocks, msg_admin_access_log) + 17 HR tables from Cycle 4 (hr_employees + hr_positions + hr_employee_positions + hr_emergency_contacts + hr_document_types + hr_employee_documents + hr_leave_types + hr_leave_balances + hr_leave_requests + hr_staff_certifications + hr_training_requirements + hr_training_compliance + hr_cpd_requirements + hr_work_authorisation + hr_onboarding_templates + hr_onboarding_checklists + hr_onboarding_tasks) + 3 Scheduling tables from Cycle 5 Step 1 (sch_bell_schedules, sch_periods, sch_rooms) + 3 Scheduling tables from Cycle 5 Step 2 (sch_timetable_slots with two btree_gist EXCLUSION constraints catching teacher and room double-booking, sch_room_bookings, sch_room_change_requests) + 4 Scheduling tables from Cycle 5 Step 3 (sch_calendar_events with multi-column time-consistency CHECK and 8-value event_type CHECK, sch_calendar_day_overrides, sch_coverage_requests with 3-branch assignment_chk and UNIQUE(timetable_slot_id, coverage_date), sch_substitution_timetable with ON DELETE CASCADE on coverage_request_id) + 8 Enrollment tables from Cycle 6 Step 1 (enr_enrollment_periods, enr_admission_streams, enr_intake_capacities, enr_capacity_summary, enr_applications, enr_application_screening_responses, enr_application_documents, enr_application_notes) + 2 Enrollment tables from Cycle 6 Step 2 (enr_offers, enr_waitlist_entries) + 5 Payments tables from Cycle 6 Step 3 (pay_family_accounts, pay_family_account_students, pay_fee_categories, pay_fee_schedules, pay_stripe_accounts) + 7 Payments tables from Cycle 6 Step 4 (pay_invoices, pay_invoice_line_items, pay_payments, pay_ledger_entries with composite PK (id, created_at) and DB-enforced FK family_account_id replicating onto each of the 6 annual partitions, pay_refunds, pay_payment_plans, pay_payment_plan_installments). Plus 36 partition objects under sis_attendance_records (4 year partitions × 8 hash leaves + 4 year parents) + 64 hash partitions of msg_threads + 24 monthly partitions of msg_messages (2025-08 → 2027-08) + 24 monthly partitions of msg_notification_log (2025-08 → 2027-08) + 24 monthly partitions of msg_moderation_log (2025-08 → 2027-08) + 6 annual partitions of pay_ledger_entries (2025–2030) + 24 monthly partitions of tsk_tasks (2025-08 → 2027-08) + 3 annual partitions of tsk_tasks_archive (2025/2026/2027).
- Tenant migrations are SQL files in `packages/database/prisma/tenant/migrations/`, split by semicolons, each statement executed individually by `provision-tenant.ts`. **Caveat:** statements that start with `--` after trim are filtered out — keep header comments minimal or use `/* … */`. **Caveat 2:** never put a `;` inside a string literal (e.g. inside a `COMMENT ON COLUMN ... IS '...'` value or a `DEFAULT` expression) — the splitter will cut the statement mid-string. Use commas / "and" instead. Tripped this on `007_msg_messaging.sql`.
- Tenant SQL must be idempotent: use `CREATE TABLE IF NOT EXISTS`, `CREATE INDEX IF NOT EXISTS`, and `DROP CONSTRAINT IF EXISTS … ; ADD CONSTRAINT …` for FK changes (Postgres has no `ADD CONSTRAINT IF NOT EXISTS`).
- Enum-like columns use `TEXT + CHECK IN (…)` rather than PG `ENUM` types — `CREATE TYPE` isn't idempotent under the SQL splitter.

## Test Users (seeded, Keycloak)

| Email                        | Role                                 | Password      |
| ---------------------------- | ------------------------------------ | ------------- |
| admin@demo.campusos.dev      | Platform Admin (all 444 permissions) | admin123      |
| principal@demo.campusos.dev  | School Admin (Sarah Mitchell)        | admin123      |
| teacher@demo.campusos.dev    | Teacher (James Rivera)               | teacher123    |
| student@demo.campusos.dev    | Student (Maya Chen)                  | student123    |
| parent@demo.campusos.dev     | Parent (David Chen, Maya's father)   | parent123     |
| vp@demo.campusos.dev         | Vice Principal (Linda Park)          | vp123         |
| counsellor@demo.campusos.dev | Counsellor (Marcus Hayes)            | counsellor123 |

**Cycle 4 Step 0 note:** The `admin@` Platform Admin persona is intentionally NOT bridged to `hr_employees` — it represents a system administrator, not a school employee. The other four staff (Mitchell via `principal@`, Rivera, Park, Hayes) each have an `hr_employees` row.

Dev login: `POST /api/v1/auth/dev-login` with `{"email":"..."}` and `X-Tenant-Subdomain: demo` header.

## Design Documents (authoritative references)

Read these when you need table definitions, column details, ADR specifics, or permission descriptions:

- `docs/campusos-erd-v11.html` — Complete schema: all 840 tables with full column definitions, indexes, constraints, Kafka events, ADR cross-references
- `docs/campusos-architecture-review-v10.html` — 30 sections: system architecture, multi-tenancy, IAM, events, scalability, security
- `docs/campusos-function-library-v11.html` — 148 functions, 28 groups, 3 access tiers each
- `docs/campusos-dev-deployment-plan.html` — Build pipeline, environments, Wave 1 sequence
- `docs/campusos-business-strategy.html` — Pricing, team, GTM, community exchange
- `docs/campusos-cycle1-implementation-plan.html` — Cycle 1 plan: 11 steps for SIS + Attendance
- `docs/cycle1-cat-script.md` — Cycle 1 Customer Acceptance Test script (the Step 11 deliverable; reproducible end-to-end walkthrough)
- `docs/campusos-cycle2-implementation-plan.html` — Cycle 2 plan: 10 steps for Classroom + Assignments + Grading
- `docs/cycle2-cat-script.md` — Cycle 2 Customer Acceptance Test script (the Step 10 deliverable; live API + worker walkthrough)
- `docs/campusos-cycle3-implementation-plan.html` — Cycle 3 plan: 11 steps for Communications (messaging, notifications, announcements, moderation)
- `docs/cycle3-cat-script.md` — Cycle 3 Customer Acceptance Test script (the Step 11 deliverable; 7 scenarios end-to-end across Kafka consumers + delivery worker + audience fan-out + content moderation)
- `docs/campusos-cycle4-implementation-plan.html` — Cycle 4 plan: 10 steps for HR & Workforce Core (employees, positions, leave, certifications, training compliance, onboarding) + Step 0 HR-Employee Identity Migration
- `docs/cycle4-cat-script.md` — Cycle 4 Customer Acceptance Test script (the Step 10 deliverable; 4 bridge-verification checks + 9 plan scenarios end-to-end across the HR-Employee identity bridge, the leave-approval workflow, the LeaveNotificationConsumer's `hr.leave.coverage_needed` republish for Cycle 5 Scheduling, the compliance dashboard amber row, and 6 permission denial paths)
- `docs/campusos-cycle5-implementation-plan.html` — Cycle 5 plan: 10 steps for Scheduling & Calendar (bell schedules, timetable with btree_gist EXCLUSIONs, rooms + bookings, calendar + day overrides, coverage requests + substitution timetable, CoverageConsumer)
- `docs/cycle5-cat-script.md` — Cycle 5 Customer Acceptance Test script (the Step 10 deliverable; 4-check schema preamble + 10 plan scenarios end-to-end across the timetable read paths for admin/teacher/parent, EXCLUSION 23P01 → 409 translation, the seeded coverage row, the live leave→CoverageConsumer→OPEN-rows flow with 6 rows landed in 3s, the Assign substitute path with `sch.coverage.assigned` envelope captured on the wire, the calendar override resolution chain, the booking-vs-booking AND booking-vs-timetable conflict gates, and 6 permission denial paths)
- `docs/campusos-cycle6-implementation-plan.html` — Cycle 6 plan: 12 steps for Enrollment & Payments (M81 enrollment pipeline + M84 family billing engine; 22 of the 60+ ERD tables across the two modules; vertical slice from application-submission to invoice-payment via the `enr.student.enrolled` event consumed by the `PaymentAccountWorker`)
- `docs/cycle6-cat-script.md` — Cycle 6 Customer Acceptance Test script (the Step 12 deliverable; 4-check schema preamble + 12 plan scenarios end-to-end across the parent-application submit → admin-transition → admin-offer → parent-accept → `enr.student.enrolled` envelope + PaymentAccountWorker idempotent reaction → admin billing reads → parent partial-then-final pay → admin partial-refund + 3 ADR-057 envelopes captured live + 6 permission denials across all 3 tiers)

## Conventions

- Tenant-scoped tables use SQL migrations in `packages/database/prisma/tenant/migrations/`
- Platform tables use Prisma schema in `packages/database/prisma/platform/schema.prisma`
- NestJS modules follow the pattern: module.ts, service.ts, controller.ts, dto/ in `apps/api/src/<domain>/`
- Every API endpoint needs `@RequirePermission()` unless marked `@Public()`. New global guards must be registered in `AppModule` (not in submodules) so guard ordering stays deterministic
- Use `TenantPrismaService.executeInTenantContext(fn)` for **single-statement** tenant queries (read or single-table write). Internally runs inside a `$transaction` with `SET LOCAL search_path` to keep tenant scope pinned to one connection — never use a session-level SET on a pooled client.
- Use `TenantPrismaService.executeInTenantTransaction(fn)` for **multi-statement** writes that must be atomic (e.g. cross-schema inserts that span platform + tenant tables, like `POST /students`)
- **State-machine transitions must lock the row inside the transaction.** Any endpoint that reads a status, validates it, then updates it MUST use `SELECT ... FOR UPDATE` inside `executeInTenantTransaction`. Reading status outside the transaction and updating inside it is a concurrency bug — two simultaneous requests can both pass the check and both apply mutations. This pattern has been caught in Cycles 4, 5, and 6 reviews (leave approve/reject/cancel, room booking, room change requests, day override creation, and Cycle 6's invoice cancel ledger reverse + refund-driven invoice status reconcile). Every future lifecycle transition must follow the locked-read pattern from the start.
- **Cross-table lock ordering must be consistent.** When a service touches more than one row (e.g. an invoice + a payment in `PaymentService.pay` and `RefundService.issue`), every code path must acquire the locks in the same order to avoid deadlock. Cycle 6 settled on **invoice first, then payment** — `PaymentService.pay` locks the invoice with `FOR UPDATE` and writes a payment row inside the same tx; `RefundService.issue` locks the parent invoice **before** the payment row for the same reason. Concurrent transitions on different orderings deadlock under load (Postgres detects + aborts one). When adding a new write path that touches both tables, follow the existing order.
- **Concurrency hot-spots use `pg_advisory_xact_lock`.** Where a schema UNIQUE / EXCLUSION can't catch a race because the new row depends on aggregating existing rows (e.g. `pay_family_accounts.account_number = MAX+1`, the existence-check in `generateFromSchedule`, `enr_capacity_summary.recompute`, room booking conflict checks), services take a per-key advisory tx lock at the top of the relevant tx. Key shape is always `hashtext('<resource>:' || <id>)` so locks across resources cannot collide. The schema constraint remains the belt-and-braces. The pattern lives in `apps/api/src/scheduling/room-booking.service.ts`, `apps/api/src/payments/invoice.service.ts::generateFromSchedule`, `apps/api/src/payments/consumers/payment-account.consumer.ts::createOrLinkAccount`, and `apps/api/src/enrollment/capacity-summary.service.ts::recompute`.
- Multi-persona reads (any endpoint where multiple personType values hold the gating permission) MUST resolve the caller via `ActorContextService.resolveActor(...)` and apply a row-level filter. Don't trust `@RequirePermission` alone for row scope. Pattern: see `apps/api/src/sis/student.service.ts::visibilityClause` (parent → linked children, student → self, teacher → assigned-class enrollments, admin → no filter).
- Class-bound writes (attendance, grading) MUST gate on the link table (e.g. `sis_class_teachers`) before mutating; school admins bypass. The `att-001:write` code is held school-wide by every teacher, so the link-table check is the actual access gate.
- Admin status comes from the **current tenant's scope chain**, never across all cached scopes. Use `permissionCheckService.hasAnyPermissionInTenant(accountId, tenant.schoolId, codes)` or read `actor.isSchoolAdmin`. The previous `hasAnyPermissionAcrossScopes` helper has been removed for this reason.
- Tenant tables aren't in the Prisma schema — query via `client.$queryRawUnsafe<RowType[]>(sql, ...args)` / `client.$executeRawUnsafe(sql, ...args)`. Always cast UUID args explicitly: `$1::uuid`. Same for `$1::date`. Prisma sends raw query parameters as TEXT and Postgres won't auto-coerce
- Schema-qualify cross-schema reads (`platform.iam_person`) to be explicit
- DTOs use `class-validator` + `class-transformer` (global ValidationPipe in `main.ts`). The `packages/shared` Zod option is unused so far
- Kafka events follow `{domain}.{entity}.{verb}` naming (e.g. `att.student.marked_tardy`). Wire topics get an env prefix via `prefixedTopic()` (e.g. `dev.att.student.marked_tardy`) controlled by `KAFKA_TOPIC_ENV`. Producers + consumers MUST use `prefixedTopic()` so a misconfigured env can't cross-pollinate environments.
- **ADR-057 canonical event envelope** (Cycle 3 Step 0). Every emit goes through `KafkaProducerService.emit(EmitOptions)`. The body on the wire is an `EventEnvelope` JSON object with `event_id` (UUIDv7), `event_type` (un-prefixed topic), `event_version` (default 1), `occurred_at`, `published_at`, `tenant_id`, `source_module`, `correlation_id`, `payload`. Consumers read `event_id` + `tenant_id` straight off the envelope; the legacy transport headers (`event-id`, `tenant-id`, `tenant-subdomain`) are still set on every message for backward compatibility. New consumers should read from the envelope; the GradebookSnapshotWorker reads envelope-first with header fallback.
- No DROP TABLE, no DROP COLUMN in migrations. Additive only. (Pre-deployment edits to fix architectural errors are categorically different — re-provision the tenant.)
- Snake_case in SQL, camelCase in TypeScript. Map at the service layer with a `rowToDto` helper
- **Web auth gating uses `personType` + permission codes from `/auth/me`** for menu visibility and persona routing only. Backend `PermissionGuard` is the authoritative access check on every request.
- **Web fetch wrapper (`apps/web/src/lib/api-client.ts`)** sends `X-Tenant-Subdomain: demo` (override via `NEXT_PUBLIC_TENANT_SUBDOMAIN`) and Bearer token. On 401 it single-flights `/auth/refresh` and retries once; on terminal 401 it calls the registered `onUnauthenticated` handler which clears state and routes to `/login`.

## Claude Code Operating Rules

After completing each step and before each commit:

1. Update this CLAUDE.md to reflect current status, new conventions, new commands, and any schema changes. The "Project Status" section must always state exactly which steps are done and which remain.
2. Update the active HANDOFF document (currently HANDOFF-CYCLE1.md) with any new tables, endpoints, seed data changes, deviations from the ERD, bug fixes, or architecture decisions. Update the step status table. Document what was built in the same level of detail as the existing completed steps.
3. Include both files in every commit.

These two files are the source of truth that external architecture reviewers read. If they are stale, reviewers cannot do their job. A step is NOT complete until both files are current. Treat updating these files as part of the definition of done, not as a follow-up task.

When starting a new cycle, create the new HANDOFF-CYCLE{N}.md from the template structure used in HANDOFF-CYCLE1.md before beginning Step 1.

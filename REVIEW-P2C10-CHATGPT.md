# REVIEW-P2C10-CHATGPT — fix log

**Round 2 verdict (`9ca4aa5` + `730b2b3`):** FAIL — 3 new BLOCKING in `PreorderService` (P2-10a fixes confirmed PASS but the same broad-STAFF + cross-school patterns hadn't propagated to P2-10b).
**Round 2 fix commit:** this commit.
**Round 3 verdict:** pending.

The reviewer ran the gate against the full P2-10 cycle this round. P2-10a fixes at `730b2b3` PASS. P2-10b (`9ca4aa5`) `PreorderService` surfaces 3 new BLOCKINGs: (1) student/guardian validation not school-scoped, (2) FSM admin on-behalf doesn't validate student in current tenant, (3) PreorderService still uses `personType === 'STAFF'` for window CRUD / confirm / cancel / generate-report / visibility / window-gate bypass. The Round 2 fix commit lands all 3 BLOCKINGs + 7 new pinned regression tests so the contracts cannot regress.

## Round 2 triage table

| #              | Reviewer finding                                                                | Verdict           | Action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Live verification                                                                                                                                                                                                                                                                                                                                                             |
| -------------- | ------------------------------------------------------------------------------- | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **BLOCKING 1** | Preorder student/guardian authorization is not school-scoped                    | **VALID — FIXED** | `PreorderService.assertCanOrderForStudent` STUDENT branch SQL rewrites from `SELECT 1 FROM sis_students s JOIN platform.platform_students ps WHERE s.id = $1::uuid AND ps.person_id = $2::uuid` to `WHERE s.school_id = $1::uuid AND s.id = $2::uuid AND ps.person_id = $3::uuid`. GUARDIAN branch SQL rewrites to add `JOIN sis_students s ON s.id = sg.student_id AND s.school_id = $1::uuid`. `buildVisibilityFilter` STUDENT + GUARDIAN branches mirror the same school predicate. A student / guardian whose platform identity also exists at another school can no longer order under THIS tenant against a foreign-school sis_students row. | 2 new pinned regression tests under `REVIEW-P2C10 ROUND 2 — BLOCKING 1`: (a) STUDENT branch SQL captured + asserted to contain `s.school_id = $1::uuid AND s.id = $2::uuid AND ps.person_id = $3::uuid`; (b) GUARDIAN sub-query SQL captured + asserted to JOIN sis_students with `s.school_id = $1::uuid` and bind `sg.student_id = $2::uuid AND g.person_id = $3::uuid`.    |
| **BLOCKING 2** | FSM admin on-behalf ordering doesn't validate student belongs to current school | **VALID — FIXED** | `assertCanOrderForStudent` admin path NO LONGER blanket-bypasses. The FSM admin branch (school admin OR holds FDS-006:write) now runs `SELECT 1 AS ok FROM sis_students WHERE school_id = $1::uuid AND id = $2::uuid LIMIT 1` and throws `BadRequestException('studentId does not match a student in this school')` on miss. A School A FSM admin providing a School B student UUID can no longer mint a preorder with `school_id = $A` + `student_id = $B`.                                                                                                                                                                                       | 1 new pinned regression test under `REVIEW-P2C10 ROUND 2 — BLOCKING 2`: admin actor with cross-school studentId is refused 400 + the INSERT into fds_meal_preorders never fires (defence-in-depth check on captured SQL).                                                                                                                                                     |
| **BLOCKING 3** | PreorderService still uses `personType === 'STAFF'` for FSM admin operations    | **VALID — FIXED** | PreorderService constructor now takes `PermissionCheckService`. New `isFsmAdmin(actor)` helper short-circuits on `actor.isSchoolAdmin` then calls `permissionCheckService.hasAnyPermissionInTenant(actor.accountId, schoolId, ['fds-006:write'])`. Every callsite that previously checked `personType === 'STAFF'` rewrites to `await this.isFsmAdmin(actor)`: `assertCanManageWindows` (window CRUD), `confirmPreorder` admin gate, `cancelPreorder` admin override path, `generateProductionReport` admin gate, `buildVisibilityFilter` list-all bypass, `createPreorder` window-gate bypass, `assertCanOrderForStudent` admin on-behalf gate.   | 4 new pinned regression tests under `REVIEW-P2C10 ROUND 2 — BLOCKING 3` — STAFF actor with `allow=false` permCheck stub (no FDS-006:write held) is refused on `createWindow` / `confirmPreorder` / `generateProductionReport` / on-behalf order through a closed window. Mirrors the P2-10a fix that the same review surfaced for recipe / inventory / transfer / staff-meal. |

## Files in this commit

| File                                                      | Change                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/api/src/food-service/preorder.service.ts`           | New `isFsmAdmin(actor)` helper; constructor adds `PermissionCheckService`; `assertCanManageWindows` + `confirmPreorder` + `cancelPreorder` + `generateProductionReport` + `buildVisibilityFilter` + `createPreorder` window-gate-bypass + `assertCanOrderForStudent` all rewrite to use the new helper; admin path validates studentId in current tenant; STUDENT + GUARDIAN sub-queries add `s.school_id = $tenant.schoolId` predicates |
| `apps/api/src/food-service/food-service-advanced.spec.ts` | Existing GUARDIAN / STUDENT tests updated to use `makePermCheck({ allow: false })` so they hit the GUARDIAN / STUDENT branches; admin-bypass-window-gate test gains matcher returning the row for the new student-validation query; 7 new pinned regression tests under `REVIEW-P2C10 ROUND 2 …` describe blocks                                                                                                                         |
| `REVIEW-P2C10-CHATGPT.md`                                 | this file                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `CLAUDE.md`                                               | Round 2 fix log appended                                                                                                                                                                                                                                                                                                                                                                                                                 |

## CI parity

- format:check + lint:logs (708 files clean) + api build clean + web build clean + **525/525 vitest tests passing** (was 518 pre-Round 2 fix; +7 new pinned regression tests).
- No schema migrations in Round 2 (the only schema delta is the FDS-006 catalogue entry added in Round 1).

## What follows

Awaiting Round 3 verdict. P2-10a fixes (`730b2b3`) already confirmed PASS by the reviewer at the top of Round 2; if Round 3 returns PASS on Round 2 fix commit, the full P2-10 cycle is approved.

---

**Round 1 verdict (P2-10a `a91b192`):** FAIL — 4 BLOCKING + 4 MAJOR.
**Round 1 fix commit:** `730b2b3` — confirmed PASS by reviewer at top of Round 2.

The reviewer ran the gate against the P2-10a Recipes + Inventory slice (`a91b192`) — the planning commit `3775ad0` was documentation-only. The fix commit lands all 4 BLOCKING + the 1 actionable MAJOR (MAJOR 5 staff-meal patch) + 11 new pinned regression tests. MAJORs 6 / 7 / 8 are recommendation-class (defensive transfer joins, DB trigger on inventory_transactions, payroll settlement event) and move to the Phase 2 punch list.

## Triage table

| #              | Reviewer finding                                                  | Verdict                        | Action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Live verification                                                                                                                                                                                                                                                                                                                                                                                                     |
| -------------- | ----------------------------------------------------------------- | ------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **BLOCKING 1** | Recipe ingredient mutations are not school-scoped                 | **VALID — FIXED**              | `RecipeService.addIngredient` FOR UPDATE lock now reads `WHERE id = $1::uuid AND school_id = $2::uuid`. `updateIngredient` + `deleteIngredient` JOIN now reads `JOIN fds_recipes r ON r.id = i.recipe_id AND r.school_id = $2::uuid`. New `assertInventoryItemBelongsToSchool` helper validates `input.inventoryItemId` against `fds_inventory_items.school_id = $tenant.schoolId` on both add + update paths.                                                                                                                                                                                                                                                                | 3 new pinned regression tests under `REVIEW-P2-10a ROUND 1 — BLOCKING 1`: (a) addIngredient lock SQL contains `WHERE id = $1::uuid AND school_id = $2::uuid`; (b) addIngredient with cross-school inventoryItemId → 400 "does not match an inventory item in this school"; (c) updateIngredient JOIN contains `AND r.school_id = $2::uuid`.                                                                           |
| **BLOCKING 2** | Existing inventory level movement can mutate foreign-school stock | **VALID — FIXED**              | Both `InventoryService.movement` + `InventoryService.stocktake` rewrite the existing-level lock from `JOIN fds_inventory_items i WHERE l.group_id = $1 AND l.item_id = $2 FOR UPDATE OF l` to `JOIN fds_inventory_groups g ON g.id = l.group_id JOIN fds_inventory_items i ON i.id = l.item_id WHERE l.group_id = $1::uuid AND l.item_id = $2::uuid AND g.school_id = $3::uuid AND i.school_id = $3::uuid FOR UPDATE OF l`. A School A actor providing a `(groupId, itemId)` pair that exists in School B now collapses to 404 + "Inventory item or group not found for this school" instead of mutating the foreign-school level row.                                        | 1 new spec under `REVIEW-P2-10a ROUND 1 — BLOCKING 2` capturing the actual lock SQL and asserting it contains the 2 new JOINs + the 2 new `school_id = $3::uuid` predicates.                                                                                                                                                                                                                                          |
| **BLOCKING 3** | `fds.inventory.low` is best-effort after commit, not durable      | **VALID — FIXED**              | New `deterministicInventoryLowEventId(transactionId)` helper produces a v5-shaped UUID via `sha256(transactionId + ':fds.inventory.low:v1')` with the v5 marker nibbles set (same shape as P2-4a `deterministicPayrollEventId` + P2-6 `deterministicCreditNoteEventId`). The emit path now lives INSIDE `executeInTenantTransaction` via `OutboxService.enqueueInTx`, so the outbox row commits with the parent tx and `OutboxPublisherWorker` publishes when the broker is back. The post-tx `emitInventoryLow` helper that called `KafkaProducerService.emit` is removed. `InventoryService` constructor now takes `OutboxService` instead of `KafkaProducerService`.       | 2 new specs under `REVIEW-P2-10a ROUND 1 — BLOCKING 3`: (a) `deterministicInventoryLowEventId` is stable (same input → same id) + v5-shaped (`/^[0-9a-f]{8}-[0-9a-f]{4}-5[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/`) + different inputs produce different ids; (b) USAGE crossing reorder_threshold downward enqueues to outbox (not kafka.emit) with full ADR-057 payload + the deterministic v5-shaped event_id. |
| **BLOCKING 4** | Generic STAFF is treated as full Food Service Manager authority   | **VALID — FIXED**              | New `FDS-006 (Food Service Administration)` permission code added to `packages/database/data/permissions.json` (catalogue 350 → 352). New shared helper `assertFsmAdminScope` in `apps/api/src/food-service/fsm-scope.ts` short-circuits on `actor.isSchoolAdmin` then calls `permissionCheckService.hasAnyPermissionInTenant(accountId, schoolId, ['fds-006:write'])`. Every `assertCanManage()` in `RecipeService` / `InventoryService` / `TransferService` / `StaffMealService` rewrites from `personType === 'STAFF'` to the new helper. `seed-iam.ts` grants Staff role `FDS-006:read+write`; admin tiers reach via `everyFunction`. Teacher/Parent/Student NOT granted. | 4 new specs under `REVIEW-P2-10a ROUND 1 — BLOCKING 4` — generic STAFF actor with `allow=false` permCheck stub is refused 403 on recipe / inventory / transfer / staff-meal create paths. Live cache verification via `iam_effective_access_cache`: admin/principal/vp/counsellor hold `fds-006:write`; teacher/parent/student all FALSE.                                                                             |
| **MAJOR 5**    | Staff meal `patch()` updates by ID only                           | **VALID — FIXED**              | `StaffMealService.patch` UPDATE now predicates on both `id` AND `school_id` (was id-only + relied on school-scoped reload to 404 a cross-school hit, but the mutation already landed). The schoolId placeholder is captured via `params.length` so the UPDATE SQL ends with `WHERE id = $N::uuid AND school_id = $M::uuid` regardless of which optional fields were patched.                                                                                                                                                                                                                                                                                                  | 1 new spec under `REVIEW-P2-10a ROUND 1 — MAJOR 5` captures the UPDATE SQL and asserts it ends with `AND school_id = $\d+::uuid`.                                                                                                                                                                                                                                                                                     |
| **MAJOR 6**    | Transfer completion level updates could be school-defensive       | **ACCEPTED — Phase 2 backlog** | Transfer creation already validates both groups and item against current school AND completion locks the transfer-request row by `(id, school_id)` — so the request shape is verified before the level UPDATE fires. Adding defensive school joins on the level lock is recommendation-class polish; carried to Phase 2 punch list.                                                                                                                                                                                                                                                                                                                                           | n/a — no code change this cycle.                                                                                                                                                                                                                                                                                                                                                                                      |
| **MAJOR 7**    | Inventory transaction immutability is service-side only           | **ACCEPTED — Phase 2 backlog** | The schema-level service discipline (no UPDATE / no DELETE path exposed) holds today. A DB-level `BEFORE UPDATE OR DELETE` trigger is stronger but lands once per-tenant DB role model exists. Joins the broader "immutability triggers" punch list with Cycle 8 `tkt_ticket_activity` + Cycle 10 `hlth_health_access_log` + Cycle 21 `fac_work_order_activity`.                                                                                                                                                                                                                                                                                                              | n/a — no code change this cycle.                                                                                                                                                                                                                                                                                                                                                                                      |
| **MAJOR 8**    | Staff meal payroll deduction integration is pull-only             | **ACCEPTED — Phase 2 backlog** | `payrollDeductions()` is a read aggregator the P2-4 payroll job consumes. The plan to add a durable `fds.staff_meal.settled` event marking each settled period is recommendation-class and depends on the payroll integration contract; carried to P2-10b (or P2-4 payroll harden) as a follow-up.                                                                                                                                                                                                                                                                                                                                                                            | n/a — no code change this cycle.                                                                                                                                                                                                                                                                                                                                                                                      |

## Files in this commit

| File                                                      | Change                                                                                                                                                                                                                                                                   |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `packages/database/data/permissions.json`                 | +FDS-006 (Food Service Administration)                                                                                                                                                                                                                                   |
| `packages/database/src/seed-iam.ts`                       | Staff role gains FDS-006:read+write with REVIEW-P2-10a comment block                                                                                                                                                                                                     |
| `apps/api/src/food-service/fsm-scope.ts`                  | NEW — shared `assertFsmAdminScope(permCheck, actor, surface)` helper                                                                                                                                                                                                     |
| `apps/api/src/food-service/recipe.service.ts`             | BLOCKING 1 — addIngredient/updateIngredient/deleteIngredient school-scope + inventoryItemId validation; BLOCKING 4 — FDS-006:write gate                                                                                                                                  |
| `apps/api/src/food-service/inventory.service.ts`          | BLOCKING 2 — movement + stocktake existing-level lock JOINs groups + items with school predicates; BLOCKING 3 — durable outbox emit with deterministicInventoryLowEventId; BLOCKING 4 — FDS-006:write gate; constructor swaps Kafka→Outbox + adds PermissionCheckService |
| `apps/api/src/food-service/transfer.service.ts`           | BLOCKING 4 — FDS-006:write gate; constructor adds PermissionCheckService                                                                                                                                                                                                 |
| `apps/api/src/food-service/staff-meal.service.ts`         | BLOCKING 4 — FDS-006:write gate; MAJOR 5 — patch UPDATE adds AND school_id = $M::uuid predicate; constructor adds PermissionCheckService                                                                                                                                 |
| `apps/api/src/food-service/food-service-advanced.spec.ts` | 11 new pinned regression tests under REVIEW-P2-10a ROUND 1 describe blocks; existing tests updated for new constructor signatures + outbox                                                                                                                               |
| `REVIEW-P2C10-CHATGPT.md`                                 | this file                                                                                                                                                                                                                                                                |

## CI parity

- format:check + lint:logs (708 files clean) + api build clean + web build clean + **518/518 vitest tests passing** (was 507 pre-Round 1 fix; +11 new pinned regression tests).
- Live IAM cache rebuilt + verified: admin/principal/vp/counsellor hold `fds-006:write`; teacher/parent/student all FALSE.
- Tenant migrations unchanged from P2-10a + P2-10b (no schema delta in the Round 1 fix).

## What follows

Awaiting Round 2 verdict. If APPROVED, tag `p2c10-complete` at the Round 1 fix commit and `p2c10-approved` at the closeout commit (per the Wave B convention).

**MAJORs 6 / 7 / 8** join the Phase 2 punch list — already noted in CLAUDE.md as part of the broader role-split + DB-trigger immutability + payroll-integration backlog.

---

**Status (historical):** Round 1 review scaffold preserved below.
**Cycle:** Phase 2 Cycle 10 (P2-10 Food Service Advanced — closes Wave B).
**Commits to review (in order):**

- `a91b192` — feat(food-service): P2-10a Recipes + Inventory (8 tables, 22 endpoints)
- _this commit_ — feat(food-service): P2-10b Pre-Orders + Production + Allergen Consumer (4 new tables + 1 ALTER, 11 endpoints, closes Wave B)

## Plan reference

`docs/campusos-p2c10-food-service-advanced.html`. The plan called for 15 tables across 2 sub-cycles. Actual delta: **12 new tables + 1 ALTER**, because 3 of the 15 planned tables (`fds_eligibility_applications`, `fds_usda_reimbursement_claims`, `fds_production_records`) already exist from Cycle 20 migration `070_fds_dietary_eligibility.sql`. The P2-10b ALTER on `fds_production_records` adds the new `recipe_id` link to P2-10a `fds_recipes`. The plan's "AllergenAlertConsumer" is the existing Cycle 20 `AllergyAlertConsumer` (REVIEW-FINAL-2026-05-07 MAJ-5.1).

## What changed in P2-10b (this commit)

| Surface                             | Detail                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Migration `135_fds_preorders.sql`   | 4 new base tables (`fds_preorder_windows`, `fds_meal_preorders`, `fds_meal_preorder_items`, `fds_preorder_production_reports`) + ALTER `fds_production_records ADD COLUMN recipe_id` (FK to `fds_recipes`, SET NULL). Splitter-safe — Python audit reports 0 stray `;` in strings/comments. Provisioned cleanly to both `tenant_demo` and `tenant_test` on first attempt. **33rd migration in a row** to clear the splitter trap on first attempt after audit (Cycles 4 onwards).            |
| PreorderService (new)               | 10 endpoints + 1 GET-list under `fds-005:read` / `fds-005:write`. **THE ALLERGEN CROSS-CHECK KEYSTONE** lives in `createPreorder`: load active `fds_student_allergen_alerts` for the student, intersect each item's `menu_item.allergen_codes`, throw 422 ConflictException on any CRITICAL match (carries the offending codes), surface WARNING matches in `warning_allergens` (the order still persists). Mirrors the Cycle 20 POS cross-check but applied upstream at the preorder layer. |
| Production report aggregation       | `generateProductionReport` aggregates CONFIRMED preorders into per-menu-item totals + per-allergen affected-order counts. UNIQUE(school_id, service_date, meal_type) means regeneration UPSERTs in place via `ON CONFLICT DO UPDATE`.                                                                                                                                                                                                                                                        |
| Row-scope visibility                | `buildVisibilityFilter` for STUDENT joins `platform_students.person_id` → `sis_students.id`; for GUARDIAN joins through `sis_student_guardians + sis_guardians.person_id`. Mirrors `AllergenAlertService` from Cycle 20.                                                                                                                                                                                                                                                                     |
| `assertCanOrderForStudent`          | Submit-time row-scope check: STUDENT may only order for self; GUARDIAN may only order for own children; admin/STAFF order on behalf of any tenant student. Refusals are 403 (in contrast to row-scope reads which collapse to 404 to avoid existence leakage).                                                                                                                                                                                                                               |
| Window gate                         | Non-admin orders refused on closed windows (`opens_at <= now() <= closes_at`). Admin/STAFF bypass.                                                                                                                                                                                                                                                                                                                                                                                           |
| Confirm path                        | Locks the row + flips PENDING → CONFIRMED inside a tx. Refuses if `allergen_check_passed = false` (defensive — the `createPreorder` keystone already rejects CRITICAL matches before the row lands). Admin/STAFF only.                                                                                                                                                                                                                                                                       |
| Cancel path                         | Stamps `cancelled_at` + `cancellation_reason` atomically. Owner-or-admin row-scope. Already-CANCELLED is a no-op.                                                                                                                                                                                                                                                                                                                                                                            |
| Seed `seed-food-service-advanced-b` | 2 preorder windows (tomorrow LUNCH open + next-week BREAKFAST upcoming), 5 preorders (3 CONFIRMED, 1 PENDING, 1 CANCELLED), 11 line items, 1 production report aggregating the 3 CONFIRMED preorders. Idempotent re-run skips cleanly.                                                                                                                                                                                                                                                       |
| IAM grants                          | `FDS-005:read+write` granted to Staff (FSM), Parent (parent-active surface — submit preorders for own children), Student (own preorders), and admin tiers via `everyFunction`. Verified live via `iam_effective_access_cache`: admin/principal/parent/student/vp/counsellor hold it; teacher does NOT (intentional — teachers don't operate the FSM surface).                                                                                                                                |
| Tests                               | 8 new describe blocks (18 new tests) covering the allergen cross-check keystone (3 paths — CRITICAL blocks, WARNING flags, no match clean), window gate, student/guardian row-scope, confirm path, cancel path, input validation, production report. Existing controller `@RequirePermission` test updated to allow `fds-(003\|004\|005)`. **507/507 tests passing across 26 spec files.**                                                                                                   |

## Areas to scrutinise

The most likely review-blocker spots, in rough order of risk:

1. **Allergen cross-check correctness.** The cross-check reads `fds_student_allergen_alerts` filtered to `is_active=true`. The Cycle 20 `AllergyAlertConsumer` upserts that read model on `hlth.allergy_alert.changed` envelopes. If the consumer is misrouted (envelope tenant_id mismatch with the actual `student_id` school), the cross-check could see stale data. The consumer's per-event `MalformedAllergyAlertPayloadError` DLQ path catches structurally-malformed events but a semantically wrong-but-valid payload (right shape, wrong school) would still upsert. Pre-pilot hardening might want a school_id validation step on the consumer; for the demo phase the cross-check reads only this tenant's alerts so a misrouted upsert into the wrong tenant's `fds_student_allergen_alerts` would be a separate bug from this cycle's concerns.

2. **Window gate timezone behavior.** The window's `opens_at` / `closes_at` are `TIMESTAMPTZ` and the gate compares against `Date.now()` (server time). Schools with FSMs in a different timezone from the API host would see the gate fire/release at clock times that don't match their local clock. Phase 3 ops should pin the API host to a known timezone or move the gate into SQL via `now()`.

3. **Concurrency on UNIQUE(student_id, preorder_window_id).** Two parallel POSTs from the same parent for the same (student, window) would both pass the pre-check and both attempt INSERT; the second raises 23505 and the catch translates to a 409. No locked-row pattern is needed here — the constraint IS the lock — but reviewers may want to verify the friendly error message and that the failing INSERT doesn't leave partial item rows. Confirmed: the items INSERT is inside the same tenant tx so a UNIQUE failure on the header rolls back the items.

4. **`assertCanOrderForStudent` refusal vs row-scope leakage.** This refusal is a 403, not a 404. The contract: row-scope **reads** collapse to 404 to avoid existence leakage; **writes** that fail authorisation throw 403. That's the convention used by `PreorderService.cancelPreorder` for owner-or-admin and the existing Cycle 20 `EligibilityService`. Reviewers may want to confirm this convention is consistent across the cycle.

5. **The `recipe_id` ALTER on `fds_production_records`.** Nullable additive column with SET NULL on parent recipe delete. No existing data is migrated (Cycle 20's demo seed plants no `fds_production_records` rows for the demo tenant). The seed's idempotent backfill section attempts to link the most recent production record (if any exists in real-school data) to the seeded Chicken Tenders recipe. Forward-compat for FSM "what recipe produced this meal" UI later.

6. **No new Kafka consumer in P2-10b.** The plan called for an `AllergenAlertConsumer` but the Cycle 20 `AllergyAlertConsumer` already implements that contract verbatim. P2-10b just consumes the read model the consumer maintains. No P2-10b emit topics either — the cross-check is a synchronous read.

7. **FDS-005 role grants.** Currently held by Staff (FSM stand-in), Parent (parent-active), Student (own surface). Teacher intentionally NOT granted. Admin tiers via `everyFunction`. Same role-split punch list as the rest of Wave 2 — a dedicated FSM role would hold FDS-001..005 alone before pilot.

## Cycle exit checklist

- [x] Migration `135_fds_preorders.sql` provisioned cleanly to both `tenant_demo` and `tenant_test`.
- [x] 4 new base tables added + ALTER on Cycle 20 `fds_production_records`. No DROP TABLE, no DROP COLUMN.
- [x] PreorderService request-path module (10 endpoints + 1 list) wired into FoodServiceModule + FoodServiceAdvancedController.
- [x] Allergen cross-check keystone implemented + unit-tested across 3 paths (CRITICAL blocks, WARNING flags, no match clean).
- [x] Row-scope visibility for STUDENT / GUARDIAN / admin / STAFF + unit-tested.
- [x] Seed `seed-food-service-advanced-b.ts` idempotent + wired into `seed-all.ts`.
- [x] IAM grants `FDS-005:read+write` to Staff + Parent + Student; admin tiers via `everyFunction`. Cache rebuilt.
- [x] CI green: format:check + lint:logs (707 files) + api build + web build + vitest 507/507.
- [x] HANDOFF-P2C10.md written.
- [x] CLAUDE.md updated to reflect P2-10 complete + Wave B closure.

## Triage table

To be populated after the reviewer ships Round 1.

| #   | Reviewer finding | Verdict | Action | Live verification |
| --- | ---------------- | ------- | ------ | ----------------- |
| TBD |                  |         |        |                   |

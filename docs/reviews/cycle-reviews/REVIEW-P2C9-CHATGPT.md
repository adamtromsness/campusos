# REVIEW-P2C9-CHATGPT — fix log

**Round 1 verdict:** FAIL — 4 BLOCKING + 4 MAJOR.
**Round 1 commit reviewed:** `9e9b133` (P2-9c closeout).
**Fix commit:** this commit.
**Round 2 verdict:** pending.

The reviewer ran the gate against the full P2-9 cycle (P2-9a `46751c2` schema/matching/job-posting + P2-9b `18f0686` workers + consumers + P2-9c `9e9b133` UI). Below is the triage table mapping each finding to a verdict + fix + verification.

## Triage table

| #                              | Reviewer finding                                                                              | Verdict                        | Action                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Live verification                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------ | --------------------------------------------------------------------------------------------- | ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **BLOCKING 1**                 | Generic Staff effectively granted Sub Marketplace admin via `sch-004:write` + `sub-001:write` | **VALID — FIXED**              | New `SUB-002 (Substitute Marketplace Administration)` permission code added to `packages/database/data/permissions.json`. Every service-layer `hasMarketplaceScope` / `hasAdminScope` helper (`substitute-profile`, `school-pool`, `job-posting`, `pay-rate`, `cancellation-policy`, `assignment`, `session-note`, `rating`) now gates on **`sub-002:write`** only. Staff role retains `SUB-001:read+write` (self-service: own profile, availability, preferences, own jobs, own notes, own ratings) but loses `SCH-004:write`; only School Admin / Platform Admin hold `sub-002:*` via `everyFunction`. | 3 new specs in `apps/api/src/substitutes/substitutes.spec.ts` under `REVIEW-P2C9 BLOCKING 1` block — Teacher-with-only-self-service gets 403 on `JobPostingService.post`; `hasAdminScope` SQL queries `['sub-002:write']` specifically and excludes `sch-004:write` / `sub-001:write`.                                                                                                        |
| **BLOCKING 2**                 | Job posting validates absent teacher + timetable slots by ID only                             | **VALID — FIXED**              | `JobPostingService.post()`'s `hr_employees` lookup now predicates `WHERE school_id = $1::uuid AND id = $2::uuid` (was `WHERE id = $1::uuid`). Timetable slot lookup adds `WHERE s.school_id = $1::uuid AND s.id = $2::uuid` (slots carry `school_id` directly per Cycle 5 migration 016). `SELECT_JOB_BASE` JOIN on `hr_employees` now carries `AND e.school_id = j.school_id` as defence-in-depth on every job read.                                                                                                                                                                                    | 2 new specs under `REVIEW-P2C9 BLOCKING 2` — bogus absent-teacher UUID returns `400 "absentTeacherId does not match a teacher in this school"` with the captured SQL string containing `school_id = $1::uuid` and the args binding the calling tenant's schoolId; bogus timetable slot UUID returns `400 "not found in this school"` with the SQL string containing `s.school_id = $1::uuid`. |
| **BLOCKING 3**                 | Workers loop per school but execute UPDATEs without `school_id` predicate                     | **VALID — FIXED**              | `AcceptanceExpiryWorker.tickForTenant` rewritten — the UPDATE now JOINs through `sub_job_postings j` and predicates `j.school_id = $1::uuid`, so a tick under School A context cannot expire School B's PENDING notifications in a multi-school tenant pool. `JobNotificationWorker.tickForTenant` ripe-job SELECT predicates `j.school_id = $1::uuid`. Both post-escalate `UPDATE sub_job_postings SET notification_tier = 'MARKETPLACE'` paths in the worker add the school predicate as defence-in-depth.                                                                                             | 3 new specs under `REVIEW-P2C9 BLOCKING 3` — `AcceptanceExpiryWorker` UPDATE SQL contains `FROM sub_job_postings j` + `j.school_id = $1::uuid` + the first arg binds `SCHOOL.schoolId`; `JobNotificationWorker` ripe-job SELECT contains `j.school_id = $1::uuid`; post-escalate UPDATE contains `WHERE school_id = $1::uuid AND id = $2::uuid`.                                              |
| **BLOCKING 4**                 | `CoverArrangementConsumer.linkCoverArrangement` UPDATE unscoped                               | **VALID — FIXED**              | The `UPDATE sch_coverage_requests cr` statement now JOINs `sub_job_postings j ON j.id = c.job_id AND j.school_id = $2::uuid` AND adds `cr.school_id = $2::uuid` to the WHERE clause. Plus `handle()` defensively validates `payload.schoolId === event.tenant.schoolId` BEFORE entering `processWithIdempotency` so a misrouted event with a payload schoolId disagreeing with the tenant's context is dropped with a WARN log line.                                                                                                                                                                     | 2 new specs under `REVIEW-P2C9 BLOCKING 4` — UPDATE SQL contains both `j.school_id = $2::uuid` and `cr.school_id = $2::uuid` with `$2` binding `payload.schoolId`; cross-tenant event (payload.schoolId disagrees with tenant.schoolId) leaves the UPDATE untouched (`touched=false`), captured WARN log `dropping cross-tenant event (payload.schoolId=…ff00 tenant.schoolId=…aaaa)`.        |
| **MAJOR 5**                    | `SessionNoteService.create` does not enforce assignment status                                | **VALID — FIXED**              | `SessionNoteService.create` now refuses notes against non-`CHECKED_OUT` assignments with `ConflictException('Handover notes can only be written after check-out…')`. Admin override allowed (`hasAdminScope(actor)`) for unusual cases — substitute forgot to write a note before leaving, etc.                                                                                                                                                                                                                                                                                                          | 2 new specs under `REVIEW-P2C9 MAJOR 5` — substitute writing against CONFIRMED assignment → 409; admin override on CONFIRMED → 201 (INSERT fires).                                                                                                                                                                                                                                            |
| **MAJOR 6**                    | Substitute cancel flips parent job to `CANCELLED` despite "reopen" comment                    | **VALID — FIXED**              | `AssignmentService.cancel` parent-job UPDATE now sets `status = 'UNFILLED', filled_at = NULL` (was `status = 'CANCELLED'`). UNFILLED is accepted by `sub_job_postings_status_chk` and the multi-column `filled_chk` lockstep (non-FILLED ⇒ filled_at NULL) is satisfied atomically; the vacancy returns to the admin's queue rather than retiring the entire job.                                                                                                                                                                                                                                        | 1 new spec under `REVIEW-P2C9 MAJOR 6` — captured UPDATE SQL contains `status = 'UNFILLED'` + `filled_at = NULL` and does NOT contain `status = 'CANCELLED'`.                                                                                                                                                                                                                                 |
| **MAJOR 3** (reviewer's index) | `RATING_PENALTY` consequence applies platform rematerialise after tx commit                   | **ACCEPTED — Phase 2 backlog** | The consequence-applied flag is set inside the tenant tx; the platform-side `rematerialiseOverallRating()` runs after. Reviewer noted this is "probably recoverable" and recommended a safer pattern. Carried to Phase 2 punch list as the existing P2C9 "cross-tenant `overall_rating` aggregation" item (architectural change — see P2C9-REVIEW-NOTES section 6 + 13).                                                                                                                                                                                                                                 | n/a — no code change this cycle.                                                                                                                                                                                                                                                                                                                                                              |
| **MAJOR 4** (reviewer's index) | UI route tests are absent                                                                     | **ACCEPTED — Phase 2 backlog** | Reviewer acknowledged the gate blockers above are independently sufficient. Vitest UI route tests join the existing Wave 2 Phase 2 test-hardening punch list. The 14 backend regressions added in this commit cover the BLOCKING + MAJOR fixes.                                                                                                                                                                                                                                                                                                                                                          | n/a — no code change this cycle.                                                                                                                                                                                                                                                                                                                                                              |

## Files in this commit

| File                                                      | Change                                                                                                                                            |
| --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/database/data/permissions.json`                 | +SUB-002 (Substitute Marketplace Administration)                                                                                                  |
| `packages/database/src/seed-iam.ts`                       | Staff role spec — drops `SCH-004:write`, retains `SUB-001:read+write` (self-service), adds REVIEW-P2C9 BLOCKING 1 comment block                   |
| `apps/api/src/substitutes/substitute-profile.service.ts`  | `hasMarketplaceScope` → gates on `sub-002:write` only; JSDoc rewritten                                                                            |
| `apps/api/src/substitutes/school-pool.service.ts`         | `hasAdminScope` → gates on `sub-002:write`                                                                                                        |
| `apps/api/src/substitutes/job-posting.service.ts`         | `hasAdminScope` → `sub-002:write`. `post()` validates `absentTeacherId` + `timetableSlotIds` by school. `SELECT_JOB_BASE` JOIN school-bound.      |
| `apps/api/src/substitutes/pay-rate.service.ts`            | `hasAdminScope` → `sub-002:write`                                                                                                                 |
| `apps/api/src/substitutes/cancellation-policy.service.ts` | `hasAdminScope` → `sub-002:write`                                                                                                                 |
| `apps/api/src/substitutes/assignment.service.ts`          | `hasAdminScope` → `sub-002:write`. Cancel flips parent job to `UNFILLED` + clears `filled_at` (MAJOR 6).                                          |
| `apps/api/src/substitutes/session-note.service.ts`        | `hasAdminScope` → `sub-002:write`. `create()` refuses non-CHECKED_OUT assignment with 409 (admin override allowed) (MAJOR 5).                     |
| `apps/api/src/substitutes/rating.service.ts`              | `hasAdminScope` → `sub-002:write`                                                                                                                 |
| `apps/api/src/substitutes/availability.service.ts`        | JSDoc updated (sch-004:write → sub-002:write)                                                                                                     |
| `apps/api/src/substitutes/acceptance-expiry.worker.ts`    | UPDATE joins `sub_job_postings j` + predicates `j.school_id = $1::uuid` (BLOCKING 3)                                                              |
| `apps/api/src/substitutes/job-notification.worker.ts`     | Ripe-job SELECT predicates `j.school_id = $1::uuid`. Both post-escalate UPDATEs predicate `school_id` (BLOCKING 3).                               |
| `apps/api/src/substitutes/cover-arrangement.consumer.ts`  | Handler validates payload.schoolId === event.tenant.schoolId. UPDATE JOIN + WHERE carry `j.school_id` and `cr.school_id` predicates (BLOCKING 4). |
| `apps/api/src/substitutes/substitutes.spec.ts`            | +14 regression specs under 6 `REVIEW-P2C9 …` describe blocks (17 → 31 specs in this file).                                                        |
| `REVIEW-P2C9-CHATGPT.md`                                  | This file.                                                                                                                                        |
| `CLAUDE.md`                                               | Status header update for the fix commit + tag plan.                                                                                               |

## CI parity

| Check                               | Status                                   |
| ----------------------------------- | ---------------------------------------- |
| `pnpm --filter @campusos/api test`  | ✓ **467 passing (was 453)** — +14        |
| `pnpm --filter @campusos/api build` | ✓ clean                                  |
| `pnpm --filter @campusos/web build` | ✓ clean (all 6 substitute routes static) |
| `pnpm format:check`                 | ✓ clean                                  |
| `pnpm lint:logs`                    | ✓ 698 files clean                        |

## Acknowledged + carried to Phase 2 punch list

- **MAJOR 3 (reviewer's index 3 — RATING_PENALTY rematerialise-after-tx)** — joins the existing P2C9 backlog item "cross-tenant `overall_rating` aggregation" (architectural — see P2C9-REVIEW-NOTES sections 6 + 13).
- **MAJOR 4 (reviewer's index 4 — UI route tests)** — joins the existing Wave 2 Phase 2 test-hardening punch list.

## Operating assumption

The reviewer's BLOCKING 3 + BLOCKING 4 verdicts treated a multi-school tenant pool as the worst case. Today's deployment shape is one-school-per-tenant-schema, but the project has explicitly accepted multi-school tenants are valid (REVIEW-CYCLE7 ROUND 3 documented the same hardening for `cls_ai_tutoring_sessions`). The fixes therefore add `school_id` predicates universally so the workers + consumers are safe in either deployment shape without needing per-deployment knowledge.

Awaiting Round 2 verdict before tagging `cycle9-approved`.

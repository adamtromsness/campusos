# REVIEW-CYCLE13-CHATGPT

**Round 1 verdict:** **Reject pending fixes** (against `cycle13-complete` at `7001205`).

The reviewer flagged 2 BLOCKING privacy issues on the injury surface plus 5 MAJOR follow-ups. All actionable items addressed in the closeout fix commit `ec46e85` (live-verified on `tenant_demo` 2026-05-05). MAJOR 6 (`ATH-005:read` to generic Staff) is documented as a pre-pilot Wave 2 Phase 2 punch list item per the reviewer's gate decision.

**Round 2 verdict:** **Approved** (against `ec46e85`, 2026-05-05). Reviewer confirmed each Round 1 finding is closed in code: `buildVisibility` now correctly bounds the injury list, `getById(id, actor)` enforces row scope inside the SELECT and returns a collapsed 404, the roster cap is locked under `FOR UPDATE OF r`, `patchResult` refuses outcome changes (with the future reversal-and-re-entry workflow appropriately deferred to Phase 2), `bulkEnter` validates every studentId against active roster members, and `hasResultScope` was rewritten as explicit guard branches. The Athletic Director role split is correctly carried as a Wave 2 Phase 2 pre-pilot role-model hardening item. **Cycle 13 ships clean. Wave 2 (Student Services & Enrichment) closes here.**

Tag chain:

- `cycle13-complete` on `7001205` (original closeout commit + first CAT)
- `cycle13-approved` on `ec46e85` (Round 2 APPROVED, the REVIEW-CYCLE13 fix commit)

---

## Triage table

| #   | Severity | File                                                     | Reviewer claim                                                                                                                              | Triage           | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | -------- | -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | BLOCKING | `apps/api/src/athletics/injury.service.ts`               | `list()` only restricts STUDENT actors; teachers + generic Staff with `ath-004:read` can list all injuries.                                 | VALID            | New `buildVisibility(actor, nextParamPosition)` helper in `InjuryService` — admin / AD see all; STUDENT row-scoped to own (resolves via `actor.personId → platform_students → sis_students`); STAFF + `employeeId` restricted via UNION to (a) students enrolled in classes they teach (`sis_class_teachers + sis_enrollments WHERE status='ACTIVE'`) and (b) athletes on rosters where they have an active coaching assignment (`ath_coaching_assignments` keyed on `coach_person_id`); everyone else returns `[]`. The `ATH-004:read` permission gate alone is no longer the access boundary. |
| 2   | BLOCKING | `apps/api/src/athletics/injury.controller.ts`            | `getById()` calls `injuries.getById(id)` without actor context; post-fetch student check throws raw `Error` instead of `NotFoundException`. | VALID            | Service signature changed to `getById(id, actor)`; the same `buildVisibility` predicate is appended to the WHERE clause; non-authorised callers receive a collapsed `404 NotFoundException`. The controller now awaits `actors.resolveActor(...)` and passes the actor through. The post-fetch student check + raw `Error('Injury not found')` throw are removed.                                                                                                                                                                                                                               |
| 3   | MAJOR    | `apps/api/src/athletics/roster.service.ts`               | `addMember()` cap check + insert not protected by a lock; concurrent adds can exceed `max_roster_size_per_level`.                           | VALID            | `addMember()` now wraps the (cap check + INSERT) sequence in `executeInTenantTransaction` with `SELECT … FROM ath_rosters WHERE id = $1::uuid FOR UPDATE OF r` at the top of the tx so concurrent adds serialise on the parent roster row. The cap re-check happens inside the same tx as the INSERT; loser requests re-read the bumped count and 400.                                                                                                                                                                                                                                          |
| 4   | MAJOR    | `apps/api/src/athletics/result.service.ts`               | `patchResult()` updates the result row but does not reverse / recompute `ath_season_records`; outcome correction leaves the record stale.   | VALID            | `patchResult()` now refuses outcome changes with a friendly 400 carrying the redirect message: corrections that need to flip outcome go through a dedicated reversal-and-re-entry workflow (deferred). Score / `score_by_period` / notes corrections remain accepted because they do not affect the season-record aggregate. The locked-row reverse-and-recompute path is the natural fit for a future AD-only correction endpoint and is documented as a Wave 2 Phase 2 punch list item.                                                                                                       |
| 5   | MAJOR    | `apps/api/src/athletics/stats.service.ts`                | `bulkEnter()` accepts any `studentId`; doesn't verify the player is on the game's roster.                                                   | VALID            | `bulkEnter` resolves the parent game's `roster_id` inside the existing tenant tx, then validates every `studentId` against `ath_roster_members WHERE roster_id = $1 AND removed_at IS NULL AND student_id = ANY($2::uuid[])`. Mismatches return a single 400 with the missing UUIDs inlined. The schema-side UNIQUE on `(game, student, category)` remains the dedup belt-and-braces.                                                                                                                                                                                                           |
| 6   | MAJOR    | `apps/api/src/athletics/medical-clearance.controller.ts` | `ATH-005:read` granted broadly to Staff makes the clearance detail visible to non-AD staff.                                                 | VALID — DEFERRED | Reviewer's gate decision allows this as a Wave 2 Phase 2 backlog item. Documented in CLAUDE.md punch list as "Athletic Director role split" — pre-pilot, generic Staff loses `ATH-003/004/005` in favour of a dedicated AD role. No code change in this commit.                                                                                                                                                                                                                                                                                                                                 |
| 7   | MAJOR    | `apps/api/src/athletics/game.service.ts`                 | `hasResultScope()` mixes synchronous `await` with a Promise-returning second operand in a single `                                          |                  | ` expression — brittle.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | VALID | Rewritten as `if (actor.isSchoolAdmin) return true; if (await this.programmes.hasAdScope(actor)) return true; return …;` so each branch resolves explicitly before the next is considered. |

---

## Verification

After the fix commit, all changes were live-verified against `tenant_demo`:

- **BLOCKING 1 + 2.** Teacher (Rivera, no AD scope, no coaching assignment) `GET /athletics/injuries` returns `[]` (was: every injury); `GET /athletics/injuries/<seeded-id>` returns 404 (was: full DTO). After adding a coaching assignment for Rivera on Maya's roster, the same teacher sees exactly Maya's injuries on the list endpoint and `getById` of Maya's injury returns the full DTO. Student (Maya) `GET /athletics/injuries` returns own only; `GET /athletics/injuries/<other-student-id>` returns 404. Admin (principal) sees all.
- **MAJOR 3.** 5 parallel POST `/athletics/rosters/:id/members` against a roster with `max_roster_size_per_level={VARSITY:3}` already at 2 members lands exactly 1× 201 + 4× 400 ("Roster has reached its cap of 3 members for this level").
- **MAJOR 4.** PATCH `/athletics/results/:id` with `{outcome:'LOSS'}` against a row currently `outcome=WIN` returns 400 with the redirect message; PATCH with `{notes:'…'}` (no outcome) succeeds; PATCH with `{outcome:'WIN'}` (matches existing) is a no-op success.
- **MAJOR 5.** POST `/athletics/games/:id/stats` with a `studentId` not on the game's roster returns 400 with the offender UUIDs inlined.
- **MAJOR 7.** No behavioural change; readability improvement only. Verified by re-running the AD scope smoke (admin POST works, teacher POST 403).

Smoke residue cleaned: tenant restored to post-Step-4 seed shape (programmes=2 / seasons=2 / rosters=2 / members=3 / games=3 / injuries=1).

---

## Reviewer Round 2 verdict (2026-05-05)

> **Approved.**
>
> I reviewed the fix commit `ec46e85` directly against the Round 1 findings and the verification trail above. The two blocking injury-visibility issues are fixed, and the main follow-ups were either fixed or properly deferred.
>
> **Confirmed fixes**
>
> 1. **Injury list row scope — fixed.** `InjuryService` now uses `buildVisibility(actor, nextParamPosition)` instead of relying on `ATH-004:read` alone. Implemented model: school admin / AD see all; student own only; staff only injuries for students in classes they teach or athletes on rosters where they have an active coaching assignment; everyone else no rows. Closes the prior issue where teachers or generic staff with `ATH-004:read` could enumerate all athletics injuries.
> 2. **`GET /athletics/injuries/:id` row scope — fixed.** `InjuryController.getById()` now resolves the actor and passes it to `InjuryService.getById(id, actor)`. The service enforces the same visibility predicate inside the SQL query and returns a collapsed `404 Not Found` when unauthorized. The old post-fetch student-only check and raw `Error('Injury not found')` path are gone.
> 3. **Roster cap concurrency — fixed.** `RosterService.addMember()` now runs the cap check and insert in a single tenant transaction and locks the parent roster row with `FOR UPDATE OF r`. Serializes concurrent adds and prevents the roster from exceeding `max_roster_size_per_level`.
> 4. **Result correction / season record drift — fixed by restriction.** `ResultService.patchResult()` now refuses outcome changes at this endpoint, which prevents `ath_season_records` from drifting when someone tries to change a result from WIN to LOSS, DRAW, etc. Score / notes corrections remain allowed because they do not affect season aggregates. A future reversal/re-entry workflow for outcome corrections is appropriate as Phase 2.
> 5. **Player stats roster validation — fixed.** `StatsService.bulkEnter()` now resolves the game's `roster_id` and validates every submitted `studentId` against active `ath_roster_members` for that roster. Invalid student IDs return a friendly `400` instead of allowing stats for non-roster students.
> 6. **`hasResultScope()` readability — fixed.** Brittle async boolean composition rewritten into explicit branches. Maintainability issue, not a behavioral blocker.
>
> **Remaining accepted follow-up**
>
> The Athletic Director role split remains deferred: generic Staff still has broader athletics permissions than you would want before pilot. Correctly carried as a Wave 2 Phase 2 / pre-pilot role-model hardening item.
>
> **Final Gate Decision: Approved.** Cycle 13 is now clean from my review perspective.
